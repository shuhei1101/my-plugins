# gh-kit 規約: 外部ライブラリ

採用した外部ライブラリ 1 つにつき 1 ページの Wiki を作成し、**そのプロジェクトで実際に使う部分だけ** を公式ドキュメントから抽出して整理する。

公式ドキュメントは情報量が多く読みづらいため、ここを見るだけで実装に必要な情報（インストール手順 + 使うメソッドのパラメータ）が揃う状態を目指す。

配置先は `docs/wiki/外部ライブラリ/{ライブラリ名}.md`（例: `docs/wiki/外部ライブラリ/react-image-crop.md`）。新規作成時は `docs/wiki/外部ライブラリ/README.md`（一覧）に行を追加してリンクを張る。

各セクションの書き方を「記述例 + 補足」で定義する。

## 担当セクション一覧

| No | セクション                | サブセクション                        | 必須or条件                                  | 担当                  | 補足 |
| -- | ------------------------- | ------------------------------------- | ------------------------------------------- | --------------------- | ---- |
| 1  | `## 概要`                 | -                                     | 必須                                        | issue-arch / issue-ui | -    |
| 2  | `## 現在のバージョン情報` | -                                     | 必須                                        | 〃                    | -    |
| 3  | `## インストール手順`     | -                                     | 必須                                        | 〃                    | -    |
| 4  | `## メソッド一覧`         | `### {メソッド名 / コンポーネント名}` | 必須（一覧表の各行につき 1 サブセクション） | 〃                    | -    |

## `## 概要`

### 記述例

```markdown
## 概要

React 用の画像トリミング（クロップ）UI ライブラリ。

矩形・円形・自由比率のトリミング選択と、選択範囲のリアルタイムプレビューを提供する。
出力はトリミング後の Blob / dataURL。
```

### 補足

- ライブラリが何をするものかを 1〜3 行で要約（自プロジェクトでの使い道に絞った言葉で）

## `## 現在のバージョン情報`

### 記述例

```markdown
## 現在のバージョン情報

| 項目         | 内容                                              |
| ------------ | ------------------------------------------------- |
| バージョン   | `v11.0.5`                                         |
| ライセンス   | MIT                                               |
| 公式 URL     | https://github.com/DominicTobias/react-image-crop |
| 公式ドキュメント | https://github.com/DominicTobias/react-image-crop#readme |
```

### 補足

- バージョンは採用時点で固定値を記録。アップグレードしたらここを更新（メソッド・パラメータも追従して更新する）
- ライセンスは商用利用可否を確認した上で記入（MIT / Apache-2.0 / GPL など）
- 公式 URL は GitHub / npm / 公式サイトのいずれか主要なもの 1 つ
- 公式ドキュメントは API リファレンスがある URL を直接

## `## インストール手順`

### 記述例

````markdown
## インストール手順

```bash
npm install react-image-crop@11.0.5
```

CSS の読み込みも必要:

```ts
import "react-image-crop/dist/ReactCrop.css";
```
````

### 補足

- パッケージマネージャに応じた具体コマンド（npm / pnpm / yarn / uv / pip など）
- バージョンを `@x.y.z` で固定指定
- ピア依存・追加設定（CSS インポート / Tailwind 設定 / 型定義 / 環境変数 など）があれば併記

## `## メソッド一覧`

このプロジェクトで使うメソッド / コンポーネントを 1 表で俯瞰し、表の下に各メソッドの詳細サブセクション（`### {名前}`）を並べる。

### 記述例

````markdown
## メソッド一覧

バージョン: `v11.0.5`

| No | 種別           | 名前               | 用途                                       | 補足 |
| -- | -------------- | ------------------ | ------------------------------------------ | ---- |
| 1  | コンポーネント | `<ReactCrop />`    | クロップ UI を表示するメインコンポーネント | -    |
| 2  | 関数           | `centerCrop()`     | クロップ範囲を画像中央に配置               | -    |
| 3  | 関数           | `makeAspectCrop()` | アスペクト比を強制したクロップ範囲を生成   | -    |

### `<ReactCrop />`

クロップ UI を表示するメインコンポーネント。

**パラメータ**

| No  | パラメータ      | 型                                             | 必須 | 既定        | 説明                                    | 例                                                   | 補足                                       |
| --- | --------------- | ---------------------------------------------- | ---- | ----------- | --------------------------------------- | ---------------------------------------------------- | ------------------------------------------ |
| 1   | `crop`          | `Crop`                                         | 任意 | `undefined` | 現在のクロップ範囲（controlled 利用時） | `{ unit: '%', x: 25, y: 25, width: 50, height: 50 }` | 子フィールドは下の 1-1〜1-5 参照           |
| 1-1 | `crop.unit`     | `'%' or 'px'`                                  | 必須 | -           | 座標・サイズの単位                      | `'%'`                                                | `%`=画像幅比 / `px`=画素絶対値             |
| 1-2 | `crop.x`        | `number`                                       | 必須 | -           | 左上 X 座標                             | `25`                                                 | -                                          |
| 1-3 | `crop.y`        | `number`                                       | 必須 | -           | 左上 Y 座標                             | `25`                                                 | -                                          |
| 1-4 | `crop.width`    | `number`                                       | 必須 | -           | 幅                                      | `50`                                                 | -                                          |
| 1-5 | `crop.height`   | `number`                                       | 必須 | -           | 高さ                                    | `50`                                                 | -                                          |
| 2   | `onChange`      | `(crop: PixelCrop, percentCrop: Crop) => void` | 必須 | -           | クロップ範囲変更時のコールバック        | `(c) => setCrop(c)`                                  | -                                          |
| 3   | `onComplete`    | `(crop: PixelCrop) => void`                    | 任意 | `undefined` | ドラッグ終了時の確定コールバック        | `(c) => commit(c)`                                   | プレビュー画像生成用                       |
| 4   | `aspect`        | `number`                                       | 任意 | `undefined` | アスペクト比固定                        | `1`                                                  | `1`=正方形 / `16/9`=横長 / 未指定=自由比率 |
| 5   | `circularCrop`  | `boolean`                                      | 任意 | `false`     | 円形クロップ表示                        | `true`                                               | 出力データ自体は矩形                       |
| 6   | `keepSelection` | `boolean`                                      | 任意 | `false`     | クリック時に選択範囲を保持              | `true`                                               | -                                          |
| 7   | `disabled`      | `boolean`                                      | 任意 | `false`     | クロップ操作を無効化                    | `true`                                               | -                                          |
| 8   | `minWidth`      | `number`                                       | 任意 | `0`         | 最小幅 (px)                             | `50`                                                 | -                                          |
| 9   | `maxWidth`      | `number`                                       | 任意 | `undefined` | 最大幅 (px)                             | `500`                                                | -                                          |

**使用例**

```tsx
<ReactCrop crop={crop} onChange={(c) => setCrop(c)} aspect={1}>
  <img src={src} alt="" />
</ReactCrop>
```

### `centerCrop(crop, mediaWidth, mediaHeight)`

クロップ範囲を画像中央に配置する。

**パラメータ**

| No  | パラメータ    | 型            | 必須 | 既定 | 説明                       | 例                                                 | 補足                             |
| --- | ------------- | ------------- | ---- | ---- | -------------------------- | -------------------------------------------------- | -------------------------------- |
| 1   | `crop`        | `Crop`        | 必須 | -    | 中央配置したいクロップ範囲 | `{ unit: '%', x: 0, y: 0, width: 50, height: 50 }` | 子フィールドは下の 1-1〜1-5 参照 |
| 1-1 | `crop.unit`   | `'%' or 'px'` | 必須 | -    | 座標・サイズの単位         | `'%'`                                              | `%`=画像幅比 / `px`=画素絶対値   |
| 1-2 | `crop.x`      | `number`      | 必須 | -    | 左上 X 座標                | `0`                                                | -                                |
| 1-3 | `crop.y`      | `number`      | 必須 | -    | 左上 Y 座標                | `0`                                                | -                                |
| 1-4 | `crop.width`  | `number`      | 必須 | -    | 幅                         | `50`                                               | -                                |
| 1-5 | `crop.height` | `number`      | 必須 | -    | 高さ                       | `50`                                               | -                                |
| 2   | `mediaWidth`  | `number`      | 必須 | -    | 画像の幅 (px)              | `1280`                                             | -                                |
| 3   | `mediaHeight` | `number`      | 必須 | -    | 画像の高さ (px)            | `720`                                              | -                                |

**戻り値**

| 型     | 説明                       | 例                                  |
| ------ | -------------------------- | ----------------------------------- |
| `Crop` | 中央配置済みのクロップ範囲 | `{ unit: '%', x: 25, y: 25, ... }`  |

**使用例**

```tsx
const initial = centerCrop({ unit: "%", width: 50 }, 1280, 720);
```

### `makeAspectCrop(crop, aspect, mediaWidth, mediaHeight)`

アスペクト比を強制したクロップ範囲を生成する。

**パラメータ**

| No  | パラメータ    | 型              | 必須 | 既定        | 説明                 | 例                         | 補足                             |
| --- | ------------- | --------------- | ---- | ----------- | -------------------- | -------------------------- | -------------------------------- |
| 1   | `crop`        | `Partial<Crop>` | 必須 | -           | ベースのクロップ範囲 | `{ unit: '%', width: 50 }` | 子フィールドは下の 1-1〜1-5 参照 |
| 1-1 | `crop.unit`   | `'%' or 'px'`   | 必須 | -           | 座標・サイズの単位   | `'%'`                      | `%`=画像幅比 / `px`=画素絶対値   |
| 1-2 | `crop.x`      | `number`        | 任意 | `0`         | 左上 X 座標          | `0`                        | -                                |
| 1-3 | `crop.y`      | `number`        | 任意 | `0`         | 左上 Y 座標          | `0`                        | -                                |
| 1-4 | `crop.width`  | `number`        | 任意 | `undefined` | 幅                   | `50`                       | width / height のどちらか必須    |
| 1-5 | `crop.height` | `number`        | 任意 | `undefined` | 高さ                 | -                          | 〃                               |
| 2   | `aspect`      | `number`        | 必須 | -           | アスペクト比         | `16/9`                     | `width/height` で計算            |
| 3   | `mediaWidth`  | `number`        | 必須 | -           | 画像の幅 (px)        | `1280`                     | -                                |
| 4   | `mediaHeight` | `number`        | 必須 | -           | 画像の高さ (px)      | `720`                      | -                                |

**戻り値**

| 型     | 説明                         | 例                                  |
| ------ | ---------------------------- | ----------------------------------- |
| `Crop` | アスペクト比強制済みの範囲   | `{ unit: '%', width: 50, height: 28.125, ... }` |

**使用例**

```tsx
const c = makeAspectCrop({ unit: "%", width: 50 }, 16 / 9, 1280, 720);
```
````

### 補足

**一覧表の種別列:**
- `コンポーネント` / `関数` / `フック` / `クラス` / `定数` など

**一覧表の用途列:**
- 1 行で要約。詳細パラメータは書かない（下の `### {名前}` サブセクション側で書く）

**各メソッドサブセクション:**
- 一覧表の各行につき 1 つ作成（`### {メソッド名 / コンポーネント名}`）
- 構成: 1〜2 行の説明 → **パラメータ**（表）→ **戻り値**（表）→ **使用例**（コード）
- パラメータ表カラム: `No` / `パラメータ` / `型` / `必須` / `既定` / `説明` / `例` / `補足` の 8 列固定
- 戻り値表カラム: `型` / `説明` / `例` の 3 列（戻り値が自明なコンポーネントなどは省略可）
- 使用例は import 文込みの最小動作版（5〜15 行程度）

**パラメータの網羅性:**
- 「現在使っているパラメータ」だけでなく **主要パラメータも全部** 載せる
- 理由: 採用後にチューニングで別パラメータを足す可能性が高く、その都度公式ドキュメントを引き直すのを避けたい
- 採用判断時に「他にどんなパラメータがあるか」が分かる状態にしておく
- 公式ドキュメント全列挙ではなく **主要 = 通常使われる範囲** に絞る（明らかに使わない upstream-only / deprecated は除外）

**「既定」列の書き方:**
- 任意パラメータの省略時デフォルト値を書く（例: `false` / `0` / `undefined` / `'auto'`）
- 必須パラメータは `-` 固定
- 公式が明示してない場合は `undefined` または `-`

**「例」列の書き方:**
- 実装でそのまま使えそうな具体値を書く（ダミー値ではなく現実的な値）
- 長い型は `{ a: 1, ... }` で省略可

**enum パラメータ（値で動作が変わるもの）の書き方:**
- 型列: `'a' or 'b' or 'c'` で取りうる値を列挙
- 例列: 代表値 1 つ（例: `'a'`）
- 補足列: 各値の意味を `a=～ / b=～ / c=～` 形式で書く
- 値が多い（5 個以上など）ときのみ別表 / 別サブセクションに切り出してよい

**データクラス型パラメータ（オブジェクト 1 つを受け取るもの）の書き方:**
- ライブラリは型エイリアスやデータクラス（`Crop` / `Options` など）でパラメータをまとめて渡すケースが多い
- **行を追加して展開する**（API 側の入れ子ルールと同じ）
  - 親行: 型を `Crop` で書き、例列にオブジェクトリテラル全体、補足列に「子フィールドは下の N-X 参照」
  - 子行: `crop.unit` / `crop.x` のように `parent.child` 形式で各フィールドを 1 行ずつ
  - No 列の枝番（`1` / `1-1` / `1-2`）で親子関係を表現
- **展開は 1 段目まで**（読みやすさ優先）
  - 子フィールドの型がさらに別クラスの場合、孫フィールドは展開しない
  - 孫の構造は子行の補足列に型定義 `Sub = { ... }` で書く
- フィールドが多い（10 個以上） / 同じ型を複数メソッドで使う場合は、別サブセクション `### {型名} 型` に切り出して各メソッドからは「`Crop` 型は下の `### Crop 型` 参照」で済ます

**入れ子（オブジェクト / 配列の中身）の書き方:**
- パラメータ列の表記:
  - オブジェクトの子: `parent.child`
  - 配列要素の子: `parent[].child`
- 親行の補足列に「子フィールドは下の N-X 参照」と書いて誘導する
- 例列: 親行は全体の例、子行はその値だけ

**補足:**
- **メソッド単位では** このプロジェクトで実際に使うものだけ抜粋（使わないメソッド自体は書かない）
- 採用メソッドのパラメータは網羅的に書く（上記「パラメータの網羅性」参照）
- 表の上に **採用しているバージョン** を必ず明記。アップグレード時の見直しトリガーになる
- 表とサブセクションの順序を揃える（使用頻度の高い順 or 依存順）
