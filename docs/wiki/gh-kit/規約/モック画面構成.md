# gh-kit: モック画面構成規約

モック画面（HTML / CSS / JS）の置き場所と命名の規約。
プロジェクトの **feature ブランチ** で `docs/mock/` 配下に作成し、master マージ後は GitHub Pages（Branch: `master`、Folder: `/docs`）で配信する。
**開発中のモック確認は `raw.githack.com`** で feature ブランチの HTML を直接描画して見る（後述「配信方針」参照）。

新規モック画面を作るときは「ディレクトリ構成」のツリーに沿って新しいディレクトリを切り、上位の索引（`docs/mock/pages/index.html` または `docs/mock/components/index.html`）にリンクを 1 行追加する。

## 配信方針

| 状態 | 配信方法 | URL 例 |
| ---- | -------- | ------ |
| 開発中（feature ブランチ） | **raw.githack.com**（CDN で raw HTML を描画） | `https://raw.githack.com/shuhei1101/my-plugins/{ブランチ名}/docs/mock/pages/{画面名}/issues/{N}/{案名}/index.html` |
| 確定後（master マージ後） | GitHub Pages | `https://shuhei1101.github.io/my-plugins/mock/pages/{画面名}/issues/{N}/{案名}/` |

### raw.githack.com の使い方

- pr-ui フェーズでモック画面を作成 → feature ブランチに push
- raw.githack.com の URL でブラウザから直接描画 → Issue / PR コメントに URL を貼る
- 外部 CSS / JS 参照（`assets/style.css` 等）も同じ raw.githack.com 経由で解決される
- ブランチごとに独立 URL になるため、PR ごとに別モックを並行検討できる

## ディレクトリ構成

```
docs/
├── index.html                                          # Pages のトップページ（ハブ）
└── mock/
    ├── index.html                                      # モック索引（pages / components 入口）
    ├── assets/
    │   ├── style.css                                   # 共通スタイル
    │   └── app.js                                      # 共通スクリプト
    ├── pages/
    │   ├── index.html                                  # 画面索引（画面リスト + 現役採用案リンク）
    │   └── {画面名}/
    │       ├── index.html                              # Issue 一覧（最新順）
    │       └── issues/
    │           └── {issue番号}/
    │               ├── index.html                      # 当該 Issue の案リスト（採用マーク付き）
    │               └── {案名}/
    │                   ├── index.html                  # 案の実画面
    │                   └── {派生案名}/                 # 任意。派生案はネスト可
    │                       └── index.html              # 派生案の実画面
    ├── components/
    │   ├── index.html                                  # コンポーネント索引（リスト）
    │   └── {コンポーネント名}/
    │       └── index.html                              # 全案をフラットに 1 ページにまとめたカタログ
    ├── styles/
    │   ├── index.html                                  # スタイル一覧（採用 + 候補）
    │   └── {スタイル名}/
    │       └── index.html                              # 当該スタイルのサンプル画面
    └── issues/
        ├── index.html                                  # Issue 索引（最新順）
        └── {issue番号}/
            └── index.html                              # 当該 Issue で作った画面モックへのリンク集
```

`{画面名}` `{案名}` `{派生案名}` `{コンポーネント名}` `{スタイル名}` は kebab-case。
例: `customer-list`、`a-left-filter`、`with-bulk-edit`、`topbar`、`modern-light`。

`{issue番号}` は GitHub Issue 番号をそのまま使う（例: `50`、`123`）。
`pages/{画面名}/50/` と `issues/50/` が同じ番号で対応し、相互に辿りやすくなる。

画面とコンポーネントで階層方針が異なる:
- 画面: Issue ごとに案を分ける（`pages/{画面名}/issues/{issue番号}/{案名}/`）。
  - 同じ画面でも新しい Issue で改修が入ると新しい `issues/{issue番号}/` を切り、当該 Issue で複数案を検討・採用する。
- コンポーネント: 案を別ページに分けず、`components/{コンポーネント名}/index.html` 1 ページに全案を並べる。
  - ボタンやサイドバーのように単体の見た目で比較できるものが対象で、Issue 単位の改修概念も持たない。

## `index.html` の扱い

各階層に `index.html` を置く理由は、当該ディレクトリの URL（末尾 `/`）でブラウザが自動的に開けるようにするため。

| No  | 階層                                          | `index.html` の中身  | 補足 |
| --- | --------------------------------------------- | -------------------- | ---- |
| 1   | `pages/`                                      | 画面索引             | -    |
| 2   | `pages/{画面名}/`                             | Issue 一覧           | -    |
| 3   | `pages/{画面名}/issues/{issue番号}/`          | 案リスト             | -    |
| 4   | `pages/{画面名}/issues/{issue番号}/{案名}/`   | 案の実画面           | -    |
| 4-1 | `pages/{画面名}/issues/{issue番号}/{案名}/{派生案名}/...` | 派生案の実画面（さらにネスト可能）。共通アセットへの相対パスはネスト 1 段ごとに `..` を 1 個増やす | -    |
| 5   | `components/`                                 | コンポーネント索引   | -    |
| 6   | `components/{コンポーネント名}/`              | 全案カタログ         | -    |
| 7   | `styles/`                                     | スタイル一覧         | -    |
| 8   | `styles/{スタイル名}/`                        | 当該スタイルのサンプル画面 | -    |
| 9   | `issues/`                                     | Issue 索引           | -    |
| 10  | `issues/{issue番号}/`                         | 当該 Issue の画面モックへのリンク集 | -    |

## `docs/`

GitHub Pages の公開ルート。
モック以外のコーナーが将来増える可能性があるため、`docs/` 直下にはハブだけを置き、各コーナーはサブディレクトリで分ける。

| No  | 配置物              | 説明                                                            | 補足 |
| --- | ------------------- | --------------------------------------------------------------- | ---- |
| 1   | `index.html`        | 全コーナーへの導線を持つトップページ。デザインは共通スタイル流用 | -    |
| 2   | `{コーナー名}/`     | 各コーナーのルート。現状は `mock/` のみ                          | -    |

## `docs/mock/`

モックコーナーのルート。
索引ページ・共通アセット・画面束ね・コンポーネント束ね・Issue 横断ビューを置く。

| No  | 配置物              | 説明                                                                                                        | 補足 |
| --- | ------------------- | ----------------------------------------------------------------------------------------------------------- | ---- |
| 1   | `index.html`        | モック索引。`pages/index.html` `components/index.html` `styles/index.html` `issues/index.html` への入口リンクを置く | -    |
| 2   | `assets/`           | 全モックで共有する CSS / JS                                                                                  | -    |
| 3   | `pages/`            | 画面モックの束ね。配下に画面ディレクトリを並べる                                                             | -    |
| 4   | `components/`       | 共通コンポーネントのモックの束ね。配下にコンポーネントディレクトリを並べる                                   | -    |
| 5   | `styles/`           | 画面全体のデザインスタイルの管理。採用 1 案 + 候補案を画面で見比べる                                          | -    |
| 6   | `issues/`           | Issue 単位の横断ビュー。1 Issue で複数画面を扱う場合に、当該 Issue から各画面モックへ最短で辿れるようにする   | -    |

## `docs/mock/assets/`

モック画面で共有する静的アセット。
画面ごとに独立した CSS / JS を置きたい場合のみ、当該画面ディレクトリ配下に追加する。

| No  | 配置物         | 説明                                                                    | 補足 |
| --- | -------------- | ----------------------------------------------------------------------- | ---- |
| 1   | `style.css`    | 全モック画面共通のスタイル。デザイントークン（カラー / 余白 / 角丸）を CSS 変数で定義 | -    |
| 2   | `app.js`       | 全モック画面共通のスクリプト。各画面で必要な処理だけが発火するよう DOM 有無で分岐 | -    |

## `docs/mock/pages/`

画面モックの束ね。索引ページと画面ディレクトリだけを置く。

| No  | 配置物          | 説明                                                                                                  | 補足 |
| --- | --------------- | ----------------------------------------------------------------------------------------------------- | ---- |
| 1   | `index.html`    | 画面索引。画面リストを表示し、各画面の名前 + 現役採用案へのリンクを並べる。形式はリストでもテーブルでもよい | -    |
| 2   | `{画面名}/`     | 1 画面 1 ディレクトリ                                                                                  | -    |

## `docs/mock/pages/{画面名}/`

当該画面に紐づく Issue の一覧。Issue ディレクトリを最新順（番号降順）に並べる。

| No  | 配置物          | 説明                                                                                                                                            | 補足 |
| --- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| 1   | `index.html`    | Issue 一覧。各 Issue を最新順に並べる。「Issue 番号・タイトル・採用案」が分かる粒度で記載。形式はリストでもテーブルでも何でもよい                  | -    |
| 2   | `{issue番号}/`  | 1 Issue 1 ディレクトリ                                                                                                                           | -    |

## `docs/mock/pages/{画面名}/issues/`

当該画面の Issue ディレクトリを束ねる中間階層。
このディレクトリには `index.html` を置かない（Issue 一覧は親の `pages/{画面名}/index.html` が担当する）。

| No  | 配置物            | 説明                                                                                                                                  | 補足 |
| --- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| 1   | `{issue番号}/`    | 1 Issue 1 ディレクトリ                                                                                                                 | -    |

## `docs/mock/pages/{画面名}/issues/{issue番号}/`

1 Issue で検討した案の集まり。各案へのリンクを並べ、採用案には採用マーク（`badge ok` 等）を付ける。

| No  | 配置物            | 説明                                                                                                                              | 補足 |
| --- | ----------------- | --------------------------------------------------------------------------------------------------------------------------------- | ---- |
| 1   | `index.html`      | 当該 Issue の案リスト。カード形式で縦に並べる。派生案も入れ子にせずフラットに並べ、ディレクトリ階層は表示名のパス表記（例: `a-left-filter / with-bulk-edit`）で示す。並び順は kebab-case の昇順。各カードには「案名（または派生パス）+ 1〜2 行の日本語の説明 + リンク」を載せ、採用案には採用マークを付ける。比較表・採用理由・良い点 / 懸念点などのメタ情報は不要。共通アセットを `../../../../assets/` で参照 | -    |
| 2   | `{案名}/`         | 1 案 1 ディレクトリ                                                                                                                | -    |

## `docs/mock/pages/{画面名}/issues/{issue番号}/{案名}/`

1 案の実画面を 1 ディレクトリで管理する。
参照 URL は `mock/pages/{画面名}/issues/{issue番号}/{案名}/` で終わる形になり、`index.html` がエントリポイントになる。
当該案を起点にユーザーが追加要望を出した場合は、配下に `{派生案名}/` を切ってネストする。派生はさらにネスト可能（`{派生案名}/{孫派生名}/`）。

| No  | 配置物              | 説明                                                                                                          | 補足 |
| --- | ------------------- | ------------------------------------------------------------------------------------------------------------- | ---- |
| 1   | `index.html`        | 当該案の実画面。共通アセットを `../../../../../assets/style.css` `../../../../../assets/app.js` で参照         | -    |
| 2   | `{派生案名}/`       | 派生案ディレクトリ（任意・複数可）。命名は親案からの変更点が分かる形にする（例: `with-bulk-edit`、`compact`） | -    |
| 2   | `style.css`（任意） | 案固有のスタイル                                                                                              | -    |
| 3   | `app.js`（任意）    | 案固有のスクリプト                                                                                            | -    |
| 4   | `images/`（任意）   | 案固有の画像                                                                                                  | -    |

## `docs/mock/components/`

共通コンポーネントのモックの束ね。索引ページとコンポーネントディレクトリだけを置く。

| No  | 配置物                | 説明                                                                                                  | 補足 |
| --- | --------------------- | ----------------------------------------------------------------------------------------------------- | ---- |
| 1   | `index.html`          | コンポーネント索引。各コンポーネントを並べ、リンクで当該コンポーネントのカタログへ遷移                  | -    |
| 2   | `{コンポーネント名}/` | 1 コンポーネント 1 ディレクトリ                                                                       | -    |

## `docs/mock/components/{コンポーネント名}/`

1 コンポーネントの全案をフラットに 1 ページにまとめたカタログ。
画面側と違って案ごとに別ページに分けない。
配置・状態（hover / disabled 等）・サイズ・色違いなど、単体で見比べたいものを縦に並べる。

| No  | 配置物              | 説明                                                                                                | 補足 |
| --- | ------------------- | --------------------------------------------------------------------------------------------------- | ---- |
| 1   | `index.html`        | 当該コンポーネントの全案カタログ。共通アセットを `../../../assets/style.css` `../../../assets/app.js` で参照 | -    |
| 2   | `style.css`（任意） | コンポーネント固有のスタイル                                                                        | -    |
| 3   | `app.js`（任意）    | コンポーネント固有のスクリプト                                                                      | -    |

## `docs/mock/styles/`

画面全体のデザインスタイル（雰囲気・配色・タイポ・余白の方向性）を管理するディレクトリ。
プロジェクト初期で 1 案を採用するために、候補スタイルを画面で見比べる場所。
Wiki に文章で書いても見ない / 伝わらないため、実画面で表現する。

| No  | 配置物            | 説明                                                                                              | 補足 |
| --- | ----------------- | ------------------------------------------------------------------------------------------------- | ---- |
| 1   | `index.html`      | スタイル一覧。各スタイルをカード形式で並べ、kebab-case 名前順。採用案には採用マークを付ける         | -    |
| 2   | `{スタイル名}/`   | 1 スタイル 1 ディレクトリ                                                                          | -    |

## `docs/mock/styles/{スタイル名}/`

1 スタイルのサンプル画面を 1 ディレクトリで管理する。
当該スタイルが適用された画面（ヘッダ + サイド + テーブル + フォーム + ボタン等の主要要素が並ぶ 1 枚）を見せる。
スタイル固有の CSS / JS を持つ場合は同ディレクトリ配下に置く。

| No  | 配置物              | 説明                                                                                              | 補足 |
| --- | ------------------- | ------------------------------------------------------------------------------------------------- | ---- |
| 1   | `index.html`        | 当該スタイルのサンプル画面                                                                        | -    |
| 2   | `style.css`（任意） | このスタイル独自の CSS。共通の `assets/style.css` を上書き / 補強する                              | -    |
| 3   | `app.js`（任意）    | このスタイル独自の JS                                                                              | -    |

## 採用スタイルと共通アセットの関係

採用が決まったスタイルは、共通アセット `docs/mock/assets/style.css` の内容と一致させる。
共通アセットを「採用スタイル本体」とし、`styles/{採用スタイル名}/` は「採用スタイルのサンプル展示」として残す。
未採用の候補スタイルは `styles/{スタイル名}/style.css` でそれぞれ独自の見た目を表現する。

## `docs/mock/issues/`

Issue 単位の横断ビュー。索引ページと Issue ディレクトリだけを置く。
`pages/` の縦割り（画面 → 改修 → 案）だけだと、1 Issue で複数画面を改修した場合に各画面を探し回ることになるため、Issue ベースのエントリポイントを別途持つ。

| No  | 配置物                | 説明                                                                              | 補足 |
| --- | --------------------- | --------------------------------------------------------------------------------- | ---- |
| 1   | `index.html`          | Issue 索引。新しい Issue が上にくるよう最新順で並べる。各 Issue の概要を併記        | -    |
| 2   | `{issue番号}/`        | 1 Issue 1 ディレクトリ。ディレクトリ名は GitHub Issue 番号そのまま（例: `50`）      | -    |

## `docs/mock/issues/{issue番号}/`

当該 Issue で扱った画面モックへのリンク集。
このディレクトリ自体には実画面を置かず、`pages/{画面名}/{issue番号}/` または末端の `{案名}/` へのリンクだけを並べる。

| No  | 配置物         | 説明                                                                                                                                            | 補足 |
| --- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| 1   | `index.html`   | 当該 Issue 概要 + 関連する画面モックへのリンク集。各リンクは `pages/{画面名}/{issue番号}/index.html`（案リスト）か末端の `{案名}/index.html` を指す | -    |

リンク方針:
- 改修ごとに 1 リンクで足りる場合は案リスト（`pages/{画面名}/{issue番号}/index.html`）を指す
- 採用案が決まっていて実画面を直接見せたい場合は末端の `pages/{画面名}/{issue番号}/{案名}/index.html` を指す
- 1 Issue で扱った全画面を漏らさず載せる

## 画面 UI 構成（全 `index.html` 共通）

モック画面（`pages/.../{案名}/index.html` を中心とした各 `index.html`）の **画面内の UI 要素** に関する規約。
ディレクトリ構成と直交するルールで、全モック画面に適用する。

### トップバー

各画面の最上部に置く帯。以下のルールに従う。

| No | 要素             | 扱い   | 補足                                                                                |
| -- | ---------------- | ------ | ----------------------------------------------------------------------------------- |
| 1  | サイト名 / ロゴ  | 任意   | 置く場合は左端。クリックで `docs/index.html`（または `docs/mock/index.html`）へ戻る |
| 2  | 同階層の案タブ   | **必須**（案複数時） | 同じ `pages/{画面名}/issues/{issue番号}/` 配下の `{案名}` をタブで横並び            |

#### 同階層の案タブ

- 現在開いている `{案名}` と兄弟の `{案名}` を **タブとして横並びに表示** する（例: `案A` `案B` `案C`）
- リンク先は各案の `index.html`（相対パス: `../{案名}/index.html`）
- 現在開いている案は active 状態で強調表示（背景色 / 下線など）
- 派生案（`{派生案名}/`）配下の画面では、親 `{案名}` の兄弟タブ + 自身の派生案兄弟タブの **2 段** で並べてよい
- タブが収まり切らない場合は **横スクロール対応**（`overflow-x: auto` + タッチ操作可）。折り返しはしない
- 案が 1 つだけのときはタブそのものを表示しない（タブは比較目的のため）

### パンクズリスト

- **トップバーではなく画面本体（ページコンテンツの最上部）に置く**
- 末端の `{案名}` まで開いている場合、`pages` から末端まで **全階層を遡れる** リンク列にする
  - 例: `pages / customer-list / issues / 50 / a-left-filter / with-bulk-edit`
- 各セグメントは該当ディレクトリの `index.html` を指すリンク
- 末端セグメント（現在地）はリンク化せず通常テキスト表示
- セパレーターは `/` または `›` で統一

### 採用マーク

- 案リスト・派生案・スタイル一覧などで採用案を識別するためのバッジ
- 共通スタイル `assets/style.css` に `.badge-ok` / `.badge-adopted` 等のクラスを定義し、各画面はクラスを当てるだけで使えるようにする
- 文言は短く（例: `採用` / `OK` / `Adopted`）

### 共通アセットの参照

- ネスト階層に応じて `../` を適切に積む。テンプレートに以下を明記:
  - `pages/{画面名}/issues/{issue番号}/{案名}/index.html` → `../../../../../assets/`
  - `pages/{画面名}/issues/{issue番号}/{案名}/{派生案名}/index.html` → `../../../../../../assets/`
- パスがずれると共通スタイルが当たらず崩れるため、新規画面追加時はテンプレートを必ずコピーする
