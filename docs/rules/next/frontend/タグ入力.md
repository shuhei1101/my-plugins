# `<TagInput>` — IME 対応タグ入力

- Enter でタグ確定、onBlur でも確定（モバイル対応）
- IME 入力中の Enter は無視（`composing` フラグ）
- 重複・空文字（trim後）は追加しない
- Props: `value: string[]`, `onChange`, `placeholder?`
- FormField では `value={field.value} onChange={field.onChange}`
