# shadcn `<Form>` パターン

react-hook-form + Zod + shadcn/ui の標準組み合わせ。

## 構造

`<Form {...form}>` → `<form onSubmit={form.handleSubmit(onSubmit)}>` → `<FormField>` → `<FormItem>` → `<FormLabel>` + `<FormControl>` + `<FormMessage>`

- `register` 直接使用禁止（`<FormField render={({ field }) => ...}>` を使う）
- `<FormMessage />` がエラーメッセージを自動描画

## 入力タイプ別ポイント

| 型 | ポイント |
|---|---|
| Text / Textarea | `{...field}` でそのまま渡す |
| Number | `onChange` で `Number()` 変換、空文字→ `null` |
| Select | `value` を `toString()`、`onValueChange` で `Number()` 変換 |
| Switch | `checked={field.value} onCheckedChange={field.onChange}` |
| Tag | `<TagInput value={field.value} onChange={field.onChange}>` |
| Date | `<Calendar>` + `<Popover>` 組み合わせ |

## ルール

- 必須フィールドには `<FormLabel>` 内に `<RequiredMark />`
- `isDirty` で保存ボタン制御
- 送信成功後は `form.reset(data)`
