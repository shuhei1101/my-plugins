# `<AutosaveIndicator>` — 自動保存状態表示

- Props: `status`, `lastSavedAt: Date | null`, `onRetry: () => void`
- 4 状態: `idle`（lastSavedAt があれば「N 分前に保存」）/ `saving` / `saved` / `error`（retry ボタン）
- `saved` → `idle` への遷移は hook 側（数秒後）
