# 状態管理

- リアクティブ更新を必須にしない。
  - 実装が複雑になるなら素直な手続き的更新（イベントごとに再取得・再描画）でよい。
  - 判断基準はメンテナンス性。

- 画面切替の状態（タブ・選択 ID・フィルタ等）は URL クエリに反映するのを推奨する。
  - URL を貼れば画面状態を共有・再現できるため。
  - 必須ではない。

- 凝った仕組みが要る時だけ薄い Proxy + EventTarget を使う。
  - 深いネスト・Map/Set・private field を Proxy で包まない。
  - 仕組みが膨らんだら設計を見直す合図。
