# Markdown 編集規約

## フロントマターの配置

> ファイルに YAML フロントマター（`---` ブロック）がない場合は、このリファレンスを無視してください。

YAML フロントマターを持つ Markdown ファイルでは、**開き `---` より前に何も置いてはならない** —
HTML コメントも、空行も、何も。多くのレンダラーは `---` がファイルの先頭行にある場合のみ
YAML ブロックとして認識し、それ以外はフロントマターを本文として描画してしまう。

## `---`について
マークダウン内で指定がない場合、`---`による区切り線は使わないこと。

## `**`について
マークダウンで指定がない場合、`**`による太字や`*`による斜め文字は使わないこと（見づらいし、編集しづらいから）
`>`これもあんまり使わないでください。

## 一行の長さについて
- 本文の段落・箇条書きは句点（。）ごとに改行する（1 文 = 1 行。レンダリングでは 1 段落に結合される）
  - エディタで横スクロールが発生する長い 1 行を作らない
  - 文を書き足すときも既存行への連結ではなく新しい行に足す
- 表のセル・見出しは対象外（セル内の改行は `<br>`）

## テーブルの No / # カラムについて
- 連番だけの No / # カラムは付けない
  - 行の挿入・削除・並び替えのたびに全行の番号がズレて修正コストになるため
- 番号自体に意味がある場合（不変 ID・Issue / PR 番号など、番号で参照する運用があるもの）のみ付けてよい

## テーブルの同上マーク（〃）について
- 同列で値が連続しても `〃` で省略せず、セルごとに実値を書く
  - 表の並び替え機能で行順が変わると `〃` の参照先が壊れるため
  - AI が行を編集・挿入したときに `〃` の参照先まで追従できず、値がずれるため
