<!-- This file is a Japanese mirror. When updating the English original, update this file too. -->
# Incidents（インシデントログ）

| Date | Summary | Detail |
|---|---|---|
| 2026-05-21 | master で直接ファイルを編集すると worktree マージ時に競合が発生する。必ず work-start → worktree 内で作業する | [detail](../../references/incidents/master-direct-edit-causes-merge-conflict.md) |
| 2026-05-23 | スキルが Step 0 で他のスキルを読み込む設計にすると起動ごとに 2500×N トークンを消費してコンテキストを圧迫する。判定知識はスキル本体の References に内包させること | [detail](../../references/incidents/skill-reading-token-cost.md) |
| 2026-05-23 | 複数のスキルが同じ判断基準を inline で二重管理していた。creator スキル群の共通知識は `references/` に集約し、各スキルから参照する設計にすること | [detail](../../references/incidents/creator-skill-inline-duplication.md) |
| 2026-05-24 | merge スキルがユーザーの明示的指示なしに `git merge` を自動実行した。セッション内で過去に許可を得ていても次のマージは別指示が必要。修正: SKILL.md に Critical Prohibition セクション追加・Step 6 に絶対禁止ルール追記 | [detail](../../references/incidents/merge-auto-execution-without-permission.md) |
| 2026-05-24 | `.work/specs/` フォルダ名が「仕様書」を連想させるため、AI に自動読み込みされないにもかかわらず重要ドキュメント扱いになり古くなりやすかった。AI 非読み込みのフォルダは `notes/` など非公式な名前にすること | [detail](../../references/incidents/work-folder-name-implies-official-docs.md) |
| 2026-05-24 | merge スキルが master cwd で conversation-to-claude を呼んでいたため、生成された glossary/incidents が master 直接コミットになり PR ブランチに同梱されなかった。修正: ワークツリーに cd してから呼び出す。教訓: git commit を含むスキルへの委譲は呼び出し側で cwd を明示的に制御すること | [detail](../../references/incidents/conversation-to-claude-master-direct-commit.md) |
| 2026-05-24 | `.jp.md` ファイルの JP 警告コメント有無を `head -5` で検証したため、frontmatter 持ちファイル（警告が 7 行目以降にある）を「警告なし」と誤検出し対象件数を 39 件と過大算定した。実際は 25 件。ファイル先頭メタデータの検証は frontmatter を skip した上で本体先頭を見るべき | [detail](../../references/incidents/header-check-misses-frontmatter-files.md) |
| 2026-05-24 | hook-creator で `hooks/prompts/*.md` を作成した際に `*.jp.md` の JP ミラーを作成しなかった。`skill-jp-mirror-sync.md` ルールは SKILL.md のみ対象のため hook prompts はカバーされていなかった。修正: `hook-prompts-jp-mirror-sync.md` ルールを追加 | [detail](../../references/incidents/hook-prompts-jp-mirror-missing.md) |
| 2026-05-24 | スキルのステップを独立スキルに切り出した際、master に同じステップを変更する PR が既に存在し、実装後に2回の master 取り込みが必要になった。対象ステップの切り出し前に `git log --oneline master -- {ファイル}` で直近の変更を確認すること | [detail](../../references/incidents/extract-step-check-master-first.md) |
| 2026-05-24 | `CLAUDE.md` を編集した際に `CLAUDE.jp.md` の JP ミラー更新を忘れた。`skill-jp-mirror-sync.md` も `hook-prompts-jp-mirror-sync.md` も CLAUDE.md はカバーしていなかった。修正: `claude-md-jp-mirror-sync.md` ルールを追加 | [detail](../../references/incidents/claude-md-jp-mirror-missing.md) |
| 2026-05-24 | `incidents.md` / `glossary.md` を編集しても `rules-jp/` の JP ミラーが更新されず、英語版との乖離が長期間放置された。修正: `incidents-glossary-jp-mirror-sync.md` ルールを追加して同コミット更新を強制する | [detail](../../references/incidents/incidents-glossary-jp-mirror-missing.md) |
| 2026-05-24 | 複数の Stop フックが同時発火すると、`"Read and follow: /path"` 方式の指示が `stop_hook_active` ガードで無効化される。Claude は直接指示（notify-aituber 等）を先に実行して再 Stop し、2回目の発火では stop_hook_active=true のため全フックがスキップ → 間接参照の指示が永遠に実行されない。修正: reason/stdout にファイル内容を直接埋め込む方式に戻す | [detail](../../references/incidents/stop-hook-multiple-block-race-condition.md) |
| 2026-05-24 | PR115 でフックパターンを一括変更した際、work-kit/dev-kit/ui-kit は修正したが claude-kit/hooks.json の UserPromptSubmit × 5・PostToolUse × 1 が漏れた。教訓: フックパターンの横断変更は `grep -r "対象パターン" --include="*.json" .` で全プラグインを確認してから修正すること | [detail](../../references/incidents/hook-pattern-change-missed-plugin.md) |
| 2026-05-25 | statusLine の `python -c "..."` 内で `r5["resets_at"]` のダブルクォートが外側を切り、rate_limits.resets_at が入った瞬間に NameError でステータスラインが落ちる。修正: `r5.get('resets_at')` でシングルクォート完結に変更 | [detail](../../references/incidents/statusline-python-quote-nesting.md) |
| 2026-05-25 | `apply-statusline.py` を WSL Python で実行したが、Claude Code は Windows ネイティブで動いていたため別の settings.json を書き換え、変更が反映されなかった（エラーも出ない）。教訓: `Path.home()` を使うスクリプトは Claude Code と同じ Python 環境で実行する | [detail](../../references/incidents/path-home-cross-env-mismatch.md) |
| 2026-05-24 | `skill-creator-dispatch` フックが `UserPromptSubmit` のみだったため、曖昧なプロンプト時に Claude が `SKILL.md` を直接編集した。修正: `PreToolUse` ブロック型フックを claude-kit/dev-kit/ui-kit/work-kit の4プラグインに追加。教訓: ファイル種別ガードは `UserPromptSubmit` ではなく `PreToolUse` で実装すること | [detail](../../references/incidents/skill-creator-dispatch-userpromptsubmit-only.md) |
| 2026-05-25 | UserPromptSubmit フックが「work-start を先に実行すること」と明示しているにもかかわらず、Claude が直接 master 上でファイルを編集し始めた。フックの指示は実装の前提条件であり、「少し変えるだけ」でもスキップしてはならない | [detail](../../references/incidents/work-start-skipped-despite-hook-instruction.md) |
| 2026-05-25 | `.claude/rules/core/` の `incidents.md` と `glossary.md`（Claude が常時読み込む英語本体）のコンテンツが全て日本語で書かれており JP ミラーと実質同内容だった。修正: 全コンテンツを英語化（PR123）。英語本体ルールファイルには英語のみ記述すること | [detail](../../references/incidents/core-rules-jp-content-in-en-file.md) |
| 2026-05-25 | PR121 で `skill-creator-dispatch` PreToolUse フックを4プラグイン全てに追加したが、claude-kit がグローバルに SKILL.md をガードしているため dev-kit/ui-kit/work-kit への追加は重複だった。PR124 で削除。教訓: グローバルなガードフックは claude-kit にのみ追加し、他プラグインにはそのプラグイン固有のフックのみ追加する | [detail](../../references/incidents/duplicate-hooks-in-non-claude-kit-plugins.md) |
| 2026-05-27 | YAML frontmatter を持つエージェント JP ミラーファイルを作成した際、`<!-- Generated by -->` スタンプが省略され、JP ミラー警告コメントが frontmatter の後ではなく前に配置された。修正: `mark-generated` を呼び出し、コメントを closing `---` の直後に挿入する。frontmatter より前に置くと GitHub で `---` が水平線としてレンダリングされる | [detail](../../references/incidents/agent-jp-mirror-stamp-before-frontmatter.md) |
| 2026-05-27 | 方針確定のみの PR（PR138、実装は別 PR へ分離）で、AI が設計 QA にフックの Python pseudocode を詳細に書きすぎた。ユーザーから「実装詳細は書かなくていい」と強めに指摘。教訓: PR が「方針 / 設計のみ」にスコープされている場合、QA は *何を / なぜ / どこに* までで止め、*どう実装するか* には踏み込まない | [detail](../../references/incidents/design-qa-implementation-creep.md) |
| 2026-05-28 | PR140 で AI が先回りで `refs-inject-kit` プラグインを切り出し、injection rules を集約する設計（`${plugin}/path` プレースホルダ記法）を実装。ユーザーから「ややこしすぎる」と却下され、プラグイン全体（5 コミット、~700 行）を撤回。教訓: 3 つ以上の consumer か drift 事故の再発が確認できるまでプラグイン横断の集約は作らない。2 プラグイン間のコピペは、プレースホルダ/パス解決層より安い | [detail](../../references/incidents/premature-cross-plugin-centralization.md) |
| 2026-05-28 | PR140 で AI が `references/index.yaml` を `index.md`（Markdown テーブル）に変換し、フックに正規表現 Markdown パーサーを書いた。ユーザーから「ファイルがコード消費されるなら YAML が正解」と指摘され撤回。教訓: コードが parse する config は構造化フォーマット（YAML/JSON）で保持する。人間用 Markdown ビューが欲しければ YAML から生成する | [detail](../../references/incidents/markdown-for-code-consumed-config.md) |
| 2026-05-28 | PR140 で AI が 5 件の reference ファイル（python-script / tkinter / health / cheatsheet / refactoring-judgement）を作成したが、どれも `injection_rules.yaml` のパターンに紐付けていなかった。ユーザーから検証を求められ、orphan 検出スクリプトでギャップが判明。教訓: `injection_rules.yaml` を編集したら、YAML vs filesystem の orphan 検出スクリプトを毎回実行する | [detail](../../references/incidents/orphan-references-not-checked.md) |
| 2026-05-28 | PR140 で AI が injection_rules パターンに `tools/**/*.py` と書いたが、プロジェクトルート直下の `tools/` しかマッチせず、モノレポの `packages/foo/tools/bar.py` にマッチしない。ユーザーが指摘し `**/tools/**/*.py` に修正。教訓: 「フォルダ名」型の glob パターンはデフォルトで `**/` を前置する。ルート固定パターンはプロジェクトルートに必ずあるファイル専用 | [detail](../../references/incidents/glob-pattern-missing-recursive-prefix.md) |
| 2026-05-28 | PR135 で AI が next-kit references を「ベストプラ網羅」型で 46 ファイルに仕上げた（1 ファイルに複数ファイル種別を詰め、選定/比較/トレードオフセクションあり）。ユーザーから却下: フック自動 inject 前提なら 1 ファイル = 1 ユースケースに分割しないと、`query.ts` 編集で他 5 種類のファイル種別の情報まで読み込まれる。90 ファイルに再分割、選定/比較/トレードオフは完全削除。教訓: フック自動 inject 向け references を設計するときは `injection_rules.yaml` のパターン（トリガーマップ）から始める。内容の目次から始めない | [detail](../../references/incidents/bestprac-over-usecase-references-bloat.md) |
| 2026-05-28 | PR135 で AI が `.claude/rules/feature/_overview.md` にエントリ追記したが、master 側では PR141 のクリーンアップで既に削除されていた。長期ブランチで気付かずゴーストファイルを編集し続け、merge 前に整合のため別途 `git rm` コミットが必要に。教訓: 長期ブランチで rule / overview / index 系ファイルを編集する前に `git show master:{file}` で存在確認。`_overview.md` 型 index ファイルは可能なら維持しない | [detail](../../references/incidents/master-deletion-overlooked-on-long-branch.md) |
| 2026-05-28 | PR131 でサブスキル（`issue-log`）を `--title --type --priority ...` のような CLI 風引数フラグで設計してしまった。Claude Code のスキルは引数を取らない — コンテキストにロードされる Markdown ファイルにすぎない。修正: 「概要」セクションを CLI フラグ表ではなく、期待される入力の自然言語箇条書きに書き直した | [detail](../../references/incidents/skill-cli-args-format.md) |
| 2026-05-28 | PR131 で `templates/.work/issues/_index.yaml` を初期テンプレートとして置いたが、同ディレクトリの `.gitignore` に `_index.yaml` が含まれていたため、プラグイン repo 内でテンプレートファイルが gitignored され commit できなかった。修正: テンプレートファイルを削除し、`setup.py` で `_expand()` 後に `_index.yaml` を書く方式に変更。教訓: `.gitignore` のあるディレクトリにファイルを置く前に `git check-ignore -v` で trackable か確認する | [detail](../../references/incidents/template-under-gitignore.md) |
| 2026-05-28 | PR131 で 1 件のイシューを永続化するサブスキルを `issue-log` と命名したが、ユーザーから「log はエラーログ / デバッグログを連想させる」と指摘。永続レコード保存には不適切な含意。`issue-save` にリネーム。教訓: データを *永続化* するスキル / 関数は `save` / `write` / `record` を使う。`log` はロギングストリームへの本物の出力にのみ使う | [detail](../../references/incidents/skill-name-log-implies-error-log.md) |
| 2026-05-28 | py-kit/next-kit の references 自動注入フックが、マッチした reference の**本文全体**を `decision: block` の reason に注入していたため、マッチするファイル操作のたびにコンテキストを圧迫していた（Read でも発火するため悪化）。ユーザー指摘時、AI は当初 Read マッチャー削除を提案したが、問題はトリガーではなく注入内容の量だった。修正（PR147）: **path + description**（ポインタ）を **絶対パス**で注入し本文は注入しない。さらに `${CLAUDE_PLUGIN_ROOT}` は注入される reason テキスト内では展開されない（hooks.json 内のみ）ため、フックスクリプト自身が絶対パスを生成する必要がある。Read マッチャーは issue-scan のため維持。 | [detail](../../references/incidents/injection-hook-full-body-bloat.md) |
| 2026-05-28 | PR153 で 2 つの dispatch プロンプト（`hook-creator-dispatch` / `plugin-creator-dispatch`）が存在するのに未配線（オーファン）だった — 旧 UserPromptSubmit→PreToolUse 移行の名残。AI は当初削除を推奨（plugin-creator が `plugins/**` 全体にマッチし広すぎると判断）。ユーザーが訂正: これらは未移行の意図された機能であり、現行の PreToolUse(Edit/Write) 方式に作り変えるべき。広さの懸念はルール順序（広いキャッチオールを最後に）で解決。教訓: スタイル移行で取り残されたオーファン生成物は削除ではなくモダン化する。削除は確認を取ってから。 | [detail](../../references/incidents/orphaned-dispatch-prompts-modernize-not-delete.md) |
| 2026-05-28 | 並行する 2 つの PR（PR152 と PR153）が独立して claude-kit を v3.28.0 にバンプした。PR153 が先にマージされたため、PR152 のバンプが master と衝突。修正: Step 3（master 適合確認）で検出し、マージ前に v3.29.0 へ繰り上げ。教訓: `git diff HEAD..master` で同じプラグインのバージョンバンプが見えたら、即座に PR ブランチ側を次のバージョンに繰り上げてからマージする。 | [detail](../../references/incidents/parallel-pr-version-bump-collision.md) |
| 2026-05-28 | `session-kit`（PR150/151 で構築）は注入トークンを毎ターン削除し、reference を会話ターンごとに再注入させていた。しかし PR147 で注入は既にポインタのみ（path+description、本文なし）になっており、ターンごと再注入の価値は失われていた — それでも session-kit は PR155 で削除されるまで残った。教訓: ある変更（PR147）が既存インフラの前提を崩したら、数 PR 後ではなくその時点でインフラを見直す。限界的な最適化のためにプラグイン横断プラグインを丸ごと残さない。 | [detail](../../references/incidents/session-kit-removed-after-premise-change.md) |
| 2026-05-29 | PR156（`ref-inject` 構築）で、AI が決定論的な `scripts/generate.py` と `PreCompact` リフレッシュフックを追加したが、いずれもユーザーが削除。ユーザーは Claude 主導のコピー+置換（コンテキストに残る）を望み、TTL トークンが `/compact` 後も再注入するため。教訓: プラグインごとのファイル生成はスクリプトより Claude 主導を優先。TTL/既存の仕組みが既に賄う挙動のためにフックを足さない — 最小の仕組みをデフォルトに。 | [detail](../../references/incidents/ref-inject-overbuilt-script-and-hook.md) |
| 2026-05-29 | PR156 で、creator-dispatch の `PreToolUse` ブロック（セッションフラグ型: 最初の編集をブロック、以降通す）が SKILL.md / rule / .py / .yaml / CLAUDE.md の書き込みで発火。AI は creator スキルを呼ばず生の書き込みを再試行して手書きし、ユーザーが「/skill-creator つかえ」「/py-script これ使って」と指示する羽目に。教訓: dispatch ブロックは編集の再試行ではなく、指定された creator スキル（skill-creator / rule-creator / py-script / claude-creator）を呼ぶ合図。 | [detail](../../references/incidents/creator-dispatch-block-means-invoke-creator.md) |
| 2026-05-29 | PR157（py-kit を `ref-inject` へ移行）で、注入フックの再生成が creator-dispatch の catchall ブロックを踏んだ（`.py` で plugin-creator、`.j2` で j2-stamp-check、CLAUDE.md/glossary で claude/rule-creator）。これらのファイルの所有者は `/ref-inject:apply` であり plugin-creator ではないため、汎用の `plugins/` catchall は**誤検知** — セッションフラグのブロックを通して ref-inject:apply で進め、plugin-creator へ寄り道しない。`creator-dispatch-block-means-invoke-creator` の補足: 「creator を呼べ」は具体的な dispatch ルールに当てはまり、専用機構スキルがファイルを所有する場合の catchall には当てはまらない。 | [detail](../../references/incidents/ref-inject-reapply-dispatch-false-positive.md) |
| 2026-05-29 | PR158（PR157 の py-kit→ref-inject 移行を next-kit でミラー）で、手本となるファイル（移行済み py-kit・expires_at 更新済み ref-inject テンプレ）が無かった。pr-handoff が予約したワークツリーが PR157 マージ *前* の master から分岐していたため（`git merge-base --is-ancestor` が false）。最初の diff で py-kit が旧トークン設計のままと判明。修正: 先に `git merge master` してからミラー。教訓: ある PR が先行 PR をミラーするなら、ブランチ内ファイルを手本に使う前に先行 PR がブランチ履歴にあるか確認する — pr-handoff で予約した枝が先行 PR の成果を含むとは限らない。 | [detail](../../references/incidents/worktree-reserved-before-predecessor-merge.md) |
| 2026-05-29 | PR159 で、claude-kit の `references/index.yaml` の description に `…paths: design…` と書いてしまい、引用なし YAML スカラー内の `: `（コロン+スペース）がネストしたマッピングと解釈され `yaml.safe_load` が壊れた（注入フックが依存）。修正: コロン+スペースを避けて言い換える（または値全体を引用）。教訓: `index.yaml` / `injection_rules.yaml` の description を書くとき、引用なしスカラーに `単語: ` を入れない。編集のたびに `yaml.safe_load` で検証する（orphan チェックも実行）。 | [detail](../../references/incidents/yaml-unquoted-colon-space-breaks-parse.md) |
| 2026-05-29 | PR161 で、AI 呼び出しスキル（`mark-generated`）を完全削除し、代わりに ref-inject フックがその reference を注入する計画だった。しかしフックは Claude の **Write/Edit/Read ツール呼び出し**でしか発火しない — ヘルパースクリプト（work-kit の `setup-task.py`）生成物や `injection_rules` 外の型（html-kit の `.js`/`.css`）はカバーしない。完全削除ならそれらのスタンプが黙って失われていた。修正: スキルを**薄ラッパー**（フォールバック）として残し、仕様だけ注入 reference へ移す。教訓: AI 呼び出しスキルを「注入 reference だけ」に変える前に、全呼び出し元の対象が Write/Edit 経路にあるか確認する。スクリプト生成・非マッチ型はスキルが必要。 | [detail](../../references/incidents/injection-only-fires-on-write-edit-path.md) |
| 2026-05-29 | PR160 で、ユーザーが「claude-kit もやって」と言った際、私はそれを未着手の次PR候補として扱った — しかしその移行は既に PR159 として master にマージ済みで、master はセッション開始時スナップショットから 2 PR 先（PR159+PR161）まで進んでいた。調査中に気づいた。PR160 ワークツリーが PR159 前のコミットから分岐していたため `git merge master` も必要だった。教訓: 後続 / PR横断の作業を提案・着手する前に実際の現 master（`git log master`、`git worktree list`）を確認する — セッション開始時の git スナップショットは古く、記録済みの次PR候補が並行セッションで既にマージ済みのことがある。 | [detail](../../references/incidents/stale-session-git-snapshot-already-merged-followup.md) |
