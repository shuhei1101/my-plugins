<!-- Generated by claude-kit/rule-creator v3.23.0 -->
# Glossary

## Plugin System

| Term | Description |
|---|---|
| incidents | A fixed rule file (`.claude/rules/core/incidents.md`) that accumulates incident prevention logs for this project. Two-layer structure: index (always loaded) and detail files (`.claude/references/incidents/`). |
| glossary | A fixed rule file (`.claude/rules/core/glossary.md`) that manages project-specific terminology. Kept concise because it is always loaded. |
| conversation-to-claude | A skill (`/claude-kit:conversation-to-claude`) that analyzes the current session's conversation history and persists knowledge as artifacts such as skills, rules, hooks, CLAUDE.md, incidents, and glossary entries. |
| claude-refactor | A skill (`/claude-kit:claude-refactor`) that audits and reorganizes rules, skills, CLAUDE.md, and hooks under `.claude/`. Detects duplication, proposes file-type migrations and folder restructuring, then executes after confirmation. |
| 判定知識 | A block embedded in a skill's own References section — without loading external files — that contains the criteria and rules the skill needs for proposals and decisions. Introduced for token efficiency (PR68). |
| リンクルール | One of two rule types (file-linkage type). Bundles related files under `paths:` to enforce "if you edit one, check the others" coordination. |
| コンテキストルール | One of two rule types (load-trigger type). Auto-loads required knowledge/specs when a specific work area is touched. |
| ユースケース指向 | Design principle for `paths:`. Start from "when do I want this rule to be read?" and work backwards to identify the trigger files to list under `paths:`. |
| notes/ | The `.work/notes/` folder managed by work-kit. A place for temporary design notes and discussion memos related to a PR. Treated as informal working notes rather than official specs because AI does not auto-load it. Formerly `specs/` (renamed in PR88). |
| notes-to-claude | A work-kit skill (`/work-kit:notes-to-claude`) that promotes temporary memos in `.work/notes/` to rules, CLAUDE.md, or references. Runs fully automatically without a confirmation phase. Uses creator skills from the claude-kit plugin internally (added in PR90). |
| 1行参照パターン | A pattern where only the single line `"Read and follow: /path/to/file.md"` is output in a hook's `reason`/stdout instead of embedding the full file content. Claude reads the file itself, preventing long-text injection into the conversation session. Introduced for the Stop hook (PR89); unified across all hooks in hook-creator v3.20.0 (PR104). |
| pr-handoff | A skill (`/work-kit:pr-handoff`) that reserves the next PR using the same flow as work-start, based on the "Next PR candidates" recorded in the current PR's TODO.md. Records background context in TODO.md to improve handoff quality (added in PR91). Serial-dependency support introduced in PR95. |
| 実施条件 | A concept with two uses. ① The 3rd column of the `## 次PR候補` table in TODO.md (dependency for each candidate: `即時実施可` / `「{other candidate}」が完了したら`) (PR95). ② The `### 実施条件` sub-section under `## 概要` in TODO.md — records the prerequisite conditions for starting this PR itself (PR101/102). |
| creator-dispatch フック | A former `PreToolUse(Edit/Write)` [[セッションフラグ型ブロック]] hook (`plugins/claude-kit/hooks/creator_dispatch.py`) that matched the edited file path and told Claude to run the corresponding creator skill (skill / rule / claude / hook / plugin) before editing. History: UserPromptSubmit keyword detection (PR103) → PreToolUse file-path matching → config-driven `RULES` script (PR153). **Removed in PR159**: the creator skills were migrated to [[claude-kit ref-inject 移行]] (reference auto-injection), so the creator-dispatch rules were dropped. Only the non-creator `j2-stamp-check` rule survived, extracted into `plugins/claude-kit/hooks/j2_stamp_check.py`. The PostToolUse JP-mirror check (`jp_mirror_check.py`) is unaffected. |
| claude-kit ref-inject 移行 | PR159. claude-kit's creator skills (skill / rule / hook / claude / plugin-creator) were converted from step-format procedures into **self-contained authoring guides under `references/`**, auto-injected by the ref-inject hook (`inject_references.py` + `injection_rules.yaml`) when the matching file is edited (SKILL.md → skills.md, rule → rules.md, CLAUDE.md → claude-md.md, hooks.json → hooks.md, plugin.json → plugin-structure.md; glossary/incidents files → their format guides). The creator skills remain as **thin wrappers** (kept for explicit invocation and for callers like conversation-to-claude / notes-to-claude). The old [[creator-dispatch フック]] was removed (only `j2-stamp-check` kept). claude-kit thereby joins py-kit / next-kit under [[kit-hooks-index-sync]]. Provenance stamping moved into `references/common.md`. |
| 関連PR | The `### 関連PR` sub-section under `## 概要` in TODO.md. Records PRs directly related to this one (predecessor, split-sibling, follow-up, etc.) in a table of PR number and summary (added in PR102). |
| 直列依存 | A dependency where a follow-up PR can only be implemented after a preceding PR is merged. pr-handoff does not reserve serially-dependent candidates at once; instead it transcribes them into the preceding PR's `## 次PR候補` to carry the chain forward (introduced in PR95). |
| 即時予約対象 | A classification in pr-handoff Step 1. Candidates whose prerequisites are empty/即時実施可, or depend only on already-merged PRs. These are actually reserved by `/work-kit:work-start` in the current pr-handoff run (introduced in PR95). |
| 依存後続候補 | A classification in pr-handoff Step 1. Candidates that depend on another candidate in the same table. Not reserved immediately; instead transcribed into the new TODO.md of the dependency target (即時予約対象) under `## 次PR候補` (introduced in PR95). |
| master 適合確認 | Step 3 of the merge skill. When master has progressed since the branch diverged, examines direct and indirect related changes and adapts autonomously according to tiebreaker priority order (added in PR92). |
| 間接依存 | The detection target in merge Step 3. Occurs when the master has changed code that calls or imports something this PR modified. Cannot be detected by direct file-name matching with `git diff --name-only` — requires contextual judgment. |
| タイブレーク順位 | Priority order when judgment is tied in merge Step 3: recency → blast radius → PR purpose → safer side (take master) (defined in PR92). |
| 出自メタデータ | An HTML/Python comment embedded at the top of files created by generator skills, in the format `<!-- Generated by {plugin}/{skill} v{version} -->`. Used as an identifier to detect outdated generated artifacts when a plugin version is bumped (introduced in PR94). |
| mark-generated | A claude-kit skill (`/claude-kit:mark-generated`) called by each generator skill (creator family, work-start, pr-handoff, debug-fab, etc.) to return the provenance metadata comment string for the target file type. Also returns a warning comment for JP mirror files (added in PR94). |
| version-sync | A claude-kit skill (`/claude-kit:version-sync {plugin}`) that detects files generated by an older plugin version using the metadata stamped by `mark-generated`, and reflects the diff from `changelogs/`. Runs in fully-automatic mode (added in PR94). |
| 着手可能 (🟢) | One of the PR statuses presented by pr-show. A reserved branch where `git log master..{branch} --oneline` shows 1 or fewer commits — just reserved by pr-handoff with no implementation started yet (introduced in PR99). |
| 進行中 (🟡) | One of the PR statuses presented by pr-show. A reserved branch with 2 or more commits from master — considered to be actively worked on in another Claude Code session; best not to touch in the current session (introduced in PR99). |
| 条件あり (🔴) | One of the PR statuses presented by pr-show. A [[依存後続候補]] in TODO.md's `## 次PR候補` whose prerequisites depend on another candidate. Not yet reserved by pr-handoff; becomes a reservation target only after the preceding PR is merged (introduced in PR99). |
| pr-show | A skill (`/work-kit:pr-show`) that lists the status of reserved PRs in 3 categories (ready / in-progress in another session / condition pending). Extracted as a standalone skill from merge Step 12 (added in PR109). Can also be invoked directly outside of merge. |
| disable-model-invocation | A frontmatter flag for Claude Code skills. When set to `true`, prevents the model from autonomously invoking the skill — it only fires when the user explicitly calls `/plugin:skill`. Skills with this flag do not need "no autonomous invocation" prohibitions (redundant). Only a same-recent-message call check is needed (consolidated in PR113). |
| セッションフラグ型ブロック | A design pattern where a PreToolUse hook uses a per-session flag file (`/tmp/{hook-name}-{session_id}`) to return `decision:block` only on the first call, then passes through subsequent edits in the same session once the user has gone through the creator skill. Originated in dev-kit's `python-skill-dispatch` / `yaml-skill-dispatch` (PR104); applied to all plugins' `skill-creator-dispatch` in PR121. |
| py-kit | A plugin (split from dev-kit in PR129) for Python-specific coding conventions and scaffolding. Originally adopted pure DDD as the standard layout (PR129). **In PR138 the standard was overhauled** to a "feature-folder layout + TypeScript-style + function-first" approach (design only), and **PR140 implemented v2.0.0**: references restructured into 38 files under topic-axis folders (`core/`, `architecture/`, `shared/`, `scripts/`, `testing/`, `concurrency/`, `packaging/`, `performance/`, `llm/`, `fastapi/`), with `references/index.yaml` carrying metadata and `injection_rules` (star chart) for per-edit-path reference auto-injection (the injection hook itself is reserved for follow-up PR `add-py-kit-references-injection-hook`). DB usage is out of scope (Web is delegated to next-kit). |
| 機能フォルダ型レイアウト | py-kit (PR138 onward) の標準レイアウト。`src/{pkg}/{shared,features,integrations,runtime,server}/` の構成で、機能（feature）単位でフォルダを切る方式。純DDD（domain/application/infrastructure/interface）の対義。`features/` 配下は自由に追加可能、`shared/` と `main.py` のみ必須、他は任意。AITuber 構造と Next.js (next-kit) の機能フォルダ思想を統合した結果。 |
| TypeScript 風 Python | py-kit (PR138 onward) の標準スタイル。Python 3.12+ の PEP 695（`type` 文・PEP 695 ジェネリクス）、`Callable[...]` 型エイリアス、`Protocol`（構造的型付け）、`@overload`、`Literal` + `match` + `assert_never`、Pydantic v2 / @dataclass / TypedDict を組み合わせて TypeScript の主要機能（type 文・interface・関数型 alias・ジェネリクス・discriminated union・関数オーバーロード）を Python で再現する。 |
| 関数ファースト | py-kit (PR138 onward) の標準スタイル。クラス（メンバー + 振る舞いを束ねたもの）ではなく **DTO（@dataclass / BaseModel）+ モジュールレベルの関数** で実装する設計方針。クラスを使うのは ① DTO（不変データコンテナ）、② ライブラリが強要する場合（FastAPI Middleware、Pydantic BaseModel 継承、CLI Command クラス等）、③ 長期保持のランタイム状態（接続プール等）に限定。 |
| ハンドラーデコレータ | py-kit (PR138 で採用方針確定) の例外ハンドリングパターン。例外キャッチや横断関心事を `@catch_and_log(level="warning")`, `@catch_and_map(SomeError, to=DomainError)`, `@with_retry(times=3)`, `@with_timeout(60)` 等のデコレータで束ねる。クラスベース AOP の代わりに関数デコレータで横断関心事を吸収する軽量パターン。 |
| 結合テスト + スモークテスト方針 | py-kit (PR138 で採用方針確定) のテスト方針。**単体テストは書かない**（AI 駆動開発前提、ソースコードが一次仕様）。代わりに ① **結合テスト**: ユースケースごとに、ルート → サービス → 条件分岐 → 結果まで通す。外部依存（LLM・TTS 等）は Mock 化。② **スモークテスト**: 外部サービスへの実接続確認。お金がかかるためユーザー手動実行のみ。AI 自動実行は禁止、`--run-smoke` 等の明示フラグでガード。 |
| 注入星取り表 (injection rules) | py-kit (PR138 で設計確定) の `references/index.yaml` に書く、編集対象ファイルパスのパターン → 注入する reference の組（`required` / `optional`）のマッピング表。次 PR の自動注入フック（PreToolUse）が編集対象ファイルに対応する reference を `decision: block` で注入する際の判断材料となる。 |
| QA.old.md 退避 | PR の途中で大方針転換した時、旧 QA.md を `QA.old.md` にリネームして履歴として残し、新方針で QA.md を一から書き直す運用パターン（PR138 で確立）。`QA.md` 冒頭で「旧 QA は QA.old.md を参照」と明示し、旧 QA の C セクションで「廃止 / 継続 / 修正」の対応表を整理する。 |
| html-kit | Rename of `ui-kit`. Handles HTML/CSS/JS implementation conventions, logging setup, and mock generation. Name change clarifies language scope (PR128). |
| next-kit | Plugin for Next.js App Router implementation conventions. Originally created in PR132. **Overhauled in PR135 (v3.1.0)**: 90 references split per file-type (`route-ts.md`, `query-ts.md`, `*-screen-tsx.md`, etc.), `[id]/page.tsx` now is the View screen itself (the old `view/` subroute is gone), `references/index.yaml` + `injection_rules.yaml` + `hooks/inject_references.py` provide hook-based auto-injection mirroring py-kit. Stack: shadcn/ui + Tailwind + react-hook-form + Zod + TanStack Query + Drizzle + Server Actions + Better Auth. Next.js 16-targeted (proxy.ts, Cache Components, async Request APIs, async params). |
| 1 ファイル = 1 ユースケース | References 分割の設計原則（PR135 で確立、QA-073）。フック自動 inject 前提で、編集対象ファイルが 1 つマッチしたら 1 つの reference だけ inject されるよう、複数ファイル種別を 1 つに詰めない・比較や選定セクションは持たない。違反例は [[bestprac-over-usecase-references-bloat]] 参照。 |
| next-references-injection | next-kit の PreToolUse(Edit/Write/MultiEdit/Read) フック名。py-kit の `py-references-injection` と同じ **ref-inject 注入形式**（PR158 で移行）。`injection_rules.yaml` で対象ファイルを match させ、マッチしたパターンの **required は本文全量・optional は path + description のみ** を `decision: block` の reason に注入する。重複注入はパターン単位の TTL トークン `~/.claude/tokens/next-kit/{session_id}.yaml`（各エントリ `expires_at` = 注入時刻 + TTL）で抑制し、`now < expires_at` の間はスキップ・期限切れで再注入（TTL デフォルト 3600 秒、env `NEXT_KIT_INJECTION_TTL` で上書き）。Read も対象なので issue-scan など読み取り経路でも効く。env `NEXT_KIT_INJECTION_LANG=jp` で日本語版テンプレに切替（デフォルト en）。注入設計の詳細は [[ref-inject 注入設計]] と同一。PostToolUse の [[next-ts-check]] は別フックとして併設したまま。 |
| next-ts-check | next-kit の PostToolUse(Edit/Write/MultiEdit) フック名（`hooks/ts_check.py`）。`*.ts` / `*.tsx` 編集後に、編集ファイルから上方向に `tsconfig.json` を探索して `tsc --noEmit --incremental` を実行する。型エラーがあれば stdout に出力して Claude が認識・修正できるようにする。`decision: block` は使わない（作業フローを止めない）。next-kit v3.2.0 で追加（PR143）。 |
| 注入トークン | py-kit/next-kit が `~/.claude/tokens/{plugin}/{session_id}-{patternhash}` に作る空ファイル（プラグインごとにサブフォルダ）。「この injection_rules パターンの reference はこのセッションで注入済み」の印で、**マッチしたパターン単位**（ファイル単位ではない）。同じパターンにマッチする別ファイルはそのパターンの reference をスキップし、追加パターンにマッチするファイルはその追加パターンの reference だけ注入する。注入要否はトークンの存在で決まる。トークンはセッション全体で生きるため各パターンはセッション中1回だけ注入される（once-per-pattern）。空ファイルは `~/.claude/tokens/{plugin}/` に溜まり続け自動削除はされない（PR150 でファイル単位導入、PR151 でパターン単位 + `~/.claude/tokens/` 配下に変更、PR155 で session-kit 削除によりターンごとリセットは廃止）。 |
| kit-hooks-index-sync | リンクルール（`.claude/rules/feature/kit-hooks-index-sync.md`）。`py-kit` / `next-kit` / 将来の `*-kit` の references 自動注入構造（`hooks/inject_references.py` / `hooks/hooks.json` / `templates/injection.{md,jp.md}.j2` / `references/index.yaml` / `index.jp.yaml` / `injection_rules.yaml` / `CLAUDE.md` 系）が plugin 間で揃っていることを保証する。共通化は不可能と判断され（PR140 で `refs-inject-kit` プラグイン試行が `premature-cross-plugin-centralization` で却下された結果）、ルールで「同コミットで両方を変える」ことを強制する形にした。 |
| issue-scan | A work-kit skill (`/work-kit:issue-scan`) that picks one project source file at random per invocation and Reads it. The matching `*-kit` plugin's `PreToolUse(Read)` reference auto-injection hook supplies the applicable conventions; the skill compares the file (and related sibling/dependency files for context) against those references and records findings via `/work-kit:issue-save`. Already-scanned files are excluded via `_index.archive.yaml`. |
| issue-create | A work-kit skill (`/work-kit:issue-create`) that takes the user's input describing one or more problems, splits it into independently actionable issues, and records each via `/work-kit:issue-save`. |
| issue-save | A work-kit shared sub-skill (`/work-kit:issue-save`, `disable-model-invocation`) that persists one issue: reads `.work/issues/_index.yaml`, increments `last_id`, writes `ISSUE-{N}.md` (provenance-stamped via `mark-generated`), and appends the entry to `_index.yaml`. Called by `issue-scan` and `issue-create`. |
| .work/issues/ | Project-local issue management directory created by work-kit setup. Contains open issue files (ISSUE-{N}.md), a `closed/` subfolder, a local-only `_index.yaml` (git-excluded), and a git-tracked `_index.archive.yaml` with closed issues and scan history. |
| wontfix | An issue resolution status (`resolution: wontfix` in `_index.archive.yaml`) meaning the problem is acknowledged but will not be fixed intentionally. Issues closed as wontfix are excluded from future scan results. |
| jp-mirror-translator | A subagent in work-kit (`plugins/work-kit/agents/jp-mirror-translator.md`) that translates between English originals and JP mirrors. Model: haiku. Pass a `.md` path to generate/update the `.jp.md`; pass a `.jp.md` path to update the English original (added in PR133). |
| qa-review | A work-kit skill (`/work-kit:qa-review [PR{N}]`) that reads a PR's QA.md and presents each unresolved entry via AskUserQuestion in batches of up to 4. Answers are held in the prompt history; QA.md is updated in a single pass after all batches are answered (added in PR139). |
| impl-review | A work-kit skill (`/work-kit:impl-review`) that analyzes PR commits and walks the user through each change area interactively via AskUserQuestion. Prioritizes the in-progress session PR; the overview list is held internally (not shown upfront). Supports a deep-dive mode for detailed per-area explanation. Designed for mobile/SSH sessions where reading diffs directly is impractical (added in PR139). |
| issue-tool | A work-kit helper script (`plugins/work-kit/scripts/issue-tool.py`). The `close` subcommand moves `.work/issues/ISSUE-{N}.md` to `.work/issues/closed/`, removes the entry from the local `_index.yaml`, and appends a `closed_issues` entry (with `linked_pr`, `resolution`) to `_index.archive.yaml`. Invoked by the `merge` skill from the TODO.md `## 関連イシュー` table (added in PR146). |
| 関連イシュー | The `## 関連イシュー` section in TODO.md (columns: `ID / 概要 / resolution`). Lists `.work/issues/ISSUE-{N}` entries the PR is intended to resolve. The `merge` skill's Step 5 reads this table and calls [[issue-tool]] `close` for each row, auto-closing the issues and recording `linked_pr` in the archive (added in PR146). Omit the section entirely when the PR has no related issues. |
| ref-inject | A plugin (`/ref-inject:apply`) that **attaches the reference auto-injection mechanism to a target plugin** (new or existing) — the `*-kit` style hook used by py-kit / next-kit. Copies the injection files from `plugins/ref-inject/templates/` (hooks + references skeleton only). **Scoped to the injection part only**: it does NOT own `plugin.json`, the plugin's root `CLAUDE.md`, or `marketplace.json` (those are plugin-creator's job). **No generator script** — the `apply` skill has Claude read each template and write the destination file itself, substituting placeholders (`__PLUGIN_NAME__` / `__ENV_PREFIX__` / `__LOG_TAG__` / `__DEFAULT_TTL__`); this keeps the structure in context (decided PR156 — an earlier `scripts/generate.py` was removed). Not a shared runtime (that was rejected — see [[premature-cross-plugin-centralization]]); it automates the copy-paste the incident log blessed. The skill was renamed `create` → `apply` (PR156) because it both adds to existing plugins and seeds new ones. Created in PR156 (mechanism only; py-kit/next-kit/claude-kit migration deferred to follow-up PRs). |
| ref-inject 注入設計 | The injection design baked into the hook ref-inject generates (decided PR156). `required` references are injected **full-body**, `optional` as **path + description only**. The token at `~/.claude/tokens/{plugin}/{session_id}.yaml` is a **pattern-keyed YAML map** (each entry `{expires_at: epoch}` where `expires_at = injection time + TTL`, extensible). Skip while `now < expires_at`; re-inject once `now >= expires_at` (TTL default 3600s, overridable via `settings.json` `env` `{PREFIX}_INJECTION_TTL`). Storing `expires_at` rather than `injected_at` means a TTL env-var change does not retroactively affect already-written entries (changed PR157). Every hook fire cleans expired entries and deletes emptied token files. **No `PreCompact` hook** — after `/compact` the body re-injects once the TTL elapses; a dedicated compact-refresh hook was judged unnecessary overhead (PR156). Supersedes the per-pattern empty-file token ([[注入トークン]], PR150/151) and pointer-only injection ([[injection-hook-full-body-bloat]], PR147) — bodies are back because the TTL token throttles re-injection. |
