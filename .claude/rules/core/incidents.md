# Incidents

| Date | Summary | Detail |
|---|---|---|
| 2026-05-21 | Editing files directly on master causes merge conflicts when merging worktrees. Always use work-start and work inside the worktree. | [detail](../../references/incidents/master-direct-edit-causes-merge-conflict.md) |
| 2026-05-23 | Designing skills to load other skills in Step 0 consumes 2,500×N tokens per invocation and bloats the context. Embed decision knowledge in the skill's own References section. | [detail](../../references/incidents/skill-reading-token-cost.md) |
| 2026-05-23 | Multiple skills were maintaining the same decision criteria inline, creating duplication. Consolidate shared knowledge for creator skills in `references/` and reference it from each skill. | [detail](../../references/incidents/creator-skill-inline-duplication.md) |
| 2026-05-24 | The merge skill auto-executed `git merge` without explicit user instruction. Even if permission was granted earlier in the session, each merge requires a separate instruction. Fix: added Critical Prohibition section to SKILL.md and an absolute-prohibition rule to Step 6. | [detail](../../references/incidents/merge-auto-execution-without-permission.md) |
| 2026-05-24 | The `.work/specs/` folder name implied official specifications, causing it to be treated as important documentation even though it is not auto-loaded by AI, leading to it becoming stale. Use an informal name like `notes/` for folders not auto-loaded by AI. | [detail](../../references/incidents/work-folder-name-implies-official-docs.md) |
| 2026-05-24 | The merge skill called conversation-to-claude from the master cwd, so generated glossary/incidents were committed directly to master instead of being included in the PR branch. Fix: cd to the worktree before delegating. Lesson: explicitly control cwd when delegating to skills that run git commit. | [detail](../../references/incidents/conversation-to-claude-master-direct-commit.md) |
| 2026-05-24 | Validating JP warning comments with `head -5` caused files with frontmatter (warning at line 7+) to be falsely detected as missing the warning, inflating the count to 39 instead of the actual 25. When checking file-header metadata, skip frontmatter and inspect the start of the body. | [detail](../../references/incidents/header-check-misses-frontmatter-files.md) |
| 2026-05-24 | Creating `hooks/prompts/*.md` with hook-creator did not create `*.jp.md` JP mirrors. The `skill-jp-mirror-sync.md` rule only covered SKILL.md, not hook prompts. Fix: added `hook-prompts-jp-mirror-sync.md` rule. | [detail](../../references/incidents/hook-prompts-jp-mirror-missing.md) |
| 2026-05-24 | When extracting a skill step into a standalone skill, a PR that already modified the same step existed on master, requiring two master-sync rounds after implementation. Before extracting a step, check recent changes with `git log --oneline master -- {file}`. | [detail](../../references/incidents/extract-step-check-master-first.md) |
| 2026-05-24 | Editing `CLAUDE.md` without updating the `CLAUDE.jp.md` JP mirror. Neither `skill-jp-mirror-sync.md` nor `hook-prompts-jp-mirror-sync.md` covered CLAUDE.md. Fix: added `claude-md-jp-mirror-sync.md` rule. | [detail](../../references/incidents/claude-md-jp-mirror-missing.md) |
| 2026-05-24 | Editing `incidents.md` / `glossary.md` without updating the `rules-jp/` JP mirrors, leaving them out of sync for a long time. Fix: added `incidents-glossary-jp-mirror-sync.md` rule to enforce same-commit updates. | [detail](../../references/incidents/incidents-glossary-jp-mirror-missing.md) |
| 2026-05-24 | When multiple Stop hooks fire simultaneously, `"Read and follow: /path"` instructions are disabled by the `stop_hook_active` guard. Claude executes direct instructions (e.g. notify-aituber) first and re-Stops; on the second firing all hooks are skipped because stop_hook_active=true, so indirect-reference instructions are never executed. Fix: reverted to embedding file content directly in reason/stdout. | [detail](../../references/incidents/stop-hook-multiple-block-race-condition.md) |
| 2026-05-24 | When batch-changing hook patterns in PR115, work-kit/dev-kit/ui-kit were updated but 5 UserPromptSubmit and 1 PostToolUse entries in claude-kit/hooks.json were missed. Lesson: before a cross-plugin hook pattern change, verify all plugins with `grep -r "<pattern>" --include="*.json" .`. | [detail](../../references/incidents/hook-pattern-change-missed-plugin.md) |
| 2026-05-25 | Inside a `python -c "..."` status-line command, the double-quotes in `r5["resets_at"]` broke the outer shell string, causing a NameError the moment rate_limits.resets_at appeared and crashing the status line. Fix: changed to `r5.get('resets_at')` using only single quotes. | [detail](../../references/incidents/statusline-python-quote-nesting.md) |
| 2026-05-25 | Running `apply-statusline.py` with WSL Python while Claude Code runs natively on Windows caused a different settings.json to be overwritten, so changes were never applied (no error either). Lesson: scripts using `Path.home()` must run in the same Python environment as Claude Code. | [detail](../../references/incidents/path-home-cross-env-mismatch.md) |
| 2026-05-24 | The `skill-creator-dispatch` hook only covered `UserPromptSubmit`, so Claude directly edited `SKILL.md` on ambiguous prompts. Fix: added `PreToolUse` block-type hooks to 4 plugins (claude-kit/dev-kit/ui-kit/work-kit). Lesson: file-type guards must be implemented as `PreToolUse` hooks, not `UserPromptSubmit`. | [detail](../../references/incidents/skill-creator-dispatch-userpromptsubmit-only.md) |
| 2026-05-25 | Claude started editing files directly on master despite a UserPromptSubmit hook explicitly stating that work-start must be run first. Hook instructions are a prerequisite for any implementation and must never be skipped, even for small changes. | [detail](../../references/incidents/work-start-skipped-despite-hook-instruction.md) |
| 2026-05-25 | `incidents.md` and `glossary.md` in `.claude/rules/core/` — the English originals always loaded by Claude — had all content written in Japanese, making them identical to their JP mirrors. Fix: rewrote all content in English (PR123). Rule: English-original rule files must contain only English. | [detail](../../references/incidents/core-rules-jp-content-in-en-file.md) |
| 2026-05-25 | skill-creator-dispatch PreToolUse was added to all 4 plugins (PR121), but claude-kit already guards SKILL.md globally — dev-kit/ui-kit/work-kit copies were redundant duplicates. Fix: removed from the 3 non-central plugins (PR124). Lesson: global guard hooks belong only in claude-kit; other plugins should add only plugin-specific hooks. | [detail](../../references/incidents/duplicate-hooks-in-non-claude-kit-plugins.md) |
| 2026-05-27 | When creating a JP mirror agent file with YAML frontmatter, the `<!-- Generated by -->` stamp was omitted and the JP mirror warning was placed before the frontmatter instead of after. Fix: always invoke `mark-generated`; insert the returned comment block immediately after the closing `---` of frontmatter, never before it. | [detail](../../references/incidents/agent-jp-mirror-stamp-before-frontmatter.md) |
| 2026-05-27 | In a policy-only PR (PR138, with implementation deferred to a follow-up PR), AI included detailed Python pseudocode of the hook in the design QA. User sharply corrected: don't write implementation detail in policy QA. Lesson: when a PR is scoped to "policy / design only", QA must stop at *what / why / where*, never *how*. | [detail](../../references/incidents/design-qa-implementation-creep.md) |
| 2026-05-28 | AI created a JP mirror sync rule for `agents/*.md` (PR137), but the user deleted it as unnecessary (PR141). JP mirror sync rules are only needed for files that are frequently edited (SKILL.md, hook prompts, CLAUDE.md). Agent files are structural definitions rarely modified — the warning comment in the file itself is sufficient. | [detail](../../references/incidents/unnecessary-jp-mirror-sync-rule-for-agents.md) |
| 2026-05-28 | In PR140, AI proactively built a separate `refs-inject-kit` plugin to centralize injection rules across py-kit / next-kit / future plugins with `${plugin}/path` placeholder syntax. User rejected it as too complex; the whole plugin (5 commits, ~700 LOC) was reverted. Lesson: don't centralize across plugins until 3+ consumers exist or drift incidents recur — copy-paste between 2 plugins is cheaper than a placeholder/path-resolution layer. | [detail](../../references/incidents/premature-cross-plugin-centralization.md) |
| 2026-05-28 | In PR140, AI converted `references/index.yaml` to `index.md` (Markdown table) on human-readability grounds, requiring a regex Markdown parser in the hook. User caught it: the file is code-consumed so YAML is correct. Reverted. Lesson: code-consumed config stays in structured format (YAML/JSON); generate a Markdown view from YAML if humans need one. | [detail](../../references/incidents/markdown-for-code-consumed-config.md) |
| 2026-05-28 | In PR140, AI added 5 reference files (python-script / tkinter / health / cheatsheet / refactoring-judgement) without binding them to any `injection_rules.yaml` pattern. User asked to verify — a small orphan-check script revealed the gap. Lesson: after every `injection_rules.yaml` edit, run a YAML-vs-filesystem orphan-detection script. | [detail](../../references/incidents/orphan-references-not-checked.md) |
| 2026-05-28 | In PR140, AI wrote `tools/**/*.py` as an injection_rules pattern, which only matches project-root `tools/` and misses monorepo subprojects like `packages/foo/tools/bar.py`. User caught it. Fixed to `**/tools/**/*.py`. Lesson: for "by-name folder" glob patterns, prefix with `**/` by default — root-anchored patterns are for files that must literally sit at the project root. | [detail](../../references/incidents/glob-pattern-missing-recursive-prefix.md) |
| 2026-05-28 | In PR135, AI built next-kit references optimized for "best-practice coverage" (46 files packed with multiple file kinds per doc, plus selection/comparison/trade-off sections). User rejected: the hook-injection use case requires 1 file = 1 use case so editing `query.ts` doesn't pull in 5 unrelated file kinds. Re-split to 90 files; deleted all selection/comparison/trade-off content. Lesson: when designing references for hook auto-injection, start from `injection_rules.yaml` patterns (the trigger map), not a content TOC. | [detail](../../references/incidents/bestprac-over-usecase-references-bloat.md) |
| 2026-05-28 | In PR135, AI added an entry to `.claude/rules/feature/_overview.md` without realizing the file had already been deleted on master (by PR141 cleanup). Ghost-file edit propagated to merge prep, requiring a separate `git rm` commit on the branch to restore parity. Lesson: on long-lived branches, before editing rule / overview / index files, verify `git show master:{file}` exists; for `_overview.md`-style indexes, prefer not to maintain them at all. | [detail](../../references/incidents/master-deletion-overlooked-on-long-branch.md) |
| 2026-05-28 | In PR131, a sub-skill (`issue-log`) was designed with CLI-style argument flags (`--title --type --priority ...`). Claude Code skills don't take arguments — they are Markdown files loaded into context. Fix: rewrote the "Overview" section to list expected inputs as natural-language bullet points instead of a CLI flag table. | [detail](../../references/incidents/skill-cli-args-format.md) |
| 2026-05-28 | In PR131, a starter template file `_index.yaml` was placed under `templates/.work/issues/`, but the sibling `.gitignore` in the same directory listed `_index.yaml` — so the template file was silently gitignored in the plugin repo and couldn't be committed. Fix: drop the template file; have `setup.py` write `_index.yaml` after `_expand()`. Lesson: before placing a file under a directory with a `.gitignore`, run `git check-ignore -v` to confirm it's trackable. | [detail](../../references/incidents/template-under-gitignore.md) |
| 2026-05-28 | In PR131, a sub-skill that persists one issue was named `issue-log`. User flagged that "log" reads as error-log / debug-log — wrong connotation for durable record persistence. Renamed to `issue-save`. Lesson: when naming a skill / function that *persists* data, use `save` / `write` / `record`; reserve `log` for genuine logging-stream output. | [detail](../../references/incidents/skill-name-log-implies-error-log.md) |
| 2026-05-28 | The py-kit/next-kit references auto-injection hook injected the **full body** of every matched reference into the `decision: block` reason, bloating context on every matching file operation (worsened because it also fires on Read). When the user flagged it, AI first proposed removing the Read matcher — but the trigger wasn't the problem, the injected content volume was. Fix (PR147): inject pointers (path + description) as **absolute paths**, not full bodies — and note `${CLAUDE_PLUGIN_ROOT}` is NOT expanded in injected reason text (only in hooks.json), so the hook script must emit absolute paths itself. Read matcher kept (needed for issue-scan). | [detail](../../references/incidents/injection-hook-full-body-bloat.md) |
| 2026-05-28 | In PR153, two dispatch prompt files (`hook-creator-dispatch` / `plugin-creator-dispatch`) existed but were unwired (orphaned) — leftovers from an old UserPromptSubmit→PreToolUse migration. AI first recommended deleting them (plugin-creator matched all of `plugins/**`, seemed too broad). User corrected: these were intended-but-unmigrated features; convert them to the current PreToolUse(Edit/Write) pattern instead, and solve the breadth concern with rule ordering (broad catch-all last). Lesson: orphaned artifacts from a style migration should be modernized, not deleted — confirm before removing. | [detail](../../references/incidents/orphaned-dispatch-prompts-modernize-not-delete.md) |
| 2026-05-28 | Two parallel PRs (PR152, PR153) independently bumped claude-kit to v3.28.0. PR153 merged first, so PR152's bump collided with master. Fix: detected during Step 3 (master compatibility check) and rebumped to v3.29.0 before merging. Lesson: when `git diff HEAD..master` shows a version bump on the same plugin, immediately bump to the next version on the PR branch before merging. | [detail](../../references/incidents/parallel-pr-version-bump-collision.md) |
| 2026-05-28 | `session-kit` (built PR150/151) deleted injection tokens each turn so references re-inject per conversation turn. But PR147 had already changed injection to pointer-only (path+description, no bodies), gutting the value of per-turn refresh — yet session-kit lived on until PR155 removed it. Lesson: when a change (PR147) undercuts the premise of existing infrastructure, revisit that infra then, not several PRs later; don't keep a whole cross-plugin plugin alive for a marginal optimization. | [detail](../../references/incidents/session-kit-removed-after-premise-change.md) |
| 2026-05-29 | In PR156 (building `ref-inject`), AI added a deterministic `scripts/generate.py` and a `PreCompact` refresh hook, both removed by the user. The user wanted Claude-driven copy+substitute (stays in context) and the TTL token already re-injects after `/compact`. Lesson: prefer Claude-driven generation over a script for per-plugin files; don't add a hook for behavior the TTL/existing mechanism already covers — default to the leanest mechanism. | [detail](../../references/incidents/ref-inject-overbuilt-script-and-hook.md) |
| 2026-05-29 | In PR156, creator-dispatch `PreToolUse` blocks (session-flag: block first edit, pass after) fired on SKILL.md / rule / .py / .yaml / CLAUDE.md writes. AI retried the raw write and hand-authored them instead of invoking the creator skill; the user had to say "use /skill-creator" and "/py-script". Lesson: a dispatch block means invoke the named creator skill (skill-creator / rule-creator / py-script / claude-creator), not retry the edit. | [detail](../../references/incidents/creator-dispatch-block-means-invoke-creator.md) |
| 2026-05-29 | In PR157 (migrating py-kit onto `ref-inject`), regenerating the injection hook tripped creator-dispatch catchall blocks (plugin-creator on `.py`, j2-stamp-check on `.j2`, claude/rule-creator on CLAUDE.md/glossary). Here the files are owned by `/ref-inject:apply`, not plugin-creator, so the generic `plugins/` catchall is a **false positive** — pass through the session-flag block and proceed via ref-inject:apply; do not detour into plugin-creator. Refines `creator-dispatch-block-means-invoke-creator`: the "invoke the creator" rule applies to the specific dispatch rules, not the catchall when a dedicated mechanism skill owns the files. | [detail](../../references/incidents/ref-inject-reapply-dispatch-false-positive.md) |
| 2026-05-29 | In PR158 (mirroring PR157's py-kit→ref-inject migration for next-kit), the gold-standard files (migrated py-kit, expires_at-updated ref-inject template) were missing because the pr-handoff-reserved worktree branched off master *before* PR157 merged (`git merge-base --is-ancestor` was false). An early diff showed py-kit still on the old token design. Fix: `git merge master` into the branch first, then mirror. Lesson: when a PR mirrors a predecessor PR, verify the predecessor is in the branch history before treating in-tree files as the template — a pr-handoff-reserved branch is not guaranteed to contain its predecessor's work. | [detail](../../references/incidents/worktree-reserved-before-predecessor-merge.md) |
| 2026-05-29 | In PR159, a description in claude-kit's `references/index.yaml` contained `…paths: design…` — the `: ` (colon-space) inside the unquoted YAML scalar was parsed as a nested mapping and broke `yaml.safe_load`, which the injection hook depends on. Fix: rephrase to drop the colon-space (or quote the whole value). Lesson: when authoring `index.yaml` / `injection_rules.yaml` descriptions, never put `word: ` inside an unquoted scalar; validate every edit with `yaml.safe_load` (and run the orphan check). | [detail](../../references/incidents/yaml-unquoted-colon-space-breaks-parse.md) |
| 2026-05-29 | In PR161, the plan was to remove an AI-called skill (`mark-generated`) entirely and let the ref-inject hook inject its reference instead. But the hook fires only on Claude's **Write/Edit/Read tool calls** — not on files produced by helper scripts (work-kit's `setup-task.py`) or file types outside `injection_rules` (html-kit's `.js`/`.css`). Full removal would have silently dropped stamping for those. Fix: keep the skill as a **thin wrapper** (fallback) and move only its spec into the injected reference. Lesson: before converting an AI-called skill into "just an injected reference", check whether every caller's target is on the Write/Edit path; script-generated and non-matched files still need the skill. | [detail](../../references/incidents/injection-only-fires-on-write-edit-path.md) |
| 2026-05-29 | In PR160, after the user asked to "also do claude-kit", I treated it as the pending next-PR candidate — but that migration had already merged on master as PR159, and master had advanced two PRs (PR159+PR161) past the stale session-start snapshot. Caught during investigation; also required `git merge master` since the PR160 worktree branched off the pre-PR159 commit. Lesson: before proposing/starting follow-up or cross-PR work, check the actual current master (`git log master`, `git worktree list`) — the session-start git snapshot is stale and a recorded next-PR candidate may already be merged by a parallel session. | [detail](../../references/incidents/stale-session-git-snapshot-already-merged-followup.md) |
| 2026-05-30 | The `jp-mirror-translator` subagent placed the JP warning comment before the YAML frontmatter in SKILL.jp.md (must be after the closing `---`). This breaks GitHub frontmatter rendering. Fix: after every jp-mirror-translator run, verify the warning comment is after the closing `---`, not at the file top. | [detail](../../references/incidents/jp-mirror-translator-warning-before-frontmatter.md) |
| 2026-05-30 | `pr-handoff` reserved two "即時実施可" candidates with the same `id: 165` in index.yaml, creating duplicate PR numbers and two worktrees (`wt-PR165` and `wt-PR165b`). Fix: after pr-handoff, verify no duplicate ids in index.yaml; manually renumber the later-reserved PR if duplicates exist. | [detail](../../references/incidents/pr-handoff-duplicate-pr-numbers.md) |
| 2026-05-30 | In PR172 (rename work-kit → workspace), master PR167 had added `plugins/work-kit/skills/config/` while the rename was in progress. During `git merge master`, git flagged "file added inside a renamed directory" conflict. Resolution: manually `mv` the new files to `plugins/workspace/skills/config/`, update internal references, `git rm` old path, `git add` new path. Lesson: when a plugin rename PR and a feature-add PR to that plugin are in flight simultaneously, merge will have location conflicts for new files added to the old folder path — move them to the new path and update references. | [detail](../../references/incidents/plugin-rename-parallel-extension-conflict.md) |
