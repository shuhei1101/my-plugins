---
created_at: 2026-05-24
updates:
  - 2026-05-24 — 初回作成
related_notes: []
related_prs:
  - PR116
  - PR118
---

# ステータスライン非表示バグ — PR116 後の不具合メモ

## 概要

commit `4144128`（feat: statusline-ctx-green #PR116）マージ後にステータスラインが表示されなくなったと報告された。

## 調査結果

### apply-statusline.py の変更内容（PR116）

`ctx_color` の条件式のみ変更:
- 変更前: `up < 50` のとき `ctx_color = ''`（色なし）
- 変更後: `up < 50` かつ `up is not None` のとき `ctx_color = '\033[32m'`（緑）

### テスト結果

コマンドをテスト入力で実行 → 正常に出力された:
```
myproject/ | [33mClaude Sonnet 4.6[0m | [32mctx 30%[0m (50k/200k)
5h 10% | 7d 5%
```

### 誤操作

調査中に `exec(open('apply-statusline.py').read())` を誤って実行し、`~/.claude/settings.json` に最新の statusLine 設定を適用してしまった。

この実行が実際の修正になった可能性がある（実行前の settings.json の状態は不明）。

### プラグインキャッシュの状態

- キャッシュ最新バージョン: `3.21.1`
- リポジトリバージョン: `3.22.0`（PR116でバンプ）
- autoUpdate: true（GitHub から自動更新）

## 仮説

1. **settings.json から statusLine が消えていた**: 何らかの原因（autoUpdate 失敗など）で statusLine キーが削除された
2. **別のセッションで apply-statusline.py が壊れたバージョンで実行された**: 可能性は低い（コマンドは正常動作確認済み）

## 結論

### 真因（PR116 とは無関係の古いバグ）

`apply-statusline.py` の statusLine コマンドに以下のコードが含まれていた:

```python
"...parts2.append(...+(' (~'+tt(r5[\"resets_at\"],'%H:%M')+')' if r5.get('resets_at') else ''))..."
```

このコマンドは `python -c "..."` の中で実行されるが、内側の `r5["resets_at"]` のダブルクォートが外側ダブルクォートを切ってしまい、シェル解釈時に `resets_at` がベア単語（未定義変数）になる。結果として Python が `NameError: name 'resets_at' is not defined` を出して落ちる。

`rate_limits.resets_at` が存在する瞬間（トークン消費でレート情報が確定したとき）にのみ発症するため、セッション開始直後は問題なく、しばらく経つと突然消える現象になっていた。

### 修正

- `r5["resets_at"]` → `r5.get('resets_at')` （シングルクォートのみで完結）
- `r7["resets_at"]` も同様
- PR116 の緑機能はそのまま維持

### 二次的な学習

- `Path.home() / ".claude" / "settings.json"` は実行環境で変わる
  - WSL Python: `/home/{user}/.claude/settings.json`
  - Windows native Python: `C:\Users\{user}\.claude\settings.json`
- Claude Code が読む settings.json と、`apply-statusline.py` が書き込む settings.json が違うと、変更が見えない（エラーも出ない）
- 必ず Claude Code と同じ Python 環境からスクリプトを実行する
