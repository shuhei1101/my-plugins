# QA — PR56 未決定事項

**運用方針**:
1. 未決定事項が発生 → このファイルに QA-XXX（連番）として追加
2. ユーザーと議論・判断 → 決定
3. 決定したら → 対応する仕様書・ドキュメントに反映する

---

## QA-001: 同期対象ファイルは何か

**状態**: 解決済み

**決定**: settings.json, CLAUDE.md, skills/ を基本対象とし、
スキル実行時にユーザー配下をスキャンして差分を AI が提案する方式にする。

---

## QA-002: プラグイン名・配置先

**状態**: 解決済み

**決定**: 既存の `claude-kit` プラグインにスキルとして追加する。
スキル名は `env-sync`（WSL↔Windows 双方向）。

---

## QA-003: WSL/Windows パスの自動検出方法

**状態**: 解決済み

**決定**: 自動検出。WSL から `/mnt/c/Users/$(cmd.exe /c echo %USERNAME% 2>/dev/null | tr -d '\r')` で
Windows ユーザーホームを推定する。
