# stop.py / references_edit_guard.py argv ガード追加

**ブランチ**: fix/stop-py-argv-guard
**作成日**: 2026-06-02

## 作業内容

| # | 内容 | 状態 |
|---|---|---|
| 1 | `plugins/work/hooks/scripts/stop.py` に `len(sys.argv) < 2` fail-open ガードを追加 | 済 |
| 2 | `plugins/ref-inject/templates/hooks/scripts/references_edit_guard.py` に同ガードを追加（テンプレート先行） | 済 |
| 3 | `plugins/claude-kit/hooks/scripts/references_edit_guard.py` に同ガードを追加 | 済 |
| 4 | `plugins/dev-kit/hooks/scripts/references_edit_guard.py` に同ガードを追加 | 済 |
| 5 | 修正後、4 コピーのガードが一致していることを確認 | 済 |

## QA

なし（イシュー回答で確定済み）

## テスト

- `stop.py` を引数なしで直接実行して IndexError が発生しないことを確認 → returncode: 0, stderr: (none)
- `references_edit_guard.py` (template) を引数なしで直接実行して IndexError が発生しないことを確認 → returncode: 0, stderr: (none)

## 変更内容

`sys.argv[1]` を参照する直前に `if len(sys.argv) < 2: return` の fail-open ガードを 4 ファイルに追加。
- `stop.py`: `exit_if_stop_loop(data)` 直後に挿入
- `references_edit_guard.py` (3 コピー): `emit_block_reason(pathlib.Path(sys.argv[1]))` の直前に挿入

## 関連イシュー

| イシュー | タイトル | 状態 |
|---|---|---|
| ISSUE-093 | `stop.py` が `sys.argv[1]` を無防備に参照し、引数欠落で IndexError クラッシュ | in_progress |

## 参考ドキュメント

- `.work/notes/バグ・不具合/stop-py-argv-guard.md`
