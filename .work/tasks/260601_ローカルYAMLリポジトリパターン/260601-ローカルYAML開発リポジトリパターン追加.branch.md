# ローカルYAML開発リポジトリパターン追加

> ブランチ: `docs/local-yaml-repository-pattern`

## 概要

dev-kit の Next.js バックエンドリファレンスに、ローカル開発用 YAML リポジトリパターンを追加する。
TypeScript インターフェースでデータアクセス層を抽象化し、本番は Drizzle/Supabase、
ローカル開発時は YAML ファイルを使って同じスキーマで読み書きできる仕組みをリファレンスとして文書化する。

### 実施条件

即時実施可

---

## 作業内容

| # | 作業 | 状態 |
|---|---|---|
| 1 | QA を記録する | [x] |
| 2 | `ローカルYAMLリポジトリ.md` を新規作成（`next/backend/` 配下） | [x] |
| 3 | `ローカルYAMLリポジトリ.jp.md` を JP ミラーとして作成 | [x] |
| 4 | `_index.yaml` / `_index.jp.yaml` に新エントリを追加 | [x] |
| 5 | ノートを更新する | [x] |
| 6 | クラス/interface を廃止し `type` + ファクトリ関数の関数型スタイルに修正 | [x] |
| 7 | quest-pay 準拠に全面見直し（関数ごとの `type` 定義・素の関数・`query.ts`/`db.ts` 分離・ファイル名から「リポジトリ」除去） | [x] |
| 8 | トランザクション対応（`db` 引数の受け渡し維持・YAML はスナップショット/ロールバック + ロック） | [x] |

---

## 変更内容

| # | ファイル | 変更概要 |
|---|---|---|
| 1 | `plugins/dev-kit/references/next/backend/ローカルYAML開発DB.md` | 新規作成 → quest-pay 準拠の関数型スタイルに全面書き直し（旧 `ローカルYAMLリポジトリ.md` からリネーム） |
| 2 | `plugins/dev-kit/references/next/backend/ローカルYAML開発DB.jp.md` | 〃 |
| 3 | `plugins/dev-kit/references/.ref-injects/_index.yaml` | エントリのパス・description を更新 |
| 4 | `plugins/dev-kit/references/.ref-injects/_index.jp.yaml` | 〃 |

---

## テスト

| # | 確認項目 | 状態 |
|---|---|---|
| — | — | — |

---

## QA

| # | 質問 | 状態 |
|---|---|---|
| — | — | — |

---

## 参考ドキュメント

- [YAMLデータストア切り替え.md](.work/notes/コーディング規約・スタイル/YAMLデータストア切り替え.md)

---

## 関連ブランチ

docs/e2e-usecase-driven-design（マージ済み）

## 次ブランチ候補

| ブランチ名 | 概要 | 実施条件 |
|---|---|---|
| — | — | — |
