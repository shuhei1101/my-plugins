# QA — PR48 未決定事項

**運用方針**:
1. 未決定事項が発生 → このファイルに QA-XXX(連番)として追加
2. ユーザーと議論・判断 → 決定
3. 決定したら → 対応する仕様書・ドキュメントに反映する

---

## QA-001: スキル description のトリガー範囲

**状態**: 未決定

`ui-dev` スキルをいつ発動させるか:

- A: 開発系画面の作成・編集時に常に発動(新規・既存問わず、開発系画面を扱う作業全般)
- B: 開発系画面を**新規作成**する場合のみ。既存画面への適用はユーザーが明示的に `/dev-kit:ui-dev` で呼び出す
- C: 「デバッグボタン」「開発画面」というキーワードがあった時だけ

**前提**: TODO の現案は A(開発系画面を扱う作業ならいつでも適用)。

---

## QA-002: 対象技術スタック

**状態**: 未決定

スキルが想定する技術スタック:

- A: バニラ HTML/CSS/JS のみ(フレームワーク非依存の汎用手順を書く)
- B: React/Vue/Svelte などの SPA フレームワークも考慮
- C: バックエンド(FastAPI/Flask/Django のテンプレート)からのサーバーレンダリングも考慮
- D: A〜C すべてカバー(各バリアントの実装スニペットを並べる)

**前提**: TODO の現案は A(バニラ JS で動く最小実装を提示。フレームワーク統合は将来追記)。

---

## QA-003: 開発系画面の判定

**状態**: 未決定

「開発系画面」かどうかをどう判定するか(本番ではボタンを表示しない条件):

- A: 環境変数 / ビルドフラグ(`NODE_ENV !== "production"` 相当)で判定
- B: 特定 URL パス(`/dev/...`、`/admin/...`)配下のみ表示
- C: ローカルストレージのフラグ(`localStorage.uiDevEnabled === "true"`)で切替
- D: 規約として書くだけ(各プロジェクトで判定方法を決める)

**前提**: TODO の現案は A(`process.env.NODE_ENV !== "production"` または同等のビルド変数)。

---

## QA-004: フロートボタンの配置とスタイル

**状態**: 未決定

デフォルト位置・見た目:

- 位置: 右下(`position: fixed; bottom: 16px; right: 16px;`)を推奨デフォルトとし、上下左右切替可
- アイコン: 「🐛」または「DEBUG」テキスト?
- サイズ: 40〜48px の丸ボタン?
- 既存 UI と被らないように `z-index: 9999` 程度?

**前提**: TODO の現案は「右下・🐛 絵文字・48px 丸・z-index 9999」をデフォルトに、各項目はオプションで切替可。

---

## QA-005: JS ログの収集方法

**状態**: 未決定

直近 N 行(デフォルト 100)のログを保持する手段:

- A: `console.log/info/warn/error` をラップしてリングバッファに溜める(透過的に既存ログを取得)
- B: 専用 `devLog()` 関数を用意して、明示的に呼んだものだけ溜める
- C: ブラウザの `window.onerror` / `unhandledrejection` も合わせて捕捉

**前提**: TODO の現案は A + C(console をフックしつつ、未捕捉エラーも自動で取り込む)。

---

## QA-006: コピー時の JSON 形式

**状態**: 未決定

クリップボードにコピーする JSON のスキーマ案:

```json
{
  "page": "{画面の識別子(URL/ルート名)}",
  "files": {
    "html": ["path/to/file.html"],
    "css":  ["path/to/file.css"],
    "js":   ["path/to/file.js"],
    "backend": ["path/to/handler.py"],
    "other": []
  },
  "logs": {
    "limit": 100,
    "entries": [{"ts": "...", "level": "log", "args": ["..."]}]
  }
}
```

このスキーマで OK か?「関連ファイル」をどう登録するか(各画面に手動で書く / ファイル名規約から自動推測)。

**前提**: TODO の現案は「関連ファイルは各画面で `data-debug-files` 属性や JS 設定オブジェクトで明示登録」。

---

## QA-007: ログ行数のユーザー設定

**状態**: 未決定

デバッグモーダルから行数を変えられるとのことだが、保存方法:

- A: localStorage に保存(ブラウザ再読込でも維持)
- B: URL パラメータ(`?debugLog=200`)
- C: モーダル内のみで一時的に変更(リロードで戻る)

**前提**: TODO の現案は A(localStorage)。

---

## QA-008: ui-dev に関連 references の追加

**状態**: 未決定

今回はスキル単体で進めるが、フロントエンド全般の規約として `references/frontend.md` を埋める作業も
ついでに着手するか:

- A: 今回は ui-dev スキルのみ作成。`references/frontend.md` は別 PR で
- B: ui-dev に関係する範囲(UI デバッグ規約)だけ `references/frontend.md` に切り出す

**前提**: TODO の現案は A(スキルだけで完結)。
