# PR187 — migrate-legacy-task-folders

## 概要

PR168 で workspace のタスクドキュメント構造を新形式（`.work/tasks/{YYMMDD}_{title}/{branch-hyphenated}.md` の 1 ファイル統合）に刷新したが、既存の PR1〜PR167 の旧構造（`{YYYYMMDD}_{title}/PR{N}/{TODO.md,QA.md}` の 2 ファイル）はそのまま残してある。本 PR で旧構造を新構造に一括移行する。

### 背景

PR168 では「移行リスクのほうが高い」「必要になった時点で実施」と判断して移行を先送りした。実装後の運用で、旧構造のフォルダが残っていると pr-show / qa-review / merge 等の `find -type f -name "PR*.md"` 系コマンドが旧フォルダの古いドキュメントも拾ってしまうリスクがある。一斉に移行して構造を統一する。

### 何をするか

- 旧構造ファイルを走査するスクリプトを書く（ローカル一回限りでよい）
- 各 `{YYYYMMDD}_{title}/PR{N}/{TODO.md,QA.md}` を新形式 `{YYMMDD}_{title}/{branch-hyphenated}.md` に変換
  - 日付プレフィックスを 8 桁 → 6 桁にトリム
  - TODO.md と QA.md をマージして新 PR ドキュメントテンプレートに合わせる
  - ブランチ名は archive から取得（既マージ PR）または推定
- 旧フォルダを `git rm`、新ファイルを `git add`
- 動作確認: `find .work/tasks -type f -name "PR*.md"` で全件取得できることを確認

### 留意点

- 既マージ PR の TODO.md は履歴として残すだけで、運用上の意味は薄い → 完全変換にこだわらず、必要最小限のメタデータだけ保持する形でも可
- 「ブランチ名が分からない過去 PR」はファイル名を `PR{N}.md` で代替する案もあり

### 実施条件

即時実施可

## 作業内容

| 完了 | 作業内容 | 対象ファイル |
|---|---|---|
| - | 旧構造の全タスクフォルダを列挙 | - `.work/tasks/{YYYYMMDD}_{title}/PR{N}/` 全件 |
| - | 移行スクリプト（一回限り）を作成 | - 一時スクリプト |
| - | スクリプトでファイル名・内容を変換 | - 全旧フォルダ |
| - | 旧フォルダを git rm、新ファイルを git add | - 全旧構造 |
| - | `find .work/tasks -type f -name "PR*.md"` で全件取得できることを確認 | - 動作確認 |
| - | コミット | - |

## 変更内容

| ファイル名 | 新規/編集 | 内容 | 補足 |
|---|---|---|---|

## テスト

| ファイル名 | 新規/編集 | 内容 | 補足 |
|---|---|---|---|
| - | - | テスト追加なし（変換完了後の `find` 結果で検証） | - |

## QA

### QA-001: ブランチ名が分からない過去 PR のファイル名

**背景**: 既マージ PR はワークツリーが削除されているため、`PR{N}/{type}/{title}` 形式の元ブランチ名が分からないものがある。

| 案 | 内容 |
|---|---|
| A | `index.archive.yaml` から `task` + `title` を引いて `PR{N}-{type}-{title-slug}.md` を再構築 |
| B | シンプルに `PR{N}.md` で揃える（既マージは履歴目的なのでブランチ名は不要） |
| C | フォルダ名（`{YYMMDD}_{title}`）からタイトルを取り、`PR{N}-{title}.md` 形式に |

**推奨方式**: 着手時にユーザーと相談。B が一番シンプル。

**状態**: 未解決

**決定したら反映先**: 移行スクリプトの命名ロジック

### QA-002: TODO.md と QA.md のマージ方式

**背景**: 旧 TODO.md と QA.md を新フォーマットの 1 ファイル（`## 作業内容` / `## QA` セクション統合）に変換するときのマージ方式。

| 案 | 内容 |
|---|---|
| A | TODO.md の全セクションを新テンプレに当てはめ、QA.md を `## QA` セクションに挿入 |
| B | 既マージ PR は履歴目的なので、`# PR{N} — {title}` と「（旧構造のため履歴のみ）」とリンクだけ書いた最小ファイルにする |

**推奨方式**: 着手時にユーザーと相談。

**状態**: 未解決

**決定したら反映先**: 移行スクリプトのマージロジック

## 参考ドキュメント

- `plugins/workspace/templates/.work/tasks/yymmdd_xxx/PRNNN-type-title.md` — 新フォーマットのテンプレート

## 関連PR

| PR番号 | 概要 |
|---|---|
| #168 | PR ドキュメント単一ファイル化（本 PR の発端） |

## 次PR候補

| タイトル | 概要 | 実施条件 |
|---|---|---|
