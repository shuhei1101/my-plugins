# QA — PR31 未決定事項

**運用方針**:
1. 未決定事項が発生 → このファイルに QA-XXX（連番）として追加
2. ユーザーと議論・判断 → 決定
3. 決定したら → 対応する仕様書・ドキュメントに反映する

---

## QA-001: スキル名 ✅ 決定済み

**決定**: `vscode-workspace-sync`

---

## QA-002: フックのイベントとマッチングパターン ✅ 決定済み

**決定**: PostToolUse (Bash) + `tool_input.command` に `git worktree add` / `git worktree remove` が含まれるかチェック

---

## QA-003: フックスクリプトの言語 ✅ 決定済み

**決定**: Python (`json` モジュール)

---

## QA-004: ワークスペースファイルが見つからない場合の挙動 ✅ 決定済み

**決定**: 「見つかりません」と表示し、ユーザーにパスを入力してもらう

---
