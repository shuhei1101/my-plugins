<!-- Generated by work-kit/work-start v2.29.0 -->
# PR156 — ref-inject-generator

## 概要

リファレンス自動注入の仕組み（PreToolUse フックで編集対象パスに応じた reference を注入）を持つプラグインを**生成するジェネレータプラグイン `ref-inject`** を新規作成する。py-kit / next-kit が手作業でコピペ共有していた `inject_references.py` / `hooks.json` / templates / references 構造を、このジェネレータが雛形から展開する形にする。

今回は**ジェネレータ本体のみ**。生成される雛形には新しい注入設計を組み込む:

- **本文注入**: `required` は本文全量を注入 / `optional` はパス + description のみ
- **トークン**: `~/.claude/tokens/{plugin}/{session_id}.yaml`。pattern をキーにした拡張可能 YAML マップ（値は `injected_at` 等）
- **TTL**: デフォルト3600秒、env `{PLUGIN}_INJECTION_TTL`（秒）で上書き可。`now - injected_at < TTL` ならスキップ
- **クリーンアップ**: 発火のたびに全 `{session_id}.yaml` を走査し期限切れキーを削除 → 空ファイルは削除
- **/compact 後リフレッシュ**: `PreCompact` フックでそのセッションの `{session_id}.yaml` を削除 → 次の編集で再注入
- 言語切替 `{PLUGIN}_INJECTION_LANG=jp` は維持

### 実施条件

即時実施可

### 関連PR

| PR番号 | 概要 |
|---|---|
| #155 | session-kit 削除（トークン管理の前提変更） |
| #147 | 注入を本文全量→パス+description のみに変更（今回 required は本文全量に戻す） |

## 作業内容

| 完了 | 作業内容 | 対象ファイル |
|---|---|---|
| - | QA.md に未決定事項を記録する | - `.work/tasks/.../PR156/QA.md` |
| - | `.work/notes/` のノートを作成する | - `.work/notes/ref-inject-generator.md` |
| - | plugin.json + marketplace.json 登録 | - `plugins/ref-inject/.claude-plugin/plugin.json` / `.claude-plugin/marketplace.json` |
| - | プラグイン CLAUDE.md / CLAUDE.jp.md | - `plugins/ref-inject/CLAUDE.md` (+jp) |
| - | create スキル（雛形をコピー&置換して新プラグイン生成） | - `plugins/ref-inject/skills/create/SKILL.md` (+jp) |
| - | templates: inject_references.py 雛形（新設計） | - `plugins/ref-inject/templates/hooks/inject_references.py` |
| - | templates: PreCompact リフレッシュスクリプト雛形 | - `plugins/ref-inject/templates/hooks/refresh_on_compact.py` |
| - | templates: hooks.json 雛形（PreToolUse + PreCompact） | - `plugins/ref-inject/templates/hooks/hooks.json` |
| - | templates: injection.md.j2 + jp 雛形 | - `plugins/ref-inject/templates/hooks/templates/injection.md.j2` (+jp) |
| - | templates: references 雛形（index.yaml+jp / injection_rules.yaml / CLAUDE.md+jp） | - `plugins/ref-inject/templates/references/*` |
| - | templates: 生成先 plugin.json / CLAUDE.md(+jp) 雛形 | - `plugins/ref-inject/templates/plugin.json` 他 |
| - | ルール・CLAUDE.md を整備する | - ルートCLAUDE.md / glossary / kit-hooks-index-sync 等 |

## 参考ドキュメント

- `.work/notes/ref-inject-generator.md`: 設計メモ（注入設計の決定事項・トークン構造・テンプレ展開方式）

## 次PR候補

| タイトル | 概要 | 実施条件 |
|---|---|---|
| migrate-py-kit-to-ref-inject | py-kit のフック・references 構造を ref-inject 生成形式（新注入設計）に作り直す | 即時実施可 |
| migrate-next-kit-to-ref-inject | next-kit を ref-inject 生成形式に作り直す | 即時実施可 |
| migrate-claude-kit-to-ref-inject | claude-kit（skill-creator 等）を ref-inject 形式に寄せる。要分解検討 | 「migrate-py-kit-to-ref-inject」が完了したら |
