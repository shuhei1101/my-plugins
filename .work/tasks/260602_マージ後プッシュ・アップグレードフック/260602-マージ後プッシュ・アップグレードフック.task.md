# マージ後プッシュ・アップグレードフック

> ブランチ: feat/post-merge-push-hook

## 概要

masterへのマージ完了後、自動的に `git push` と `python tools/marketplace.py upgrade` を実行するClaude Codeフックを追加する。

### 実施条件

即時実施可

## 作業内容

- [x] `.claude/hooks/post-merge-push-upgrade.sh` スクリプトを作成（PostToolUse(Bash) フックから呼び出す）
- [x] `.claude/settings.json` にPostToolUse フックエントリを追加
- [x] QAを記録する
- [x] ノートを更新する

## 変更内容

| No | ファイル | 変更種別 | 概要 |
|---|---|---|---|
| 1 | `.claude/settings.json` | 新規 | PostToolUse(Bash) フックを追加 |
| 2 | `.claude/hooks/post-merge-push-upgrade.sh` | 新規 | マージ検知・push・upgradeスクリプト |

## テスト

| No | ケース | 結果 |
|---|---|---|
| 1 | master上でgit mergeコマンド実行後にgit push + upgradeが動くか | - |
| 2 | master以外のブランチでは発火しないか | - |

## QA

（なし）

## 参考ドキュメント

- `.work/notes/フック・自動化/マージ後プッシュ・アップグレードフック.md`

## 関連ブランチ

（なし）

## 次ブランチ候補

（なし）
