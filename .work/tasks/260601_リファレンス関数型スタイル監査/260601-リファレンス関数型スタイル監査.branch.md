# リファレンス関数型スタイル監査

> ブランチ: `docs/reference-functional-style-audit`

## 概要

dev-kit の全リファレンス（Next.js + Python）が、quest-pay の実コード同様の
**関数型・`type` 優先スタイル**に準拠しているかを精読監査し、非準拠箇所を修正する。

### 背景（前ブランチ `docs/local-yaml-repository-pattern` での経緯）

- ローカル YAML データストアのリファレンスを作成した際、初版は `Repository` / `interface` /
  クラス／ファクトリオブジェクトを使った OOP 寄りのスタイルになっており、ユーザーから却下された。
- ユーザーの方針：**クラス・`interface`・`Repository` 等のクラスチックな命名は禁止**。
  関数ごとに `type` で型を定義（例: `type FetchResource = (args: {...}) => Promise<...>`）。
  データアクセスは `fetch{Feature}` / `insert{Feature}` などの素の関数。引数はオブジェクト。
  参照すべき実コード: `~/repo/quest-pay/packages/web/app/api/*/{query,db,service}.ts`。
- この経緯から「他のリファレンスも同様に関数型で書けているか確認したい」という依頼で本ブランチを予約。

### 予備調査の結果（grep + 高シグナルファイル確認）

- **Next.js**: `next/frontend/conventions/型規約.md` がデフォルト `type`・`interface` はライブラリ拡張のみと明記。
  `class` 使用はエラークラス（`extends AppError`）と Playwright Page Object のみ → 正当。クラスベースの
  Service/Repository は不在。**概ね準拠**。
- **Python**: `python/architecture/TypeScriptスタイル適用.md` が「Function-First」を明文化し、
  Service/Repository/Provider/Validator は関数で書くと規定。`class` ヒットは DTO（`@dataclass`）・
  Pydantic・Enum・例外・Protocol の**アンチパターン例**（「やってはいけないこと」節）のみ → 正当。**概ね準拠**。

→ 予備調査では大きな違反は未検出。本ブランチでは全ファイル精読で取りこぼしを確認する。

### 監査結果（2026-06-01・全文精読）

3つの監査サブエージェントで領域分担し、英語版 `.md` を全文精読（Next.js 約89ファイル + Python 約42ファイル、計131ファイル）。
横断 grep（`class` / `interface` / `extends` / `implements` / `Repository|Manager|Service|Provider` 命名）で裏取り。

- **真の違反: 0 件**。dev-kit リファレンスは function-first / `type` 優先スタイルで完全に統一されている。
- 検出された `class` / `interface` / `extends` はすべて許容例外、または意図的アンチパターン例:
  - エラークラス（`AppError` とサブクラス）
  - Playwright Page Object（`CartPage` / `LoginPage` / `ResourceListPage`）
  - ライブラリ宣言マージ（`interface CustomTheme extends DefaultTheme`）
  - ジェネリック型制約（`<T extends z.ZodObject>` / `<TForm extends FieldValues>`）
  - 「やってはいけないこと」節の ❌ 例（`class AbstractUserService(Protocol)` ＝過剰設計の悪例）

→ 修正・イシュー化は不要。基準と準拠状況をノートに記録して完了。

### 実施条件

即時実施可

---

## 作業内容

| # | 作業 | 状態 |
|---|---|---|
| 1 | QA を記録する | [x] |
| 2 | Next.js リファレンス（`next/` 配下 ~90 ファイル）を精読し、関数型・`type` 優先違反を洗い出す | [x] |
| 3 | Python リファレンス（`python/` 配下 ~47 ファイル）を精読し、Function-First 違反を洗い出す | [x] |
| 4 | 違反があればイシュー化し、本ブランチで修正（軽微なら直接修正） | [x]（違反0件・修正不要） |
| 5 | ノートを更新する | [x] |

---

## 変更内容

ソースコード（リファレンス本体）の修正はなし（監査の結果、違反0件のため）。

| # | ファイル | 変更概要 |
|---|---|---|
| 1 | `.work/notes/コーディング規約・スタイル/dev-kitリファレンス関数型スタイル規約.md` | 新規。基準ドキュメント・許容例外・監査状況を記録 |
| 2 | `.work/notes/_index.md` | 上記ノートをコーディング規約・スタイルに追加 |

---

## テスト

| # | 確認項目 | 状態 |
|---|---|---|
| 1 | Next.js リファレンス全文精読（backend 29 + frontend/shared/他 60 = 89ファイル） | [x] 違反0 |
| 2 | Python リファレンス全文精読（42ファイル） | [x] 違反0 |
| 3 | 横断 grep で `class`/`interface`/`extends`/`implements`/クラスチック命名を裏取り | [x] 全ヒット許容例外と確認 |

---

## QA

| # | 質問 | 状態 |
|---|---|---|
| — | — | — |

---

## 参考ドキュメント

- `.work/notes/コーディング規約・スタイル/dev-kitリファレンス関数型スタイル規約.md`

---

## 関連ブランチ

docs/local-yaml-repository-pattern（前ブランチ・本監査の発端）

## 次ブランチ候補

| ブランチ名 | 概要 | 実施条件 |
|---|---|---|
| — | — | — |
