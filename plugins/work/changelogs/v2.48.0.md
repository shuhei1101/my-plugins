# v2.48.0 — 2026-05-31

## 変更内容

- `work:config` から `NEXT_KIT_TS_CHECK`（dev-kit merge 後の旧名）を削除。`DEV_KIT_NEXT_TS_CHECK` は `/dev-kit:config` で管理。
- `CLAUDE_KIT_INJECTION_DISABLE` / `DEV_KIT_INJECTION_DISABLE` を削除。`plugin-config.md` のポリシーに従い、逆極性キルスイッチは config スキルの管理対象外。
- 逆極性変数が全廃されたため、Step 1/3/4 の逆極性分岐ロジックを削除。
- Step 2 を `AskUserQuestion` 方式から番号付きプレーンテキストリスト方式に移行（`plugin-config.md` ガイドに準拠）。

## 変更ファイル

| ファイル | 内容 |
|---|---|
| `skills/config/SKILL.md` (+ `.jp.md`) | 不正エントリ削除・逆極性ロジック削除・Step 2 リスト方式へ移行 |
