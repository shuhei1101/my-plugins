# v2.46.1 — 2026-05-30

## 変更内容

- **git-guard: `git merge master/main` を許可 (PR195)**:
  - `git merge master` / `git merge main` / `git merge origin/master` / `git merge origin/main` はブロック対象から除外
  - 現在のブランチに上流ブランチを取り込む操作は安全なためガード不要
  - 引き続きブロック対象: `git push`、master/main 以外のブランチへの `git merge`
