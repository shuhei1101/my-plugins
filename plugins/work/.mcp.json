{
  "mcpServers": {
    "work-tools": {
      "command": "uv",
      "args": [
        "run",
        "--with", "mcp",
        "--with", "pyyaml",
        "python",
        "${CLAUDE_PLUGIN_ROOT}/mcp/server.py"
      ]
    }
  }
}
