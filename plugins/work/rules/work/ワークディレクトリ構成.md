---
paths:
  - "**/.work/**"
---

# .work/ ディレクトリガイド

## ディレクトリ構成

```txt
.work/
└── notes/
    ├── _index.md            # ノート索引
    └── {カテゴリ}/
        └── {topic}.md       # スナップショット仕様書
```

タスク管理（旧 `tasks/`）とイシュー管理（旧 `issues/`）は廃止された。GitHub Issues / Pull Request が真実のソース（gh プラグイン経由で扱う）。
