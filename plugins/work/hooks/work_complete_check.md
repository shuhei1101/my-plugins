[work] 応答を完了する前に確認すること:

- 以下 4 項目がすべて揃っていれば、ユーザーに `/work:merge` の実行を提案する（勝手に実行しないこと）
  1. 現在のセッションで対応しているブランチのドキュメントを更新し、`## 作業内容` テーブルで完了した行の `完了` 列を `済` にしている
  2. 同タスクドキュメントの `## QA` セクションに未回答（保留中）のエントリが残っていない
  3. 今回の作業内容が `.work/notes/` 配下のノートに反映・書き起こされている
  4. 作業内容に応じたテストの作成、実行が完了している
