[work] Before finishing this response:

1. Update the branch document for the branch you are working on in this session — mark completed rows in the `## 作業内容` table with `済` in the `完了` column
2. Confirm that the `## QA` section of the same branch document has no unresolved (pending) entries
3. Confirm that the work done in this session is reflected and documented in the relevant note under `.work/notes/`
4. If all three conditions above are met, **suggest** that the user run `/work:merge` to merge branch **{branch-name}** (include the actual branch name) — do nothing more
