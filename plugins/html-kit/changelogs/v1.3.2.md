# v1.3.2 — 2026-05-24

## 変更内容

- **debug-fab スキルに mark-generated 呼び出しを必須化**: `templates/` 配下のテンプレートを生成・コピーする際、`/claude-kit:mark-generated` を呼んで出自メタデータコメントをファイル冒頭に挿入することを必須化（Markdown ファイルのみ）。
- **templates/ の Markdown ファイルに出自メタデータを付与**: `plugins/ui-kit/skills/debug-fab/templates/CLAUDE.md` および `CLAUDE.jp.md`、`plugins/ui-kit/templates/rules/*.md` の冒頭に出自メタデータコメントを付与。

## 構造の変更

- `plugins/ui-kit/changelogs/` を新規追加

## 注記

- HTML / JS / CSS のテンプレファイル（`uidev.js`, `uidev.css`, `example.html`, `mock-skeleton.html`）は今回の対象外。将来 `mark-generated` がこれらの記法に対応した時点で別 PR にて追補する。
