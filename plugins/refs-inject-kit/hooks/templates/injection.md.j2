<!-- Generated by refs-inject-kit/inject-references-hook v1.1.0 -->
# References — auto-injected

You are about to edit `{{ file_path }}`.
The `refs-inject-kit` hook matched this path against `injection_rules.yaml`.
Read and follow the required references below **before** editing this file.

{% if required %}
---

## Required references

{% for ref in required %}
### `{{ ref.raw }}`

> {{ ref.description }}

{{ ref.body }}

{% if not loop.last %}---{% endif %}
{% endfor %}
{% endif %}
{% if optional %}

---

## Optional references (use `Read` if needed)

{% for ref in optional %}
- `{{ ref.raw }}` — {{ ref.description }}
{% endfor %}
{% endif %}

---

**Summary**:
- Target file: `{{ file_path }}`
- Required ({{ required | length }}): {% for ref in required %}`{{ ref.raw }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% if optional %}
- Optional ({{ optional | length }}): {% for ref in optional %}`{{ ref.raw }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}

After reading the required references, retry your `Edit` / `Write`. The hook will not block the same file again in this session.
