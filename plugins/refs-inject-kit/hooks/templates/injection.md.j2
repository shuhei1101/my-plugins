<!-- Generated by refs-inject-kit/inject-references-hook v1.0.0 -->
# References — auto-injected

You are about to edit `{{ file_path }}`.
The `refs-inject-kit` hook matched this path against installed plugins' `references/injection_rules.yaml`.
Read and follow the required references below **before** editing this file.

{% for plugin in plugins %}
---

## From plugin: `{{ plugin.plugin }}`

{% if plugin.required_data %}
### Required references

{% for ref in plugin.required_data %}
#### `{{ plugin.plugin }} / references/{{ ref.path }}`

> {{ ref.description }}

{{ ref.body }}

{% if not loop.last %}---{% endif %}
{% endfor %}
{% endif %}
{% if plugin.optional_data %}

### Optional references (use `Read` if needed)

{% for ref in plugin.optional_data %}
- `{{ plugin.plugin }} / references/{{ ref.path }}` — {{ ref.description }}
{% endfor %}
{% endif %}
{% endfor %}

---

**Summary**:
- Target file: `{{ file_path }}`
{% for plugin in plugins %}
- `{{ plugin.plugin }}`: required ({{ plugin.required_data | length }}){% if plugin.optional_data %}, optional ({{ plugin.optional_data | length }}){% endif %}
{% endfor %}

After reading the required references, retry your `Edit` / `Write`. The hook will not block the same file again in this session.
