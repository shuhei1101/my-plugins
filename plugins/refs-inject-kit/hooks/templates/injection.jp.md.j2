<!-- Generated by refs-inject-kit/inject-references-hook v1.0.0 -->
<!-- This file is a Japanese mirror. When updating the English original, update this file too. -->
# References — 自動注入

これから `{{ file_path }}` を編集しようとしています。
`refs-inject-kit` フックがインストール済みプラグインの `references/injection_rules.yaml` と
照合した結果、以下の reference を注入します。**編集前に必ず読んでください**。

{% for plugin in plugins %}
---

## プラグイン: `{{ plugin.plugin }}`

{% if plugin.required_data %}
### 必読 reference

{% for ref in plugin.required_data %}
#### `{{ plugin.plugin }} / references/{{ ref.path }}`

> {{ ref.description }}

{{ ref.body }}

{% if not loop.last %}---{% endif %}
{% endfor %}
{% endif %}
{% if plugin.optional_data %}

### 参考 reference (必要に応じて `Read` してください)

{% for ref in plugin.optional_data %}
- `{{ plugin.plugin }} / references/{{ ref.path }}` — {{ ref.description }}
{% endfor %}
{% endif %}
{% endfor %}

---

**サマリ**:
- 編集対象: `{{ file_path }}`
{% for plugin in plugins %}
- `{{ plugin.plugin }}`: 必読 ({{ plugin.required_data | length }} 件){% if plugin.optional_data %}、参考 ({{ plugin.optional_data | length }} 件){% endif %}
{% endfor %}

必読 reference を読み込み終わったら `Edit` / `Write` を再試行してください。
同一ファイルへの 2 回目以降の編集は、このセッション中はフックでブロックされません。
