<!-- Generated by refs-inject-kit/inject-references-hook v1.1.0 -->
<!-- This file is a Japanese mirror. When updating the English original, update this file too. -->
# References — 自動注入

これから `{{ file_path }}` を編集しようとしています。
`refs-inject-kit` フックが `injection_rules.yaml` の pattern と照合した結果、
以下の reference を注入します。**編集前に必ず読んでください**。

{% if required %}
---

## 必読 reference

{% for ref in required %}
### `{{ ref.raw }}`

> {{ ref.description }}

{{ ref.body }}

{% if not loop.last %}---{% endif %}
{% endfor %}
{% endif %}
{% if optional %}

---

## 参考 reference（必要に応じて `Read` してください）

{% for ref in optional %}
- `{{ ref.raw }}` — {{ ref.description }}
{% endfor %}
{% endif %}

---

**サマリ**:
- 編集対象: `{{ file_path }}`
- 必読 ({{ required | length }} 件): {% for ref in required %}`{{ ref.raw }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% if optional %}
- 参考 ({{ optional | length }} 件): {% for ref in optional %}`{{ ref.raw }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}

必読 reference を読み込み終わったら `Edit` / `Write` を再試行してください。
同一ファイルへの 2 回目以降の編集は、このセッション中はフックでブロックされません。
