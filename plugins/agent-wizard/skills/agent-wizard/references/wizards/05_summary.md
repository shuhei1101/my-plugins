# フェーズF: 完了

---

## ステップ15(サマリー出力)

**条件**: 新規作成完了（ステップ8）、編集完了（ステップ10）、またはユーザーがセッション終了を選択した場合

**入力**: セッション中に記録した `~/.claude/skill-memory/agent-wizard/` 内のメモリファイル

**AI実施タスク**:
- セッション中に行った作業を `~/.claude/skill-memory/agent-wizard/` のメモリファイルから集約する
- サマリーを生成する:
  - 作成/編集したエージェントの一覧（名前・パス・概要）
  - 変更内容の要約
  - 会話ログがあればファイルパスを記載
- サマリーを `~/.claude/skill-memory/agent-wizard/{YYYYMMDDHHMMSS}_session_summary.md` に保存する

**出力**:
- セッションサマリー:
  ```
  📝 Agent Wizard セッションサマリー

  ## 作成したエージェント
  | 名前 | スコープ | パス |
  |---|---|---|
  | my-agent | プラグイン(idea) | agents/ai-dev/my-agent.md |

  ## 実施した操作
  - エージェント新規作成: 1体
  - SKILL.md一覧テーブル更新: 1件

  ## 関連ファイル
  - セッションログ: ~/.claude/skill-memory/agent-wizard/...
  ```

**選択肢**:
  1. 完了（セッション終了）
  2. 続けて別のエージェントを作成する → ステップ2へ
  3. 作成したエージェントと会話してみる → ステップ13へ
  4. その他（入力してください）

**補足**:
- このステップはセッションの区切りを明確にする役割。必ず経由する必要はなく、ユーザーが途中で離脱しても問題ない
- メモリファイルが見つからない場合は、会話コンテキストから作業内容を再構成する
