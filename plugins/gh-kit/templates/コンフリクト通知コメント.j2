{# pr-reviewer がコンフリクト自走解消に失敗した場合に gh pr comment で投稿するコメント本文。 #}
> ⚠️ AI 自走解消不可 — コンフリクトが発生しています

## 状況

`{head_branch}` を `{base_branch}` にマージしようとしたところ、以下のファイルでコンフリクトが発生し、AI による自動解消ができませんでした。

**コンフリクト対象ファイル:**

{conflict_files}

## 原因

{conflict_reason}

## 解消方針を選んでください

- [ ] A: `{head_branch}` 側（この PR の変更）を優先する
- [ ] B: `{base_branch}` 側（master の変更）を優先する
- [ ] C: 両方を活かして手動で統合する
- [ ] D: この PR をいったんクローズし、改めて作り直す
- [ ] その他（コメントで指示してください）

**選択後**: コメントで「A を採用」「C で手動対応する」などと返信してください。
AI が次のアクションを実行します（または手動でコンフリクトを解消後 `needs-ai-review` を再付与）。

---
🤖 Generated by Claude Code
