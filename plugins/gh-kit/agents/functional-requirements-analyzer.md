---
name: gh-kit:functional-requirements-analyzer
description: Issue の対象領域について、既存の結合設計（バック / フロント / シナリオ）を横断調査し、機能要件を書くための観点リスト（重複エンドポイント / 影響画面 / エラーハンドリング候補 / バリデーション候補 / 状態表示候補）を返すエージェント。issue-spec の要件観点調査で使う。
---

# functional-requirements-analyzer

指定された Issue の機能要件を書く際に必要な観点を、注入された Wiki（結合設計）から横断調査して返す。
**要件そのものを決めず、要件表を書くための材料（観点）を提示する**（決定は呼び出し元がユーザー対話で行う）。

## 入力

| 引数              | 内容                                                                                       | 例                                                                                                    |
| ----------------- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- |
| Issue 番号        | 起点となる Issue                                                                           | 42                                                                                                    |
| 対象領域          | Issue が言及する業務ドメイン / 画面 / エンドポイント（複数可）                             | `["ユーザー", "認証", "プロフィール編集画面"]`                                                        |
| 注入 Wiki パス    | 該当領域の関連ファイル一覧（結合 バック/フロント の README + 詳細 / シナリオ README） | `["設計図/バックエンド結合/README.md", "設計図/バックエンド結合/ユーザー詳細取得.md", ...]` |

## ステップ 1: 起点 Issue 本文を取得

```bash
gh issue view {N} --json title,body,labels
```

タイトル・本文・`## 概要` / `## 背景` / `## 現状` を読み、要件の輪郭を把握する。

## ステップ 2: 注入 Wiki を横断走査

入力の「注入 Wiki パス」全ファイルを Read し、以下の観点で情報を抽出する。

### 2a: 既存エンドポイント / 画面との重複調査

`設計図/バックエンド結合/README.md` / `設計図/フロントエンド結合/README.md` から、Issue が言及する領域と **同名 or 類似のエンドポイント / 画面** を洗い出す。

- 重複していれば「既存 API / 画面の拡張で対応」か「新規追加」の判断材料として提示
- 既存があるなら詳細ファイル（`{論理名}.md`）まで Read してメイン フロー・条件分岐・異常系を確認

### 2b: 既存フローとの影響範囲調査

該当領域の詳細ファイルを Read し、Issue の変更が影響するフローを洗い出す。

- Issue の要求により変更されるメインフロー / 条件分岐
- 追加が必要な条件分岐 / 異常系
- 他画面 / 他エンドポイントへの波及

## ステップ 3: 機能要件観点の観点リスト作成

以下のカテゴリ別に **要件文ではなく観点** を列挙する。

| カテゴリ         | 観点の例                           |
| ---------------- | ---------------------------------- |
| 編集機能         | どのフィールドを編集可能にするか   |
| 〃               | 楽観的ロックの要否                 |
| 閲覧機能         | 一覧のフィルタの有無               |
| 〃               | 一覧のソートの有無                 |
| 〃               | ページネーションの有無             |
| バリデーション   | 各入力項目の必須指定               |
| 〃               | 各入力項目の上限文字数             |
| 〃               | 各入力項目のフォーマット制約       |
| エラー表示       | 業務エラー時のメッセージ・トースト |
| 〃               | インラインエラー                   |
| 状態表示         | ローディング                       |
| 〃               | 空状態                             |
| 〃               | 成功後の遷移                       |
| レスポンシブ対応 | モバイル対応の要否                 |
| 〃               | ブレイクポイント                   |
| 既存機能への影響 | 既存 API / 画面への波及点          |

