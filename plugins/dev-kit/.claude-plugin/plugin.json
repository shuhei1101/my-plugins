{
  "name": "dev-kit",
  "description": "Development conventions toolkit — Python (py-script/py-project), HTML/CSS/JS (FLOCSS, debug-fab, logging, mock), Next.js 16 App Router (implement/plan), and Markdown. Rule auto-injection via PreToolUse(Edit/Write/Read) in hooks/rules/inject_rules.py; each rule .md declares its own triggers via YAML frontmatter (paths/required), cached in cache.yaml. PostToolUse tsc check gated by DEV_KIT_NEXT_TS_CHECK. v4.18.0: rename folder to hooks/rules/, inject_rules.py, drop tools key (fires on Edit/Write/Read).",
  "version": "4.18.0"
}