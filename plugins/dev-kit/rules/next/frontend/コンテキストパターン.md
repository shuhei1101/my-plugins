---
paths:
  - "**/app/**/*.{ts,tsx}"
---

# React Context パターン

使い所: 複数ルート / コンポーネントで共有する UI state（prop drilling 3+ レベル）

## ルール

- 1 Context = 1 責務
- Provider: `app/(shared)/providers/`
- カスタム hook（`use{Name}`）経由でアクセス（`useContext` 直呼び禁止）
- 頻繁更新・selector 必要なら Zustand に切り替え
