---
paths:
  - "**/integrations/llm/**/*.py"
---

# トークン管理とコスト最適化

- 最適化前にまず計測: 呼び出しごとに provider / model / input_tokens / output_tokens を構造化ログ → JSON Lines 集計で日次コスト

## max_tokens

無制限だと冗長な前置きで使い切るので用途に応じて絞る:

| 用途         | 目安      |
| ------------ | --------- |
| Yes/No 判定  | 50        |
| 短い分類タグ | 100       |
| 1 段落の要約 | 300       |
| 構造化抽出   | 800〜1024 |
| 長文生成     | 2048+     |

## プロンプトキャッシュの設計（最重要）

キャッシュは「先頭から共通したプレフィックス」だけに効く。固定値は上、動的値は下:

| 位置       | 内容                                                 |
| ---------- | ---------------------------------------------------- |
| 上（先頭） | 不変のキャラ設定 / 役割 / 出力スキーマ / few-shot 例 |
| 中         | セッション単位で固定（要約・ペルソナ）               |
| 下（末尾） | リクエストごとに変わる値（直近履歴・ユーザー入力）   |

- system に timestamp 等の動的値を埋め込まない（1 文字の差で以降全部ミス）
- messages は古い履歴ほど上、順序を入れ替えない

| Provider  | 仕組み                                                            | 条件                                                                             |
| --------- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| Anthropic | `cache_control: {"type": "ephemeral"}` を明示（読み出し単価 10%） | 1024 トークン以上（Haiku は 2048）、5 分 TTL（`ttl: "1h"` も可）、4 ブロックまで |
| OpenAI    | 自動（同一プレフィックスで 50% 引き、コード変更不要）             | 先頭 1024 トークン以上一致、128 トークン単位、5〜10 分で失効                     |

- 1 回しか呼ばない用途では効かない（Anthropic は cache creation 25% プレミアムで割高）
- 使用量は `cache_read_input_tokens`（Anthropic）/ `prompt_tokens_details.cached_tokens`（OpenAI）をログ

## その他の定石

- モデル選定: 最小モデルで通せないか試してから上げる（分類 → Haiku / mini、複雑な推論 → 上位モデル）
- Batch API: リアルタイム不要な大量処理は 50% 引き（24h 以内処理）
- 履歴の刈り取り: 直近 N 件 + 古い履歴は要約（要約は小さいモデルで）
- ストリーミングは UX 改善でありコスト削減ではない
