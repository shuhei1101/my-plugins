# dev-kit v2.1.0

## Added

- Python ファイル編集時フック (`python-skill-dispatch`): `.py` ファイルを Edit/Write するとき、セッション初回のみ py-project と py-script 両スキルの概要を Claude に注入して適切なスキルを選ばせる
- YAML ファイル編集時フック (`yaml-skill-dispatch`): `.yaml`/`.yml` ファイルを Edit/Write するとき、セッション初回のみ dev-kit:yaml スキルを案内する
