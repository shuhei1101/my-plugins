# v4.10.0

## 変更概要

`markdown_frontmatter_check.py` フックを削除。

Markdown フロントマター配置ルールは `_injection_rules.yaml` の `**/*.md` パターンで
`references/markdown/markdown-editing.md` を必須注入済みであり、Python スクリプトによる
重複チェックは不要と判断した（#228）。

## 変更ファイル

| ファイル | 内容 |
|---|---|
| `hooks/scripts/markdown_frontmatter_check.py` | 削除 |
| `hooks/hooks.json` | Edit/Write/MultiEdit から `markdown_frontmatter_check` エントリ削除 |
| `CLAUDE.md` | `DEV_KIT_MARKDOWN_CHECK` トグル行・フック一覧行を削除 |
| `.claude-plugin/plugin.json` | 4.9.0 → 4.10.0 |
