# dev-kit v4.1.0 — 2026-05-30

## 変更内容

- **`references/` 配下のメタ系 YAML をアンダースコア接頭辞付きにリネーム**: `index.yaml` / `index.jp.yaml` / `injection_rules.yaml` を `_index.yaml` / `_index.jp.yaml` / `_injection_rules.yaml` にリネーム。`python/` / `html/` / `next/` の実コンテンツファイルと視覚的に分離され、references 一覧の最上部に固定表示される。
- `hooks/inject_references.py` のファイル名参照と Jinja2 テンプレート (`injection.{md,jp.md}.j2`) のメッセージを追従。

## 変更ファイル

- `references/_index.yaml` (旧 `index.yaml`)
- `references/_index.jp.yaml` (旧 `index.jp.yaml`)
- `references/_injection_rules.yaml` (旧 `injection_rules.yaml`)
- `hooks/inject_references.py` — 参照ファイル名を更新
- `hooks/templates/injection.{md,jp.md}.j2` — メッセージ内の参照名を更新
- `CLAUDE.md` (+ `.jp.md`) — references ツリーの記述を更新
