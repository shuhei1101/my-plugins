# HTML — dev-kit 共通リファレンス(日本語ミラー)

> このファイルは `html.md` の日本語ミラーです。Claude Code には読み込まれません。
> 変更する場合は JP ミラーを先に更新し、その後 `html.md` にも反映してください。

HTML 固有のマークアップ・構造・セマンティクスに関する規約。

---

> **TODO**: 今後の PR で内容を追記する。
>
> 予定トピック:
> - ドキュメント構造(doctype・lang・meta viewport)
> - セマンティック要素(header/nav/main/section/article/aside/footer)
> - フォーム入力の規約(label・autocomplete・バリデーション属性)
> - アクセシビリティのベースライン(ARIA ロール・フォーカス順)
> - `id` と `data-*` 属性の命名規約
> - インライン vs 外部 CSS/JS の配置
