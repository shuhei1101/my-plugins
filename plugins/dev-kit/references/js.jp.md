# JavaScript — dev-kit 共通リファレンス(日本語ミラー)

> このファイルは `js.md` の日本語ミラーです。Claude Code には読み込まれません。
> 変更する場合は JP ミラーを先に更新し、その後 `js.md` にも反映してください。

JavaScript 固有のクライアントサイド/Node.js コード規約。
ロギングについては `common.md` の「ログ規約」セクションを参照(JS にもそのまま適用される)。

---

> **TODO**: 今後の PR で内容を追記する。
>
> 予定トピック:
> - モジュールシステム(ESM vs CommonJS)
> - 命名規約(camelCase・PascalCase・SCREAMING_SNAKE_CASE)
> - 非同期パターン(`async/await`、未待機 Promise 禁止)
> - エラーハンドリング(型付きエラー、エラー境界)
> - DOM アクセスパターン(イベント委譲、`querySelector` vs フレームワーク)
> - 型注釈(JSDoc / TS 移行路線)
> - バンドラ / ビルドツール規約
