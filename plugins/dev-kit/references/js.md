# JavaScript — dev-kit Shared Reference

JavaScript-specific conventions for client-side and Node.js code.
For logging, see `common.md` (Logging section) — applies to JS as well.

---

> **TODO**: Content to be added in future PRs.
>
> Planned topics:
> - Module system (ESM vs CommonJS)
> - Naming conventions (camelCase, PascalCase, SCREAMING_SNAKE_CASE)
> - Async patterns (`async/await`, no floating promises)
> - Error handling (typed errors, error boundaries)
> - DOM access patterns (event delegation, `querySelector` vs framework)
> - Type annotations (JSDoc / TS migration path)
> - Bundler/build tool conventions
