---
paths:
  - "**/shared/settings.py"
  - "**/config/settings.yaml*"
---
<!-- This file is a Japanese mirror of 設定.md. When updating the English original, update this file too. -->
# settings — pydantic-settings による設定管理

`.env` ファイル / 環境変数を `pydantic-settings` で読み込んで型安全な `Settings` オブジェクトにする。

---

## 標準実装

```python
# src/{pkg}/shared/settings.py
from __future__ import annotations
from pathlib import Path
from pydantic import Field, SecretStr
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    """環境変数 + .env を読み込む設定。"""

    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        env_nested_delimiter="__",
        extra="ignore",
    )

    # ----- 一般 -----
    env: str = Field(default="local", description="実行環境（local / staging / production）")
    log_level: str = Field(default="INFO", description="logger の最低レベル")

    # ----- OpenAI -----
    openai_api_key: SecretStr = Field(description="OpenAI API キー（必須）")
    openai_model: str = Field(default="gpt-4o-mini", description="既定モデル")

    # ----- Anthropic -----
    anthropic_api_key: SecretStr = Field(description="Anthropic API キー（必須）")
    anthropic_model: str = Field(default="claude-haiku-4-5-20251001", description="既定モデル")

    # ----- パス（計算済み・env からは読まない） -----
    project_root: Path = Field(default_factory=lambda: Path(__file__).resolve().parents[2])

    @property
    def log_dir(self) -> Path:
        return self.project_root / "log"
```

ポイント:
- `BaseSettings` 継承（普通の Pydantic `BaseModel` ではない）
- `model_config = SettingsConfigDict(...)` で `.env` 読み込み有効化
- API キーは `SecretStr` でラップ（ログに出ない）
- `extra="ignore"` で未知の env を無視（CI などで関係ない env を汚す問題を避ける）

---

## `.env.sample` を必ず置く

`.env` 自体は `.gitignore` に入れる（秘匿）。
**`.env.sample`** を git 管理して、必要な env のテンプレートにする。

```bash
# .env.sample
ENV=local
LOG_LEVEL=INFO

OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
```

`.gitignore`:

```
.env
```

新規開発者は `.env.sample` をコピーして `.env` にし、値を埋める。

---

## ネストした設定

複雑なら `BaseModel` を入れ子にして `env_nested_delimiter` で読む:

```python
class DatabaseSettings(BaseModel):
    host: str = "localhost"
    port: int = 5432
    user: str
    password: SecretStr

class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_nested_delimiter="__")
    db: DatabaseSettings
```

`.env`:

```
DB__HOST=db.example.com
DB__PORT=5432
DB__USER=admin
DB__PASSWORD=secret
```

---

## SecretStr の使い方

```python
api_key: SecretStr = Field(description="...")
```

ログ出力で安全:

```python
print(settings.openai_api_key)   # SecretStr('**********')
print(settings.openai_api_key.get_secret_value())   # 実際の値（明示的に取り出す時のみ）
```

外部 API クライアントに渡すときだけ `get_secret_value()` を呼ぶ。

---

## main.py からの使い方

```python
# src/{pkg}/main.py
from {pkg}.shared.settings import Settings
from {pkg}.shared.logger import get_logger
import logging

def main() -> None:
    settings = Settings()  # .env + 環境変数を自動読み込み
    logging.getLogger().setLevel(settings.log_level.upper())

    logger = get_logger(__name__)
    logger.info("starting", extra={"env": settings.env})

    handlers = build_handlers(settings)
    ...
```

`Settings()` を **1 度だけインスタンス化** して、必要な関数に partial で埋める:

```python
chat = partial(
    chat_with_openai,
    api_key=settings.openai_api_key.get_secret_value(),
    model=settings.openai_model,
)
```

---

## バリデーション

Pydantic の検証を活用:

```python
from pydantic import field_validator

class Settings(BaseSettings):
    log_level: str = "INFO"

    @field_validator("log_level")
    @classmethod
    def validate_log_level(cls, v: str) -> str:
        valid = {"DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"}
        if v.upper() not in valid:
            raise ValueError(f"log_level must be one of {valid}")
        return v.upper()
```

---

## 設定ファイル（YAML / TOML）も読みたい場合

`Settings` 内で `model_post_init` を使って読み込む:

```python
import yaml
from pathlib import Path

class Settings(BaseSettings):
    config_path: Path = Path("config.yaml")
    prompts: dict[str, str] = {}

    def model_post_init(self, _: object) -> None:
        if self.config_path.exists():
            data = yaml.safe_load(self.config_path.read_text(encoding="utf-8"))
            self.prompts = data.get("prompts", {})
```

ただし、**API キーや動作切替フラグ等の単純な値**は `.env` で十分。
YAML を使うのは「プロンプト定義」「構造化された設定」「リスト的なもの」など。

---

## やってはいけないこと

```python
# ❌ 直接 os.environ を読む
import os
api_key = os.environ["OPENAI_API_KEY"]   # 型チェックも検証もない

# ❌ Settings を関数の中で何度もインスタンス化
def call_llm():
    settings = Settings()   # ← 起動コストがかかる、毎回 .env を読む

# ❌ SecretStr を生文字列のまま logger.info に渡す（マスクされず出る）
logger.info(f"key = {settings.openai_api_key.get_secret_value()}")
```

---

## 関連ファイル

- `shared/constants.py` — 計算済み定数（PROJECT_ROOT 等）
- `shared/ロガー.md` — log_level の運用
- `architecture/コンポジションルート.md` — Settings を main.py で受ける流れ
