<!-- This file is a Japanese mirror of ロガー.md. When updating the English original, update this file too. -->
# logger — JSON Lines 形式の構造化ログ

> このファイルは `ロガー.md` の日本語ミラーです。

`print` ではなく `logger` を使う。出力フォーマットは **JSON Lines**（1 行 1 JSON）。
集計・grep・dashboard 連携を見据える。

---

## 標準実装

```python
# src/{pkg}/shared/logger.py
from __future__ import annotations
import json
import logging
import sys
from datetime import datetime, timezone


class JsonLinesFormatter(logging.Formatter):
    """構造化ログ（1 行 1 JSON）。"""

    def format(self, record: logging.LogRecord) -> str:
        payload: dict[str, object] = {
            "ts": datetime.now(timezone.utc).isoformat(timespec="milliseconds"),
            "level": record.levelname,
            "name": record.name,
            "msg": record.getMessage(),
        }
        # extra=... で渡されたフィールドを取り込む
        for key, value in record.__dict__.items():
            if key in _LOGRECORD_RESERVED:
                continue
            payload[key] = value
        # 例外情報
        if record.exc_info:
            payload["exc"] = self.formatException(record.exc_info)
        return json.dumps(payload, ensure_ascii=False, default=str)


_LOGRECORD_RESERVED = frozenset({
    "name", "msg", "args", "levelname", "levelno", "pathname", "filename",
    "module", "exc_info", "exc_text", "stack_info", "lineno", "funcName",
    "created", "msecs", "relativeCreated", "thread", "threadName",
    "processName", "process", "message",
})


def _ensure_root_configured() -> None:
    """プロセスで一度だけ実行する root logger 設定。"""
    root = logging.getLogger()
    if getattr(root, "_jsonl_configured", False):
        return
    handler = logging.StreamHandler(sys.stdout)
    handler.setFormatter(JsonLinesFormatter())
    root.addHandler(handler)
    root.setLevel(logging.INFO)
    root._jsonl_configured = True   # type: ignore[attr-defined]


def get_logger(name: str) -> logging.Logger:
    """モジュール名で logger を取得する。`get_logger(__name__)` で使う。"""
    _ensure_root_configured()
    return logging.getLogger(name)
```

---

## 使い方

```python
# 任意のモジュールで
from {pkg}.shared.logger import get_logger

logger = get_logger(__name__)

def create_user(input: CreateUserInput) -> User:
    logger.info("creating user", extra={"name": input.name})
    user = User(...)
    logger.info("user created", extra={"user_id": user.id})
    return user
```

出力例（JSON Lines、1 行表示用に整形済み）:

```json
{"ts":"2026-05-27T10:00:00.123+00:00","level":"INFO","name":"mypkg.users.service","msg":"creating user","name_extra":"alice"}
{"ts":"2026-05-27T10:00:00.234+00:00","level":"INFO","name":"mypkg.users.service","msg":"user created","user_id":"u-123"}
```

---

## ログレベル運用

| レベル | 用途 |
|---|---|
| `debug` | 開発時のみ。トレース情報、引数 dump |
| `info` | 業務イベント。リクエスト受領、ユースケース開始/完了 |
| `warning` | 想定内のエラー（retry で吸収できるもの、無効入力等） |
| `error` | 想定外のエラー。人間が監視・対応すべき |
| `critical` | プロセス継続不能 |

設定で本番は `WARNING` 以上、開発は `DEBUG` まで出すのが標準。

---

## 構造化引数 vs f-string の使い分け

| 用途 | 推奨 |
|---|---|
| ダッシュボード / 集計に使う | **構造化引数**（`extra={...}`） |
| 開発中の一時的なデバッグ出力 | f-string |
| エラーログ（再現性のため詳細を残したい） | 構造化引数 |

```python
# ✅ 構造化（集計・grep しやすい）
logger.info("llm_call", extra={"provider": "openai", "tokens": 1234, "elapsed_ms": 567})

# ✅ f-string（人間が読むデバッグログ）
logger.debug(f"trying provider {provider_name}, attempt {attempt}/{max}")
```

---

## 例外ログ

`logger.exception(...)` を使うと自動で traceback が `exc` フィールドに乗る:

```python
try:
    result = await chat(messages)
except LlmError:
    logger.exception("chat failed", extra={"messages_len": len(messages)})
    raise
```

---

## モジュール名

`get_logger(__name__)` を使う。出力で logger name を見るとどこから出たログか即わかる:

```
"name": "mypkg.features.chat.service"
"name": "mypkg.integrations.llm.openai_client"
```

---

## レベル設定（環境変数経由）

`shared/settings.py` から読んで反映:

```python
# src/{pkg}/main.py
import logging
from {pkg}.shared.settings import Settings
from {pkg}.shared.logger import get_logger

def main() -> None:
    settings = Settings()
    logging.getLogger().setLevel(settings.log_level.upper())  # e.g. "DEBUG"
    ...
```

```python
# src/{pkg}/shared/settings.py
class Settings(BaseSettings):
    log_level: str = "INFO"
```

---

## 出力先

- 開発: stdout に出す（上の標準実装はそうしている）
- Docker / k8s: stdout のまま（コンテナログ）
- Lambda / Cloud Functions: stdout のまま（CloudWatch / Stackdriver が拾う）
- ファイル出力が必要な単発スクリプト: `log/{script_name}-{YYYYMMDD-HHMMSS}.log` に tee する（bat/sh 側で）

詳細は `scripts/launchers-windows.md` / `scripts/ランチャー-Unix.md`。

---

## やってはいけないこと

```python
# ❌ print デバッグ
print(f"user {user.id} created")   # ロガー使う

# ❌ 機密情報をログに出す
logger.info(f"login", extra={"password": password})   # NG

# ❌ ループ内で大量に DEBUG
for item in huge_list:
    logger.debug(f"processing {item}")   # 必要なら DEBUG レベルで運用するが、本番では INFO 以上
```

---

## 関連ファイル

- `shared/settings.py` — log_level 設定
- `core/言語ルール.md` — print 禁止 / メッセージは英語
- `llm/プロバイダー.md` — token usage ログの例
