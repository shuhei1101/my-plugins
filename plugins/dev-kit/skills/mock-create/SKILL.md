---
name: mock-create
description: モック作成スキル
---

# モック作成スキル
- `{プロジェクトルート}/mocks`ディレクトリ配下にhtmlでモックを作成する
  - もし`/mocks`フォルダが存在しない場合、作成する

- 初めてモックを作成するときは`mocks/theme/{a,b,c,d,e}.html`を作成する
- 上記対応後、各デザインを3案用意する
- index.htmlを作成する
- ssh経由で見ることがあるため、fastapiで軽量サーバを立てること（ポートは空いてるポートで）
