---
paths:
  - "**/frontend/**/*.css"
---

# ネスト

- ネイティブ CSS Nesting を使ってよい。
  - ただし文字列連結はできない。
  - `&__title` のような連結は無効なので、要素クラスはフル記述する。

```css
.card {
  &:hover { ... }          /* OK */
  .card__title { ... }     /* OK（連結ではなくフル記述）*/
  /* &__title { ... }      無効。書かない */
}
```

ネストは状態（`&:hover`）・擬似クラス・子孫の入れ子に限る。深くしすぎない。
