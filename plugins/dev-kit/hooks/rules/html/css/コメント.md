---
paths:
  - "**/frontend/**/*.css"
---

# CSS コメント

- 全ての CSS セレクタ（クラス・要素・擬似クラス等）にコメントを書く。
  - コメントはセレクタの直上に置く。
```css
/* ボタン共通: 横並び・中央揃え・ホバー時のトランジション */
.button {
  display: inline-flex;
  ...
}
```
- `:hover` / `:focus` 等の状態セレクタはベースクラスの「続き」なので、用途が自明なら省略可。
- `/* ... */` 形式のみ使用（`//` は CSS の正規コメントではない）。
