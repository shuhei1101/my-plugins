---
paths:
  - "**/components/**/*.{ts,js}"
  - "**/_shared/**/*.{ts,js}"
---

# カスタムエレメント命名

`app-` 接頭辞 + 小文字・ハイフン区切り（`app-sidebar`, `app-toast`）。プロジェクト固有が増えたら別接頭辞を足してよい。

`is` 属性（customized built-in）は使わない。Safari 非対応のため。要素を拡張したい時は autonomous custom element でラップする。
