---
paths:
  - "**/frontend/**/*.html"
---

# ネイティブ要素を第一選択にする

JS で作る前に標準要素で済むか確認する。

| 用途                       | 要素                      |
| -------------------------- | ------------------------- |
| モーダル                   | `<dialog>`                |
| アコーディオン / 開閉      | `<details>` / `<summary>` |
| 日付・色・範囲・ファイル等 | `<input type="...">`      |

標準要素で表現できないインタラクションのときだけ JS を足す。
