---
paths:
  - "**/app/(authenticated)/**/*ListScreen.tsx"
  - "**/app/(shared)/components/EmptyState.tsx"
---

# `<EmptyState>` — 空状態 UI

- `length === 0 && !isFetching` で必ず描画（白画面禁止）
- Props: `message`, `description?`, `action?`
- フィルタあり → 「クリア」action、フィルタなしの空 → 「新規作成」action（action は必須）
