# work-kit v2.39.0 — 2026-05-30

## 変更内容

- **config スキルを追加**: PR164 で導入した 7 つの env トグルを対話的に設定できるスキル `/work-kit:config` を追加。ループ型 2 ターン `AskUserQuestion` パターン（① env 変数選択 → ② 値 + スコープ選択 → 適用 → ループ）で 1 変数ずつ設定。`~/.claude/settings.json`（ユーザー設定）と `.claude/settings.json`（プロジェクト設定）の両方を対象とする。

## 変更ファイル

- `skills/config/SKILL.md` — config スキル定義（新規）
- `skills/config/SKILL.jp.md` — JP ミラー（新規）
