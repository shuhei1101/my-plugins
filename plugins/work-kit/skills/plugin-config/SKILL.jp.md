---
name: plugin-config
description: |
  /work-kit:plugin-config が呼び出されたとき。
  またはユーザーが「プラグイン設定を変えたい」「トグルを設定したい」「env を ON/OFF したい」「plugin config」と言ったときに実行。
---
<!-- Generated by work-kit/work-start v2.38.0 -->
<!-- This file is a Japanese mirror. When updating the English original, update this file too. -->

# work-kit:plugin-config — プラグインのトグル設定

プラグインの動作を表示・切り替えるための UI を提示します。
`~/.claude/settings.json` と `.claude/settings.json` から現在の env 値を読み込み、
現在の状態を表示し、ユーザーが選択した変更を適用します。

---

## 管理対象のトグル

| env 変数 | 説明 | 設定ファイル | 無効化値 |
|---|---|---|---|
| `WORK_KIT_PR_ENFORCEMENT` | UserPromptSubmit work-start 強制注入 | project | `"false"` |
| `WORK_KIT_STOP_REMINDER` | Stop TODO/QA リマインダー注入 | project | `"false"` |
| `WORK_KIT_USE_WORKTREE` | work-start での worktree 作成 | project | `"false"` |
| `WORK_KIT_MERGE_CONV2CLAUDE` | merge Step 4 conversation-to-claude 実行 | project | `"false"` |
| `WORK_KIT_MERGE_AUTO_HANDOFF` | merge Step 12 auto pr-handoff 呼び出し | project | `"false"` |
| `NEXT_KIT_TS_CHECK` | PostToolUse tsc 型チェック | project | `"false"` |
| `AITUBER_NOTIFY` | Stop notify-aituber 通知 | user | `"false"` |

**注記**: すべてのトグルはデフォルト有効（キー不在 = ON）。無効化するにはキーを `"false"` に設定し、再有効化はキーを削除します。

---

## タスク

### ステップ 1: 現在の env 状態を読み込む

#### 条件

- 常に実行 — 最初に実行

#### 処理

以下を実行して両方の設定ファイルを読み込みます：

```bash
cat .claude/settings.json 2>/dev/null || echo '{}'
cat ~/.claude/settings.json 2>/dev/null || echo '{}'
```

管理対象の各トグルについて、適切なファイルの `env` ブロックから状態を判定します：
- キー不在またはキーの値が `("false", "0", "no", "off")` に含まれない → **ON**
- キーの値が `("false", "0", "no", "off")` に含まれる → **OFF**

→ ステップ 2 に進みます

#### 出力

- すべての 7 つの管理対象トグルの状態マップ（ON / OFF）

---

### ステップ 2: 現在の状態を表示

#### 条件

- ステップ 1 完了

#### 処理

質問の前にユーザーに現在の状態を示すため、フォーマット済みテーブルを出力します：

```
## 現在の設定

| env 変数 | 状態 | 設定ファイル |
|---|---|---|
| WORK_KIT_PR_ENFORCEMENT | ON | .claude/settings.json |
| WORK_KIT_STOP_REMINDER | ON | .claude/settings.json |
| ...（以下同様）|
```

→ ステップ 3 に進みます

---

### ステップ 3: 変更を選択 — バッチ 1（work-kit コア）

#### 条件

- ステップ 2 完了

#### 処理

**`AskUserQuestion` ツール**を `multiSelect: true` で呼び出します：

- question: `"変更するトグルを選択してください（選択 = 現在の状態を反転）"`
- header: `"work-kit"`
- options（各オプションのラベルに現在状態を含める）:
  1. label: `"[現在: {state}] WORK_KIT_PR_ENFORCEMENT"`, description: `"UserPromptSubmit work-start 強制注入"`
  2. label: `"[現在: {state}] WORK_KIT_STOP_REMINDER"`, description: `"Stop TODO/QA リマインダー注入"`
  3. label: `"[現在: {state}] WORK_KIT_USE_WORKTREE"`, description: `"work-start での worktree 作成"`
  4. label: `"[現在: {state}] WORK_KIT_MERGE_CONV2CLAUDE"`, description: `"merge Step 4 conversation-to-claude"`

選択されたオプションを記録します。

→ ステップ 4 に進みます

---

### ステップ 4: 変更を選択 — バッチ 2（work-kit merge + その他）

#### 条件

- ステップ 3 完了

#### 処理

**`AskUserQuestion` ツール**を `multiSelect: true` で呼び出します：

- question: `"変更するトグルを選択してください（続き）"`
- header: `"その他"`
- options:
  1. label: `"[現在: {state}] WORK_KIT_MERGE_AUTO_HANDOFF"`, description: `"merge Step 12 auto pr-handoff 呼び出し"`
  2. label: `"[現在: {state}] NEXT_KIT_TS_CHECK"`, description: `"PostToolUse tsc 型チェック（next-kit）"`
  3. label: `"[現在: {state}] AITUBER_NOTIFY"`, description: `"Stop notify-aituber 通知（ユーザー設定）"`

選択されたオプションを記録します。

→ ステップ 5 に進みます

---

### ステップ 5: 変更を適用

#### 条件

- ステップ 4 完了

#### 処理

ステップ 3 またはステップ 4 で選択された各トグルに対して：

1. ターゲットファイルを決定します：
   - `AITUBER_NOTIFY` → `~/.claude/settings.json`
   - その他すべて → `.claude/settings.json`
2. ターゲットファイルから JSON を読み込みます（ファイルがない場合は `{}` を作成）
3. 状態を切り替えます：
   - 現在 **ON** → `env.{KEY}` を `"false"` に設定（無効化）
   - 現在 **OFF** → `env.{KEY}` を削除（再有効化）
4. JSON に `env` オブジェクトが存在することを確認し、2 スペースインデントで書き込みます

いずれのバッチでもトグルが選択されなかった場合は、スキップして「変更なし」と報告します。

→ ステップ 6 に進みます

---

### ステップ 6: 報告

#### 条件

- ステップ 5 完了

#### 処理

サマリーを出力します：

```
## 変更完了

| env 変数 | 変更前 | 変更後 | 設定ファイル |
|---|---|---|---|
| WORK_KIT_STOP_REMINDER | ON | OFF | .claude/settings.json |
```

→ 完了。

---

## 注記

- `settings.json` に `env` ブロックがない場合は新規作成します
- `settings.json` 自体が存在しない場合は `{"env": {}}` を新規作成します
- 本スキルは `{PREFIX}_INJECTION_DISABLE`（ref-inject 系、逆極性）を対象としません。各 kit の prefix が異なるため、直接 settings.json を手動編集してください
