[work-kit] Before finishing this response, update the task checklist:

1. Scan `.work/tasks/` to identify the in-progress PR (`PR{N}/` folder)
2. Read its `TODO.md`
3. Mark tasks completed this session as `- [x]`
4. Commit if implementation work is complete for this session
5. If all items in `## TODO` are now `- [x]`, the PR is ready to merge.
   Ask the user: "All tasks are complete. Ready to merge? Run `/work-kit:merge`."
