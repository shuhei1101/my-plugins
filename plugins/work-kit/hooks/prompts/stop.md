[work-kit] Before finishing this response:

1. Update the active PR's `TODO.md` and mark completed tasks as `- [x]`
2. If all items are `- [x]`, ask the user to run `/work-kit:merge`
   (the merge skill handles updating `index.yaml` to `completed: true` and cleanup)
