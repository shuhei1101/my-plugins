> このファイルは `stop.md` の日本語ミラーです。Claude Code には読み込まれません。
> 変更する場合は JP ミラーを先に更新し、その後 `stop.md` にも反映してください。

[work-kit] この応答を完了する前にタスクチェックリストを更新すること:

1. `docs/tasks/index.yaml` から進行中の PR（`completed: false`）を特定する
2. PR タスクドキュメント（`docs/tasks/**/PR{N}.md`）を読む
3. 今回のセッションで完了したタスクを `- [x]` にチェックする
4. 実装作業がひと区切りついていればコミットする
5. `## 作業内容` のチェックリストが全て `- [x]` になっていればマージの準備が整っている。
   ユーザーに「`/work-kit:merge` でマージできます。実行しますか？」と確認する
