# SKILL.jp.md — py-kit:py-project（日本語ミラー）

> このファイルは `SKILL.md` の日本語ミラーです。Claude Code には読み込まれません。
> 変更する場合は JP ミラーを先に更新し、その後 `SKILL.md` にも反映してください。

---

**スキル名**: py-kit:py-project
**トリガー**: 既存 Python プロジェクトへの作業全般（確認・機能追加・リファクタ・バグ修正）。
「このコード見て」「機能追加して」「リファクタして」「バグ直して」「コードレビューして」など。
新規プロジェクトには使わない → `py-kit:py-new-project` を使う。

---

# py-kit:py-project — 既存 Python プロジェクト対応

既存の Python プロジェクトを py-kit 規約に照らしてレビューし、拡張・修正する。

---

## 作業内容

### ステップ1: 規約を読み込む

共通 Python 規約をすべて読み込む：

```
{plugin_root}/references/python-standards.md
```

タスクの内容によって全セクションを使用する。

→ ステップ2へ

---

### ステップ2: プロジェクト構造を把握する

#### 処理内容

1. プロジェクトのトップレベルディレクトリ一覧を読む。
2. レイヤー構造を特定：DDD を採用しているか？どのレイヤーが存在するか？
3. `pyproject.toml` を読んで依存関係とパッケージ設定を把握する。
4. メインエントリーポイントと既存テストを特定する。
5. 既存の `.claude/rules/` ファイルを確認する。

→ ステップ3へ

---

### ステップ3: 品質チェック

#### 処理内容

1. 命名規則（モジュール・クラス・関数・変数）を規約と照合する。
2. 型ヒントのカバレッジを確認 — 欠けている箇所を報告する。
3. SOLID 違反を確認する：
   - SRP：変更理由が複数あるクラスはないか？
   - OCP：Strategy パターンに置き換えるべき if/elif チェーンはないか？
   - DIP：高レベルクラス内で具象クラスをインスタンス化していないか？
4. DDD 境界違反を確認する：ドメイン層がインフラを直接インポートしていないか？アプリケーション層にドメインロジックが混入していないか？
5. システム境界での Pydantic 使用状況を確認する。
6. 調査結果を報告する。レビューのみの依頼ならここで終了してレポートを提示する。

→ 実装が必要な場合はステップ4へ

---

### ステップ4: 変更を実装する

#### 処理内容

1. タスクを実行する（機能追加・リファクタ・バグ修正）。
2. 参考資料の規約をすべて適用する。
3. 新規・変更コードに型ヒントを全箇所付ける。
4. タスクに必要な範囲を超えた抽象化は加えないが、新規モジュール追加時は拡張性を考慮した設計にする。
5. 依存性はコンストラクタ経由でインジェクトする — クラス本体内で具象クラスをインスタンス化しない。
6. `print()` / ロガー出力は英語のみ。

→ ステップ5へ

---

### ステップ5: ルール照合

#### 処理内容

1. 今回のタスクで以下が変更されたかを確認する：
   - 抽象クラス・Protocol・それらを実装する具象クラス
   - 設定ファイル（`.yaml`・`.toml`・`.env`・`.json`）
   - 設定ファイルを読み込むソースファイル
   - 対応するテストファイルが存在するソースファイル
2. 変更種別に応じてルールを適用する：
   - **クラス構造変更** → 継承ツリー / Protocol 実装クラスへの波及を確認
   - **設定変更** → 対応するソースファイルを確認（またはその逆）
   - **ソース変更** → 対応するテストファイルの更新が必要か確認
3. プロジェクトに `.claude/rules/` がなければ `{plugin_root}/rules/` のテンプレートから今すぐ作成する。

→ ステップ6へ

---

### ステップ6: テストを更新する

#### 処理内容

1. 変更したソースファイルに対応するテストファイルを特定する（ミラーパス：`src/foo/bar.py` → `tests/foo/test_bar.py`）。
2. 変更した動作に対するテストケースを更新または新規作成する。
3. テストは外部 I/O 境界（DB・API・ファイルシステム）のみモック — 個々のメソッドの単体テストは書かない。
4. `pytest` を実行してテストがパスすることを確認する。

→ 完了

---

## 参考資料

`{plugin_root}/references/python-standards.md`：
- SOLID 原則
- DDD（ドメイン駆動設計）
- 拡張性重視の設計
- 命名規則
- 型ヒント
- Pydantic の境界
- テスト方針
- コメントルール
- 言語ルール
