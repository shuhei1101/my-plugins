<!-- Generated by claude-kit/hook-creator v3.24.1 -->
<!-- This file is a Japanese mirror. When updating the English original, update this file too. -->
# py-kit references — 自動注入

これから `{{ file_path }}` を編集しようとしています。
py-kit の注入フックが `references/injection_rules.yaml` の pattern と照合した結果、
このファイルに対する必読 reference を以下に注入します。**編集前に必ず読んでください**。

{% if required %}
---

## 必読 reference

{% for ref in required %}
### `references/{{ ref.path }}`

> {{ ref.description }}

{{ ref.body }}

{% if not loop.last %}---{% endif %}
{% endfor %}
{% endif %}
{% if optional %}

---

## 参考 reference（必要に応じて `Read` してください）

{% for ref in optional %}
- `references/{{ ref.path }}` — {{ ref.description }}
{% endfor %}
{% endif %}

---

**サマリ**:
- 編集対象: `{{ file_path }}`
- 必読 ({{ required | length }} 件): {% for ref in required %}`{{ ref.path }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% if optional %}
- 参考 ({{ optional | length }} 件): {% for ref in optional %}`{{ ref.path }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}

必読 reference を読み込み終わったら `Edit` / `Write` を再試行してください。
同一ファイルへの 2 回目以降の編集は、このセッション中はフックでブロックされません。
