<!-- Generated by claude-kit/hook-creator v3.24.1 -->
# py-kit references — auto-injected

You are about to edit `{{ file_path }}`.
The py-kit injection hook matched this path against `references/index.yaml`.
Read and follow the required references below **before** editing this file.

{% if required %}
---

## Required references (必読)

{% for ref in required %}
### `references/{{ ref.path }}`

> {{ ref.description }}

{{ ref.body }}

{% if not loop.last %}---{% endif %}
{% endfor %}
{% endif %}
{% if optional %}

---

## Optional references (参考 — 必要に応じて `Read` してください)

{% for ref in optional %}
- `references/{{ ref.path }}` — {{ ref.description }}
{% endfor %}
{% endif %}

---

**Summary**:
- Target file: `{{ file_path }}`
- Required ({{ required | length }}): {% for ref in required %}`{{ ref.path }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% if optional %}
- Optional ({{ optional | length }}): {% for ref in optional %}`{{ ref.path }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}

After reading the required references, retry your `Edit` / `Write`. The hook will not block the same file again in this session.
