<!-- Generated by claude-kit/hook-creator v3.24.0 -->
You are about to edit a Python file. The py-kit plugin provides two Python skills — choose based on context:

- **py-kit:py-project** — full Python projects (layered architecture, DI, pyproject.toml, tests)
  Use when working on a structured project or an existing codebase.

- **py-kit:py-script** — simple standalone scripts (single file, no full scaffolding)
  Use when writing a one-off automation or quick script.

Invoke the appropriate skill before proceeding.
