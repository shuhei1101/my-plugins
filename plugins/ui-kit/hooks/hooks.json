{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python",
            "args": [
              "-c",
              "import sys,json,pathlib; d=json.loads(sys.stdin.read()); p=d.get('prompt','').lower(); q=pathlib.Path(sys.argv[1]); sys.stdout.buffer.write(('Read and follow: '+str(q)+'\\n').encode()) if q.exists() and any(k in p for k in ['html','css','javascript',' js ','.js','実装','コンポーネント','ui ','画面','mock','モック','implement','frontend']) else None",
              "${CLAUDE_PLUGIN_ROOT}/hooks/prompts/ui-skill-reminder.md"
            ]
          }
        ]
      }
    ]
  }
}
