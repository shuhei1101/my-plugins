<!-- Generated by claude-kit/skill-creator v3.17.1 -->
<!-- This file is a Japanese mirror. When updating the English original, update this file too. -->
# SKILL.jp.md — ui-kit:debug-fab(日本語ミラー)

> このファイルは `SKILL.md` の日本語ミラーです。Claude Code には読み込まれません。
> 変更する場合は JP ミラーを先に更新し、その後 `SKILL.md` にも反映してください。

---

**スキル名**: ui-kit:debug-fab
**トリガー**: 開発をサポートする画面(管理画面・内部ツール・デバッグ画面など)を作成・編集するとき。
本番ユーザー向けの画面には適用しない。
「管理画面作って」「内部ツール用 UI を追加」「開発用デバッグ画面を直して」など。

---

# ui-kit:debug-fab — フロートデバッグボタン + モーダル

開発をサポートする画面には全てフロートデバッグウィジェット(FAB + モーダル)を設置する。
ウィジェットは画面の関連ファイルと直近 JS ログを収集し、「コピー」ボタンで JSON として
クリップボードへ出力する。そのまま Claude Code に貼ってデバッグできる。

スキルが提供するのは**共通モジュール**(`templates/uidev.css` + `templates/uidev.js`)。
各画面はこの 2 ファイルを 1 回読み込み、画面固有の関連ファイルだけを宣言する。
各画面に同じウィジェットコードをコピペしてはいけない。

---

## 作業内容

### ステップ1: 関連リファレンスを読み込む

参照:

```
{plugin_root}/references/principles.md   # UI 規約(必読 — DRY / CSS / JS / frontend-design)
```

加えてログスキルの規約も:

```
{plugin_root}/skills/logging/SKILL.md    # ログ規約
```

プラグインルートはこのスキルファイルの2階層上。

→ ステップ2へ

---

### ステップ2: 開発系画面であることを確認

#### 処理内容

作業対象が**開発をサポートする画面**(管理画面/内部ツール/デバッグ画面)であることをユーザーに確認する。
本番ユーザー向けの画面なら**本スキルは適用しない**で停止する。

→ ステップ3へ

---

### ステップ3: 共通ウィジェットファイルをプロジェクトに配置

#### 処理内容

1. `{plugin_root}/skills/debug-fab/templates/uidev.css` と `uidev.js` をプロジェクトの静的アセットディレクトリ
   (`static/`、`public/`、`assets/` 等)にコピーする。
2. 既に存在する場合は上書きせず、パスを確認するのみ。
3. ブラウザから読み込むときの URL を確認する。

→ ステップ4へ

#### 補足

- この2ファイルがウィジェットの**唯一の正本**。すべての開発系画面がここから読み込む。
- スキル付属のテンプレートを各画面ごとにインラインに書き換えるのは禁止。プロジェクト固有のカスタマイズが
  必要な場合は、プロジェクト内でフォークすること(スキル側のテンプレートはいじらない)。

---

### ステップ4: 画面にウィジェットローダーを追加

#### 処理内容

1. 画面の HTML `<head>` に以下を追加する:

   ```html
   <link rel="stylesheet" href="/static/uidev.css" />
   <script src="/static/uidev.js" defer></script>
   ```

2. ウィジェットは DOM 準備完了後に自動初期化される。追加の JS 配線は不要。

→ ステップ5へ

---

### ステップ5: 画面に関連ファイルを宣言する

#### 処理内容

以下のどちらか(または両方、マージされる)を選ぶ:

**A. `data-debug-files` 属性(推奨・宣言的)**

```html
<body data-debug-files='{
  "html": ["pages/user_list.html"],
  "css":  ["styles/user_list.css"],
  "js":   ["scripts/user_list.js"]
}'>
```

複数要素に分けて宣言してもよい(マージされる)。

**B. `window.__uidevFiles` グローバル(動的追加が必要な場合)**

```html
<script>
  window.__uidevFiles = {
    html: ["pages/user_list.html"],
    js:   ["scripts/user_list.js"]
  };
</script>
```

→ ステップ6へ

#### 補足

##### 何を登録するか

- `html`: この画面を描画するページテンプレート / 部分テンプレート
- `css`:  この画面固有のスタイルシート(global reset 等は除く)
- `js`:   この画面固有のスクリプト(フレームワークランタイムは除く)

フレームワーク / ライブラリのファイルは登録しない。
「この画面をデバッグするために Claude が読む必要があるファイル」だけ入れる。
バックエンド / API 先は **意図的に登録しない** — JS ファイル内の `fetch` 呼び出しから辿れるため。
設定ファイル・データソース等も同様。

---

### ステップ6: ログ規約を適用する

#### 処理内容

プロジェクトが `ui-kit:logging` の規約に従っていることを確認する:
- ロガーオブジェクト(ハンドラ内で生 `console.log` を使わない)
- JSON Lines 出力形式
- 主要なユーザー操作・状態遷移にログを出す
- 1 行ログは短く(オブジェクトを複数行に展開しない)

プロジェクトにロガーがまだない場合は、先に `/ui-kit:logging` を実行する。

→ 完了

#### 出力

- 共通ウィジェットが画面に読み込まれている
- 関連ファイルが宣言されている
- ロガーが JSON Lines を出力している

---

## 開発者の運用フロー

1. 開発系画面をブラウザで開く
2. 🐛 ボタン(デフォルト右下、Ctrl+Shift+D でも可)をクリック
3. 必要ならログレベル/行数を調整(デフォルト: エラー以上・100 行)
4. 📋 コピー → JSON がクリップボードへ
5. Claude Code に貼り付けて「これでデバッグして」 → Claude が関連ファイルとログを合わせて読んで原因を特定

### 要素ピッカー(多選択)

モーダルヘッダの 🎯「要素選択」ボタンで要素ピッカーモードに入る。画面上の要素を**複数クリックで選択**(再クリックで解除のトグル)。
フロートボタンが **📋 N** に変身し、それをクリックすると通常コピーと**同じスキーマ**に `elements: [...]` を追加した JSON が
クリップボードへコピーされる。コピー後はピッカーを終了してトップ画面へ戻る（モーダルは再表示しない）。

XPath は短縮形式 / 相対で固定。`Esc` でキャンセル。

「これらの要素を見て直して」と Claude に伝えたいときに、依頼文と合わせて貼り付けると便利。

---

## 参考資料

- `{plugin_root}/references/principles.md` — UI 規約(必読)
- `{plugin_root}/skills/logging/SKILL.md` — ログ規約
- `{plugin_root}/skills/debug-fab/templates/CLAUDE.md` — ウィジェット使い方詳細(フォルダ作業時に自動読み込み)


---

## 必須: 生成物に出自メタデータを付与する

このスキルが書き出したファイルには、必ず出自メタデータを付与すること。
これにより `claude-kit:version-sync` が古いプラグインバージョンで生成された
ファイルを検出できるようになる。

**手順**（このスキルが作成・上書きする全ファイルに対して実行）:

1. スタンプ用スキルを起動し、このスキルの識別情報と対象ファイルを渡す:

   ```
   /claude-kit:mark-generated --plugin ui-kit --skill debug-fab --file {generated-file-path}
   ```

2. 返ってきたコメント文字列を、ファイルに YAML frontmatter がある場合は **その閉じ `---` の直後** に、frontmatter がない場合はファイル最上部に挿入する。frontmatter の前（`---` より上）には絶対に置かない。Markdown レンダラが frontmatter として認識しなくなる。
3. ファイル先頭に既に `<!-- Generated by` で始まる行（または `*.py` / `*.sh` / `*.yaml` の場合は `# Generated by`）が存在する場合は、その1行を置き換える（重複させない）。

JP ミラーファイル（`*.jp.md`）の場合は、メタデータ行と JP ミラー警告行の2行が
返ってくるので、両方をそのまま貼ること。

`mark-generated` が空文字列を返した場合（例: `*.json` はコメント不可）は、
何もせずに次へ進む。
