# v4.0.0 — 2026-05-30

## 変更内容

- `conversation-to-claude` スキルを廃止し、関連ファイルを削除した
  - `skills/conversation-to-claude/` を削除
  - `hooks/prompts/pre-compact.md`(+jp) を削除し、`hooks/hooks.json` から `PreCompact` エントリを削除
  - `CLAUDE.md` / `CLAUDE.jp.md` から `conversation-to-claude` 言及を除去
  - creator スキル 3 種（`claude-creator` / `rule-creator` / `skill-creator`）の薄ラッパー説明から
    `conversation-to-claude` 呼び出し元を除去
  - `references/glossary.md`(+jp) の「Claude が用語を追記するタイミング」節を一般化（c2c 依存を解消）

## 構造の変更

- フックは `claude-kit-references-injection`（`PreToolUse`）の 1 つのみに統合された
- `hooks/prompts/` 配下のファイルがなくなった
- `references/glossary.md` と `references/incidents.md` の format 仕様は維持（他プロジェクト用）
