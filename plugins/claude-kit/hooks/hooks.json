{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python",
            "args": [
              "-c",
              "import sys,json,pathlib,re,tempfile; d=json.loads(sys.stdin.read()); fp=d.get('tool_input',{}).get('file_path','') or ''; sys.exit(0) if not re.search(r'/skills/[^/]+/SKILL(?:\\.jp)?\\.md$', fp) else None; sid=d.get('session_id','default'); flag=pathlib.Path(tempfile.gettempdir())/f'skill-creator-dispatch-{sid}'; sys.exit(0) if flag.exists() else None; flag.touch(); p=pathlib.Path(sys.argv[1]); sys.stdout.buffer.write(json.dumps({'decision':'block','reason':p.read_text('utf-8')},ensure_ascii=False).encode('utf-8')) if p.exists() else None",
              "${CLAUDE_PLUGIN_ROOT}/hooks/prompts/skill-creator-dispatch.md"
            ]
          },
          {
            "type": "command",
            "command": "python",
            "args": [
              "-c",
              "import sys,json,pathlib,re,tempfile; d=json.loads(sys.stdin.read()); fp=d.get('tool_input',{}).get('file_path','') or ''; sys.exit(0) if not (re.search(r'\\.claude/rules/.*\\.md$', fp) and not fp.endswith('.jp.md')) else None; sid=d.get('session_id','default'); flag=pathlib.Path(tempfile.gettempdir())/f'rule-creator-dispatch-{sid}'; sys.exit(0) if flag.exists() else None; flag.touch(); p=pathlib.Path(sys.argv[1]); sys.stdout.buffer.write(json.dumps({'decision':'block','reason':p.read_text('utf-8')},ensure_ascii=False).encode('utf-8')) if p.exists() else None",
              "${CLAUDE_PLUGIN_ROOT}/hooks/prompts/rule-creator-dispatch.md"
            ]
          },
          {
            "type": "command",
            "command": "python",
            "args": [
              "-c",
              "import sys,json,pathlib,re,tempfile; d=json.loads(sys.stdin.read()); fp=d.get('tool_input',{}).get('file_path','') or ''; sys.exit(0) if not (re.search(r'(?:^|/)CLAUDE(?:\\.local)?\\.md$', fp) and not fp.endswith('.jp.md')) else None; sid=d.get('session_id','default'); flag=pathlib.Path(tempfile.gettempdir())/f'claude-creator-dispatch-{sid}'; sys.exit(0) if flag.exists() else None; flag.touch(); p=pathlib.Path(sys.argv[1]); sys.stdout.buffer.write(json.dumps({'decision':'block','reason':p.read_text('utf-8')},ensure_ascii=False).encode('utf-8')) if p.exists() else None",
              "${CLAUDE_PLUGIN_ROOT}/hooks/prompts/claude-creator-dispatch.md"
            ]
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "python",
            "args": [
              "-c",
              "import sys,json,pathlib,re,tempfile; d=json.loads(sys.stdin.read()); fp=d.get('tool_input',{}).get('file_path','') or ''; sys.exit(0) if not re.search(r'/skills/[^/]+/SKILL(?:\\.jp)?\\.md$', fp) else None; sid=d.get('session_id','default'); flag=pathlib.Path(tempfile.gettempdir())/f'skill-creator-dispatch-{sid}'; sys.exit(0) if flag.exists() else None; flag.touch(); p=pathlib.Path(sys.argv[1]); sys.stdout.buffer.write(json.dumps({'decision':'block','reason':p.read_text('utf-8')},ensure_ascii=False).encode('utf-8')) if p.exists() else None",
              "${CLAUDE_PLUGIN_ROOT}/hooks/prompts/skill-creator-dispatch.md"
            ]
          },
          {
            "type": "command",
            "command": "python",
            "args": [
              "-c",
              "import sys,json,pathlib,re,tempfile; d=json.loads(sys.stdin.read()); fp=d.get('tool_input',{}).get('file_path','') or ''; sys.exit(0) if not (re.search(r'\\.claude/rules/.*\\.md$', fp) and not fp.endswith('.jp.md')) else None; sid=d.get('session_id','default'); flag=pathlib.Path(tempfile.gettempdir())/f'rule-creator-dispatch-{sid}'; sys.exit(0) if flag.exists() else None; flag.touch(); p=pathlib.Path(sys.argv[1]); sys.stdout.buffer.write(json.dumps({'decision':'block','reason':p.read_text('utf-8')},ensure_ascii=False).encode('utf-8')) if p.exists() else None",
              "${CLAUDE_PLUGIN_ROOT}/hooks/prompts/rule-creator-dispatch.md"
            ]
          },
          {
            "type": "command",
            "command": "python",
            "args": [
              "-c",
              "import sys,json,pathlib,re,tempfile; d=json.loads(sys.stdin.read()); fp=d.get('tool_input',{}).get('file_path','') or ''; sys.exit(0) if not (re.search(r'(?:^|/)CLAUDE(?:\\.local)?\\.md$', fp) and not fp.endswith('.jp.md')) else None; sid=d.get('session_id','default'); flag=pathlib.Path(tempfile.gettempdir())/f'claude-creator-dispatch-{sid}'; sys.exit(0) if flag.exists() else None; flag.touch(); p=pathlib.Path(sys.argv[1]); sys.stdout.buffer.write(json.dumps({'decision':'block','reason':p.read_text('utf-8')},ensure_ascii=False).encode('utf-8')) if p.exists() else None",
              "${CLAUDE_PLUGIN_ROOT}/hooks/prompts/claude-creator-dispatch.md"
            ]
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "python",
            "args": [
              "-c",
              "import sys,json,pathlib,re; d=json.loads(sys.stdin.read()); fp=d.get('tool_input',{}).get('file_path',''); sys.exit(0) if not fp.endswith('.md') or fp.endswith('.jp.md') else None; pats=[r'/skills/[^/]+/SKILL\\.md$',r'\\.claude/rules/[^/]+\\.md$',r'/hooks/prompts/[^/]+\\.md$',r'(?:^|/)CLAUDE(?:\\.local)?\\.md$']; sys.exit(0) if not any(re.search(p,fp) for p in pats) else None; sys.exit(0) if pathlib.Path(fp[:-3]+'.jp.md').exists() else sys.stdout.buffer.write(pathlib.Path(sys.argv[1]).read_text('utf-8').encode())",
              "${CLAUDE_PLUGIN_ROOT}/hooks/prompts/jp-mirror-check.md"
            ]
          }
        ]
      }
    ]
  }
}
