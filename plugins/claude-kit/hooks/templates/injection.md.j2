<!-- Generated by ref-inject/apply — regenerate via /ref-inject:apply -->
# claude-kit references — auto-injected

The injection hook matched `{{ file_path }}` against `references/injection_rules.yaml`.
The **required** references are inlined in full below — follow them when working with this file.

{% if required %}
{% for ref in required %}
---

## {{ ref.path }} — {{ ref.description }}

{{ ref.body }}
{% endfor %}
{% endif %}
{% if optional %}
---

## Optional references (read with `Read` if you need them)

{% for ref in optional %}
- `{{ ref.abs_path }}` — {{ ref.description }}
{% endfor %}
{% endif %}
