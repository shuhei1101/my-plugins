# claude-kit references — auto-injected

The injection hook matched `{{ file_path }}` against `references/_injection_rules.yaml`.
The **required** references must be followed when working with this file. Ones not yet seen this
session are inlined in full; ones already injected earlier are listed by path only (re-`Read` if
they have scrolled out of context).

{% if not jp_mirror %}> `CLAUDE_KIT_JP_MIRROR=false` — Skip JP mirrors; write the main `.md` file in Japanese directly.
{% endif %}

{% if required %}
{% for ref in required %}
---

## {{ ref.path }} — {{ ref.description }}

{% if ref.cached %}
_(Already injected earlier this session — re-`Read` `{{ ref.abs_path }}` if you need it again.)_
{% else %}
{{ ref.body }}
{% endif %}
{% endfor %}
{% endif %}
{% if optional %}
---

## Optional references (read with `Read` if you need them)

{% for ref in optional %}
- `{{ ref.abs_path }}` — {{ ref.description }}
{% endfor %}
{% endif %}
