# claude-kit リファレンス — 自動注入

注入フックが `{{ file_path }}` を `references/_injection_rules.yaml` と照合しました。
以下の **required** リファレンスはこのファイルを扱う際に従ってください。本セッションで未注入の
ものは本文全量を展開し、既に注入済みのものはパスのみ記載します（コンテキストから消えていれば
再度 `Read` してください）。

{% if required %}
{% for ref in required %}
---

## {{ ref.path }} — {{ ref.description }}

{% if ref.cached %}
_（本セッションで注入済み — 必要なら `{{ ref.abs_path }}` を再度 `Read` してください）_
{% else %}
{{ ref.body }}
{% endif %}
{% endfor %}
{% endif %}
{% if optional %}
---

## 任意リファレンス（必要なら `Read` で読む）

{% for ref in optional %}
- `{{ ref.abs_path }}` — {{ ref.description }}
{% endfor %}
{% endif %}
