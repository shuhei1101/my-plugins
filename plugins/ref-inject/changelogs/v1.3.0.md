# ref-inject v1.3.0 — 2026-05-29

## 変更内容

- **`{PREFIX}_INJECTION_DISABLE` env トグルをテンプレートに追加**: 注入を完全に OFF にできる環境変数を `inject_references.py` テンプレートに実装した。`true`/`1`/`yes`/`on` を設定すると注入をスキップする（デフォルト: 注入有効）。
  - 命名は各プラグインの ENV_PREFIX を流用する（例: `PY_KIT_INJECTION_DISABLE`、`NEXT_KIT_INJECTION_DISABLE`）。
  - 既存の claude-kit/py-kit/next-kit コピーへの反映は別 PR で行う。

## 変更ファイル

- `templates/hooks/inject_references.py` — `{PREFIX}_INJECTION_DISABLE` チェックを追加
