---
name: create
description: |
  Generate a new reference auto-injection plugin (a "*-kit" style plugin that injects edit-path-matched references via a PreToolUse hook). Claude reads the ref-inject templates and writes the new plugin's files itself (hooks: inject_references.py + refresh_on_compact.py + hooks.json, Jinja2 templates, references/ skeleton), then registers it in marketplace.json.
  Trigger when the user says "リファレンス注入プラグインを作って", "ref-inject で新しいプラグインを作って", "create a reference injection plugin", "make an auto-injection kit", or invokes /ref-inject:create explicitly.
---
<!-- Generated by ref-inject/create -->

# ref-inject:create — Generate a reference-injection plugin

Scaffolds a new plugin from the `ref-inject` templates. **Claude reads each template and
writes the destination file itself** (substituting placeholders as it goes) — there is no
generator script, so the structure stays in context and is easy to adapt per plugin.

The generated plugin ships a `PreToolUse(Edit | Write | MultiEdit | Read)` hook that matches
the edited file path against `references/injection_rules.yaml` and injects matched references:

- `required` → **full body** injected into the `decision: block` reason
- `optional` → **path + description only** (Claude reads on demand)

De-duped by a per-pattern TTL token (`~/.claude/tokens/{plugin}/{session_id}.yaml`), and
refreshed after `/compact` by a `PreCompact` hook.

---

## Tasks

### Step 1: Collect inputs and derive values

#### Condition

- Always — run first

#### Process

Determine (ask the user only for what is missing):

- **name**: plugin name in kebab-case (e.g. `vue-kit`, `django-kit`)
- **description**: one-line plugin description
- **ttl** (optional): default re-injection interval in seconds. Default `3600`

Then derive the placeholder values:

| Placeholder | Value | Example (`vue-kit`) |
|---|---|---|
| `__PLUGIN_NAME__` | `name` | `vue-kit` |
| `__ENV_PREFIX__` | `name` upper-cased, every run of non-alphanumeric → `_` | `VUE_KIT` |
| `__LOG_TAG__` | `{name}-references-injection` | `vue-kit-references-injection` |
| `__DEFAULT_TTL__` | `ttl` (number) | `3600` |
| `__PLUGIN_DESCRIPTION__` | `description` | … |

→ Proceed to Step 2

---

### Step 2: Copy each template, substituting placeholders

#### Condition

- Step 1 complete

#### Process

For **every** file under `${CLAUDE_PLUGIN_ROOT}/templates/`, `Read` it and `Write` it to the
destination below, replacing all five placeholders with the derived values. Replace placeholders
in text files (`.py` `.json` `.md` `.yaml` `.yml` `.j2`); copy any binary file verbatim.

| Template (under `templates/`) | Destination (under `plugins/{name}/`) |
|---|---|
| `plugin.json` | `.claude-plugin/plugin.json` |
| `CLAUDE.md` / `CLAUDE.jp.md` | `CLAUDE.md` / `CLAUDE.jp.md` |
| `hooks/inject_references.py` | `hooks/inject_references.py` |
| `hooks/refresh_on_compact.py` | `hooks/refresh_on_compact.py` |
| `hooks/hooks.json` | `hooks/hooks.json` |
| `hooks/templates/injection.md.j2` / `injection.jp.md.j2` | `hooks/templates/…` (same names) |
| `references/index.yaml` / `index.jp.yaml` | `references/…` (same names) |
| `references/injection_rules.yaml` | `references/injection_rules.yaml` |
| `references/CLAUDE.md` / `CLAUDE.jp.md` | `references/…` (same names) |
| `references/example/getting-started.md` | `references/example/getting-started.md` |

Notes:
- The only path relocation is `plugin.json` → `.claude-plugin/plugin.json`; everything else mirrors the template path.
- Leave `${CLAUDE_PLUGIN_ROOT}` in `hooks.json` literal — Claude Code expands it at runtime.
- After writing, confirm no `__PLACEHOLDER__` token remains (grep the new plugin dir).

→ Proceed to Step 3

---

### Step 3: Register in marketplace.json

#### Condition

- Step 2 complete

#### Process

Add an entry to `.claude-plugin/marketplace.json` `plugins` array (skip if `name` already present):

```json
{
  "name": "{name}",
  "source": "./plugins/{name}",
  "description": "{description}",
  "version": "0.1.0"
}
```

→ Proceed to Step 4

---

### Step 4: Report and hand off

#### Condition

- Step 3 complete

#### Process

1. Report the created plugin path and the files written.
2. Tell the user the remaining manual steps:
   - Replace `references/example/` with real reference docs (1 reference = 1 use case)
   - Fill `references/index.yaml` (+ `index.jp.yaml`) with each doc's path + description
   - Bind edit-path patterns to references in `references/injection_rules.yaml`
   - Optionally set `{ENV_PREFIX}_INJECTION_TTL` in `settings.json` `env`
3. Do **not** hand-edit the hook mechanism per plugin — change the `ref-inject` templates and re-run `/ref-inject:create` instead.

#### Notes

- The hook needs `PyYAML` and `Jinja2` in the project where the generated plugin runs.
