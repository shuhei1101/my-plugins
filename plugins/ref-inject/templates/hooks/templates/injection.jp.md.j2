<!-- Generated by ref-inject/apply — regenerate via /ref-inject:apply -->
# __PLUGIN_NAME__ リファレンス — 自動注入

注入フックが `{{ file_path }}` を `references/injection_rules.yaml` と照合しました。
以下の **required** リファレンスは本文全量を展開しています。このファイルを扱う際は従ってください。

{% if required %}
{% for ref in required %}
---

## {{ ref.path }} — {{ ref.description }}

{{ ref.body }}
{% endfor %}
{% endif %}
{% if optional %}
---

## 任意リファレンス（必要なら `Read` で読む）

{% for ref in optional %}
- `{{ ref.abs_path }}` — {{ ref.description }}
{% endfor %}
{% endif %}
