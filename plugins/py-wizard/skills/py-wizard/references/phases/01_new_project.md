# フェーズ2: 新規プロジェクト作成

---

## ステップ2.1（要件ヒアリング）

**条件**: 新規プロジェクト作成モードが選択された場合

**入力**: ユーザーが第一次情報を自由入力

**AI実施タスク**:
- ユーザーの第一次情報を受け取り、不足情報を洗い出す
- `references/hearing_template.md` の固定ヒアリングシートをもとに、未回答項目を質問する

**出力**:
- 受け取った情報のまとめ
- ヒアリングシート（既知の項目は埋めた状態で提示）
- 💡 **Tips**: 入力時は `/copy` コマンドを使用して入力フォームを活用してください

**選択肢**: なし（自由入力で回答してもらう）

**補足**:
- 回答後、未回答・情報不足の項目がある場合はこのステップを再実行して再確認
- 全項目が埋まったらステップ2.2へ進む
- AIおすすめがある項目は `→ (おすすめ: XXX)` で提示

---

## ステップ2.2（要件まとめ確認）

**条件**: ステップ2.1のヒアリングが完了した場合

**入力**: -

**AI実施タスク**:
- ヒアリング結果をそのまま整理してまとめる（装飾・構造化しすぎない）
- 他ツールとの連携がある場合、その旨も含める
- `~/.claude/skill-memory/py-wizard/{YYYYMMDDHHMMSS}_session.md` にセッション情報を保存

**出力**:
- ヒアリング結果の要件まとめ（箇条書きベース、シンプルに）
- 「この内容で合っていますか？」

**選択肢**:
1. OK → README作成へ
2. 修正・補足を伝える → 修正して再提示
3. ヒアリングに戻る → ステップ2.1へ
4. その他（入力してください）

**補足**:
- この段階ではREADME形式にまとめない。あくまでヒアリング内容の確認のみ
- 詳細な構造化はREADME作成ステップ（2.3〜2.7）で行う

---

## ステップ2.3（README — 概要）

**条件**: ステップ2.2で要件確認が完了した場合

**入力**: -

**AI実施タスク**:
- ヒアリング結果からプロジェクトの概要文を起案する
- 未確定部分があればAIおすすめを添える

**出力**:
- README「概要」セクションの案（箇条書き+インデント形式）
- 不足があれば `💡 おすすめ: XXX` で明示

**選択肢**:
1. OK → 次のREADMEセクション（機能一覧）へ
2. 修正内容を伝える → 修正して再提示
3. その他（入力してください）

---

## ステップ2.4（README — ツール仕様）

**条件**: ステップ2.3完了後

**入力**: -

**AI実施タスク**:
- ヒアリング結果から以下の固定カテゴリで仕様を整理する:
  - **機能一覧**: 主要機能を箇条書き
  - **入出力**: 入力形式・出力形式・対応フォーマット等
  - **パフォーマンス**: 処理速度・並列処理・リソース管理等
  - **インターフェース**: GUI/CLI/API等の利用方法
  - **設定**: 設定項目・環境変数・デフォルト値等
- 不足していそうな項目があればAIおすすめとして提案

**出力**:
- README「ツール仕様」セクションの案（カテゴリ別・箇条書き+インデント形式）
- 不足があれば `💡 おすすめ: XXX` で明示

**選択肢**:
1. OK → ツール構成フロー図へ
2. 修正内容を伝える → 修正して再提示
3. その他（入力してください）

---

## ステップ2.5（README — ツール構成フロー図）

**条件**: ステップ2.4完了後

**入力**: -

**AI実施タスク**:
- `~/.claude/skill-memory/{ツール名}/diagram/` フォルダを作成（存在しない場合）
- `/diagram-wizard` を起動し、ツール全体の処理フローを Mermaid フロー図で作成する
- 粒度は粗めでOK — 主要な入力・処理ブロック・出力の流れがわかるレベル
- 機能一覧セクションのトップに配置する前提で作成する
- 図案をユーザーに提示する前に、以下の形式で保存:
  - ファイルパス: `~/.claude/skill-memory/{ツール名}/diagram/{YYYYMMDDHHMMSS}_flow_diagram.md`
  - 内容:
    ```markdown
    # ツール構成フロー図 — {ツール名}
    
    ## 生成日時
    {YYYY年MM月DD日 HH:MM:SS}
    
    ## 図案
    
    ```mermaid
    [生成したMermaid図]
    ```
    
    ## 説明
    [図の説明やポイント]
    ```
- 保存失敗時はエラーをログ出力して処理続行

**出力**:
- Mermaid フロー図（ツール構成の概要）
- README「機能一覧」セクションの冒頭に挿入する形で提示
- 「検討図を保存しました: {ファイルパス}」と明示

**選択肢**:
1. OK → 次のREADMEセクション（開発技術）へ
2. 修正内容を伝える → 修正して再提示
3. その他（入力してください）

---

## ステップ2.6a（技術候補の調査）

**条件**: ステップ2.5完了後

**入力**: -

**AI実施タスク**:
- ヒアリング結果から必要な技術カテゴリを特定する（音声変換、動画処理、GUI、AI API、ファイル操作、並列処理等）
- **ユーザー指定の技術**と**未指定のカテゴリ**を明確に分離する
- 未指定のカテゴリについて、**MCP（context7）やWeb検索を必ず実行**し、最新情報を調査する（省略不可）
  - 代替技術・競合技術を複数リストアップ
  - 各技術の特徴・メリット・デメリットを簡潔にまとめる
- 連携先ツールがある場合、その旨も含める
- 代替候補がある場合、その中から**1つをAIが選定して推奨する**
  - 推奨理由を簡潔に（1-2行で）記載する
  - ユーザー指定技術がある場合は、それを尊重しつつ代替候補の中から推奨

**出力**:
ユーザー指定技術があるカテゴリについて、以下の構造で出力：

```markdown
### カテゴリ名

- **技術名** `[ユーザ指定]`
  - 説明
  - 特徴・メリット・デメリット

#### 代替候補

- **技術A** `[代替候補]`
  - 説明
  - メリット: XXX
  - デメリット: XXX

- **技術B** `[代替候補]`
  - 説明
  - メリット: XXX
  - デメリット: XXX

#### 💡 AI推奨
**技術名**を推奨します。理由: （推奨理由を1-2行で記載）
```

- 代替候補がある場合のみ「💡 AI推奨」セクションを表示
- 「この調査結果をもとに技術を選定します」

**選択肢**: なし（自動的にステップ2.6bへ進む）

**補足**:
- MCP（context7）やWeb検索は必須。最新の技術トレンド・ライブラリのメンテナンス状況を反映すること
- 候補は各カテゴリで2〜3個程度リストアップ

---

## ステップ2.6b（技術選定の確認）

**条件**: ステップ2.6a完了後

**入力**: -

**AI実施タスク**:
- ステップ2.6aの調査結果をもとに、最終的な技術スタックを提案する
- ユーザー指定技術を優先しつつ、未指定部分はAI推奨技術で補完
- 各技術の選定理由を簡潔に添える

**出力**:
- 最終技術スタック案（`[ユーザ指定]` / `[推奨]` ラベル付き）
- 各技術の選定理由（1行程度）
- 「この技術構成で合っていますか？」

**選択肢**:
1. OK → ステップ2.6cへ
2. 技術を変更する（候補から選択 or 別の技術を指定）→ 修正して再提示
3. その他（入力してください）

**補足**:
- ユーザー指定技術は必ず採用すること
- 変更があった場合は理由を確認し、調整した上で再提示

---

## ステップ2.6c（README — 開発技術セクション作成）

**条件**: ステップ2.6b完了後

**入力**: -

**AI実施タスク**:
- 確定した技術スタックをREADME「開発技術」セクション形式でまとめる
- 箇条書き+インデント形式で、用途付きで列挙
- `[ユーザ指定]` / `[推奨]` ラベルは最終版では省略（内部管理用）
- 連携先ツールがある場合、その旨も記載

**出力**:
- README「開発技術」セクションの案（箇条書き+インデント形式）
- 「この内容で合っていますか？」

**選択肢**:
1. OK → 次のREADMEセクション（フォルダ構成）へ
2. 修正内容を伝える → 修正して再提示
3. その他（入力してください）

**補足**:
- README各セクションの出力形式は `references/readme_guide.md` の共通ルール参照
- ラベル `[ユーザ指定]` / `[推奨]` はステップ2.6bまでの内部管理用で、最終READMEには含めない

---

## ステップ2.7（README — フォルダ構成）

**条件**: ステップ2.6完了後

**入力**: -

**AI実施タスク**:
- ヒアリング結果と機能一覧からフォルダ構成を起案する
- `references/python_rules.md` の基本テンプレートをベースに、プロジェクトに合わせて調整
- 任意フォルダ（input/, output/, cache/）の必要性を判断して含める
- log/ フォルダは固定で含める
- 機能ごとのサブフォルダ分割を提案
- **命名規則を反映**: インターフェース/抽象クラスと実装の関係がある場合、`references/python_rules.md` の命名規則に従った例を提示
- 不足があればAIおすすめを添える

**出力**:
- README「フォルダ構成」セクションの案（ツリー形式）
- 不足があれば `💡 おすすめ: XXX` で明示

**選択肢**:
1. OK → 次のREADMEセクション（使い方・インストール方法）へ
2. 修正内容を伝える → 修正して再提示
3. その他（入力してください）

---

## ステップ2.8（README — 使い方・インストール方法）

**条件**: ステップ2.7完了後

**入力**: -

**AI実施タスク**:
- インストール方法を起案（**Windowsユーザ向け、Pythonインストールから記載**）
  - Python 3.11以上のインストール手順
  - `pip install .` 等のパッケージインストール
  - setup/フォルダのセットアップ用batの使い方
- 使い方を起案:
  - 起動方法（GUI / CLI / batファイル）
  - 設定方法（.envファイル、GUI設定画面からの変更）
  - 連携先ツールがある場合、パス設定の説明も含める
- 不足があればAIおすすめを添える

**出力**:
- README「インストール方法」セクションの案（箇条書き+インデント形式）
- README「使い方」セクションの案（箇条書き+インデント形式）
- 不足があれば `💡 おすすめ: XXX` で明示

**選択肢**:
1. OK → README最終確認へ
2. 修正内容を伝える → 修正して再提示
3. その他（入力してください）

---

## ステップ2.9（README最終確認）

**条件**: ステップ2.8完了後

**入力**: -

**AI実施タスク**:
- ステップ2.3〜2.8で確定した各セクションを結合してREADME全文を生成
- ライセンス（MIT）を自動付与
- 全体の整合性チェック

**出力**:
- README.md全文のプレビュー

**選択肢**:
1. OK → 作成場所の確認へ
2. 特定セクションを修正する（セクション名を指定）→ 該当ステップに戻る
3. 全体的に修正を伝える
4. その他（入力してください）

---

## ステップ2.10（作成場所の確認）

**条件**: ステップ2.9完了後

**入力**: -

**AI実施タスク**: -

**出力**:
- 「プロジェクトの作成場所を指定してください」
- デフォルト候補: カレントディレクトリ

**選択肢**:
1. パスを指定する
2. カレントディレクトリに作成する
3. その他（入力してください）

---

## ステップ2.11（プロジェクト雛形生成）

**条件**: ステップ2.10で作成場所が確定した場合

**入力**: -

**AI実施タスク**:
- 以下のファイル・フォルダを生成する（`references/python_rules.md` に従う）:
  - `{package_name}/` パッケージフォルダ（機能サブフォルダ含む）
  - `{package_name}/config.py` — 設定読み込み（上部でデフォルト値定義 → 下部で.env読み込み上書き）
  - `{package_name}/main.py` — 引数処理・起動分岐（高レベルのみ）
  - `{package_name}/gui.py` — tkinter GUI（青い実行ボタン、設定モーダル）
  - `{package_name}/logger.py` — logger初期化・設定集約（**必須**: `setup_logger()` で `StreamHandler` + `FileHandler(log/YYYYMMDD_HHMMSS_{package_name}.log)` を両方アタッチ（実行ごとにタイムスタンプ付き新規ファイル、過去実行を上書きしない）、`LOG_DIR.mkdir(parents=True, exist_ok=True)` でフォルダ自動作成。参考: `voice-paste/voice_paste/logger.py`）
  - `{package_name}/exceptions.py` — カスタム例外クラス
  - `{package_name}/constants.py` — 定数定義（**必須**: `LOG_DIR = PROJECT_ROOT / "log"` を含める）
  - `{package_name}/utils.py` or `common/` — 共通ユーティリティ
  - `{package_name}/cli.py` — argparse処理
  - `run.bat` — プロジェクトルートのメインランチャー（**必須**: `log/<タイムスタンプ>_run_bat.log`（実行ごとにタイムスタンプ付き新規ファイル、PowerShell `Get-Date` で生成・詳細は `references/python_rules.md`）へ stdout/stderr を追記リダイレクト、venv自動有効化、非ゼロ終了時は `pause` でウィンドウ維持。参考: `voice-paste/run.bat`）
  - `bat/` — batファイル一式（gui.bat + モード別bat、いずれも `run.bat` と同じログ記録方式を踏襲）
  - `setup/` — セットアップ用bat一式（Python環境構築含む）
  - `tests/` — テストフォルダ（機能ミラー構成）
  - `tests/mocks/` — モックファイル置き場
  - `log/.gitkeep` — ログフォルダ（空フォルダは.gitkeepで管理）
  - `resources/` — GUIリソース等
  - `input/.gitkeep` — 入力フォルダ（必要な場合のみ、.gitkeepで管理）
  - `output/.gitkeep` — 出力フォルダ（必要な場合のみ、.gitkeepで管理）
  - `cache/.gitkeep` — キャッシュフォルダ（必要な場合のみ、.gitkeepで管理）
  - `.env.sample` — 環境変数テンプレート（キー空）
  - `.gitignore` — 適切な除外設定（.env, __pycache__, log/, cache/ 等）
  - `README.md` — ステップ2.9で確定した内容
  - `pyproject.toml` — Python>=3.11、依存ライブラリ~=指定
- 抽象クラス・インターフェースの雛形も配置
  - **命名規則に従う**: `references/python_rules.md` の命名規則セクション参照（`~able.py` / `base_~.py` / `i_~.py` 等）
  - ファイル名コメントに命名パターンを明記（例: `# Interface: media_convertable.py`, `# Implementation: ffmpeg_converter.py`）
- 言語使用ルールに従う（`references/python_rules.md` の言語使用ルール参照）:
  - print文・logger出力: すべて英語
  - コメント・docstring: すべて日本語
  - GUI表示文言: 日本語
- loggerの設定（ログレベル変更可能、がっつりログ出力）
  - `main.py` / `__main__.py` の起動直後で `setup_logger()` を呼び、以降の処理は必ずロガー経由で追跡可能にする
  - ファイル出力先は `log/YYYYMMDD_HHMMSS_{package_name}.log`（実行ごとに新規ファイル。bat ログと同じタイムスタンプ粒度にすることでソート順で相関可能。フォルダはロガー側で自動作成）
- `.bat` ランチャーはサイレント失敗しないよう、stdout/stderr を `log/<タイムスタンプ>_run_bat.log`（実行ごとに新規・PowerShell `Get-Date` ベースで生成）に追記し、失敗時は `pause` でウィンドウを維持する。長時間コマンド（PyInstaller ビルド等）は PowerShell パイプ（`Write-Host` + `Add-Content -Encoding utf8`、CP932 環境の文字化け対策込み。`Tee-Object` は PS 5.1 で `-Encoding` 非対応のため使わない）でコンソールにも同時出力すること（`references/python_rules.md` の「.bat ランチャーログ」節参照）
- **【ASCII限定】`.bat` ファイルの中身は ASCII のみ**（日本語コメント・echo文字列禁止）。`cmd.exe` は bat をシステムANSIコードページ（CP932）でパースするため、UTF-8保存した日本語バイトが CP932 リードバイトとして誤認され、続くコマンドが壊れる（実例: `setlocal` 直前に日本語コメントがあると `'etlocal' is not recognized` になる）。日本語の説明は `README.md` に書くこと
- .env未存在時のフォールバック処理を含める（.env.sampleをコピー、キー空）
- 連携先ツールがある場合、デフォルトパスを設定済みにする
- `~/.claude/skill-memory/py-wizard/{YYYYMMDDHHMMSS}_session.md` を更新

**出力**:
- 生成したファイル一覧
- 「以下のファイルを生成しました」

**選択肢**:
1. OK → GUI設計へ
2. 生成内容を確認する（ファイル名を指定）
3. 修正内容を伝える → 修正して再生成
4. その他（入力してください）

**補足**:
- GUI設定画面ですべての設定項目を変更可能にする
- 実行後に反映できない設定は赤字で「再起動後に適用されます」と表示
- 連携先ツールがある場合、デフォルトパスを特定ディレクトリ配下で設定済みにする

---

## ステップ2.12（GUI設計）

**条件**: ステップ2.11完了後

**入力**: -

**AI実施タスク**:
- プロジェクトの機能に合わせたGUIレイアウト案を3つ起案する
- 各案に名前をつける（例: 「シンプルモード」「タブ切替モード」「サイドバーモード」）
- アスキーアートで各案のイメージを表示
- 共通要素:
  - 青色の実行ボタン
  - 設定ボタン（クリックで設定モーダル表示）

**出力**:
- 3つのGUI案をそれぞれ名前＋アスキーアートで提示

**選択肢**:
1. 指定した案で決定する（番号で指定）→ 選択した案の詳細アスキーアートを表示
2. 複数の案を組み合わせたい → 修正して再提示
3. 自分でイメージを伝える
4. その他（入力してください）

**補足**:
- 設定モーダルのアスキーアートも選択後に提示する
- 設定モーダルには全設定項目を含め、実行後に反映できない項目は赤字注記の旨を明記

---

## ステップ2.13（プロジェクト専用スキル生成）

**条件**: ステップ2.12完了後

**入力**: -

**AI実施タスク**:
- プロジェクトの構造を記したスキルファイルを生成する（テンプレート: `assets/project_skill_template.md`）
- 機能ごとのセクションで、ファイル名・概要・ファイルパスのみを記載
- コンテキスト節約のため、コード内容や詳細説明は含めない
- 生成先: プロジェクトルート直下

**出力**:
- 生成したプロジェクト専用スキルのプレビュー

**選択肢**:
1. OK → 実装フェーズ移行確認へ
2. 修正内容を伝える → 修正して再提示
3. その他（入力してください）

**補足**:
- このスキルは改修モード（フェーズ3）で読み込まれ、既存コードの把握に使う
- コード実装・テスト完了時（ステップ4.5）に更新される

---

## ステップ2.14（実装フェーズ移行確認）

**条件**: ステップ2.13完了後

**入力**: -

**AI実施タスク**: -

**出力**:
- 「新規プロジェクト作成が完了しました。実装フェーズに移行しますか？」
- 生成済みファイルの一覧サマリー

**選択肢**:
1. 実装フェーズに移行する → ステップ3.1へ
2. 完了する（実装は別セッションで行う）
3. 生成内容を確認・修正する → 該当ステップに戻る
4. その他（入力してください）
